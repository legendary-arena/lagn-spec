# EC-140 — Webhook → Entitlement Fulfillment Processor (Execution Checklist)

**Source:** docs/ai/work-packets/WP-134-webhook-entitlement-fulfillment.md
**Layer:** Server (`apps/server/src/billing/` extension — new fulfillment processor + webhook-handler modification + `apps/server/src/billing/billing.routes.test.ts` extension) + Recovery script (`scripts/process-stripe-events.mjs` — new, ESM, run-once-per-cron) + Reference (`docs/ai/REFERENCE/api-endpoints.md` per D-11804 replace-whole-row + Library-only append)

**Execution Authority:**
This EC is the authoritative execution checklist for WP-134.
Implementation must satisfy every clause exactly.
Failure to satisfy any item below is a failed execution of WP-134.

---

## §0 — Pre-Flight

- [ ] WP-052 complete: `identity.types.ts` exports `AccountId` (branded `string`), `Result<T>`, `DatabaseClient`. `Result<T>` shape: success `{ ok: true; value: T }`; failure `{ ok: false; reason: string; code: <closed-union> }`. WP-134 reuses verbatim — no new envelope.
- [ ] WP-115 complete: long-lived `pg.Pool` constructed once in `startServer()`, threaded into `register*Routes(...)` registration helpers; webhook route already receives `pool` via WP-133's deps bundle.
- [ ] WP-118 complete: `docs/ai/REFERENCE/api-endpoints.md` exists with closed-set `Status` / `Auth` headers; D-11804 replace-whole-row update obligation locked.
- [ ] WP-132 complete: `legendary.entitlements` table exists (migration `011` applied); `EntitlementKey` closed union exported from `apps/server/src/entitlements/entitlements.types.ts`; partial unique index `entitlements_active_unique ON (player_id, entitlement_key) WHERE revoked_at IS NULL` is the WP-134 idempotency primitive — confirm conflict-target column names match by reading the migration before drafting the INSERT clause (per #ENT-FK below).
- [ ] WP-133 complete: `apps/server/src/billing/` quintet shipped (`billing.types.ts` / `billing.config.ts` / `billing.logic.ts` / `billing.routes.ts` + tests); `legendary.stripe_events` and `legendary.stripe_checkout_sessions` exist (migration `012` applied per EC-136 D-13302 Option A FK form `account_id text REFERENCES legendary.players(ext_id)`); `BillingConfig.priceAllowlist: ReadonlyMap<string, EntitlementKey>` is `Object.freeze`-wrapped; `recordStripeEvent` returns `{ inserted: boolean; row: StripeEventRecord }`; webhook handler currently records-and-returns-200 with zero classification.
- [ ] **#ENT-FK pre-flight check (RESOLVED at WP level).** WP-134 §IMPORTANT — Entitlements FK Resolution + §A Phase 3 step 6 lock the canonical post-correction form: `accountId → player_id` resolved via `SELECT player_id FROM legendary.players WHERE ext_id = $1 LIMIT 1` before the INSERT (WP-104 / EC-135 two-query pattern). INSERT column list is `(player_id, entitlement_key, source, source_ref)`. Documented in D-13403. The miss case (resolved player not found, despite FK CASCADE making this impossible-in-theory) returns `'cross_validation_failed'` per the locked semantic.
- [ ] **#CONFLICT-TARGET pre-flight check (RESOLVED at WP level).** WP-134 §IMPORTANT + §A Phase 3 step 8 lock `ON CONFLICT (player_id, entitlement_key) WHERE revoked_at IS NULL DO NOTHING RETURNING id`, matching the shipped partial unique index `entitlements_active_unique` byte-for-byte. The earlier `(account_id, ...)` shorthand is superseded.
- [ ] **PS-1 pre-flight check (RESOLVED).** WP-134 §C Branch 1 now specifies a shared local helper `loadStripeEventRecordByEventId(pool, eventId): Promise<StripeEventRecord | null>` used by BOTH the newly-inserted branch and the duplicate-delivery branch to fetch the row by `event_id` after `recordStripeEvent` returns. The shipped `recordStripeEvent` is unchanged (NOT modified to add `RETURNING *`); path (a) re-fetch via SELECT is locked in D-13403. WP-133 contract surface preserved.
- [ ] **PS-2 pre-flight check (RESOLVED).** WP-134 §A Phase 0a now specifies a local structural type guard `isCheckoutSessionCompletedPayload(payload: unknown): payload is { ... }` that runs BEFORE Phase 1's existing 3 guards and narrows the `payload: unknown` field. Shape-mismatch failures map to existing `'cross_validation_failed'` (no new error code). The guard has 5 dedicated tests bundled into the Guards-domain test set.
- [ ] **PS-3 pre-flight check (RESOLVED).** WP-134 §D now distinguishes startup-phase posture (missing env vars exit non-zero with code 2) from scan-loop posture (per-row errors exit 0 with stderr logging). Pool teardown via `try { ... } finally { await pool.end(); }` envelope. Stripe SDK confinement preserved (recovery script imports `loadBillingConfig` from `billing.config.ts`, never directly from `'stripe'`).
- [ ] Five executor decisions land in writing **before coding**: **D-13401** (synchronous-on-webhook posture; recommended (a)), **D-13402** (response shape extension `{processed, reason}`; recommended (a)), **D-13403** (two-axis cross-validation + `accountId → player_id` resolution mapping; recommended two-axis (a) + chosen FK-resolution path), **D-13404** (always-200 on signature-verified events; recommended (a)), **D-13405** (recovery script scheduling: manual + Render Cron @ 15min cadence; recommended (a)).
- [ ] `pnpm --filter @legendary-arena/server build` exits 0; `pnpm --filter @legendary-arena/server test` exits 0 on `main` HEAD. Reconcile suite/test counts if any baseline shifted between EC draft and execution. Engine baseline `679 / 148 / 0` UNCHANGED at WP-134 close (engine never touched — server-layer-only WP).
- [ ] `apps/server/src/billing/processStripeEvent.logic.ts` does NOT yet exist.
- [ ] `scripts/process-stripe-events.mjs` does NOT yet exist.
- [ ] No new migration. `git diff --name-only data/migrations/` is empty at session close.
- [ ] No new npm dependencies. `git diff apps/server/package.json` shows no `dependencies` block change; `pnpm-lock.yaml` may shift only if `pnpm install` is run incidentally.

## §1 — Scope Lock + File Allowlist

Nine files plus governance ledgers.

- `apps/server/src/billing/processStripeEvent.logic.ts` — **new** — fulfillment processor (3-phase: early-return guards / cross-validation / transactional fulfillment write)
- `apps/server/src/billing/processStripeEvent.logic.test.ts` — **new** — `node:test` branch coverage (13 branches per WP §B + recovery-selectability assertions)
- `apps/server/src/billing/billing.routes.ts` — **modified** — webhook handler calls `processStripeEvent` synchronously after `recordStripeEvent`; two branches (newly-inserted vs duplicate-delivery self-heal); deps bundle gains `billingConfig` (already constructed at startup; no shape change required)
- `apps/server/src/billing/billing.routes.test.ts` — **modified** — extend webhook tests for new response shape, self-heal branch, skip branch, always-200 fault path
- `scripts/process-stripe-events.mjs` — **new** — recovery script (`pg.Pool` + `loadBillingConfig` + LIMIT 100 unprocessed-events scan + per-row `processStripeEvent` invocation + stdout summary)
- `docs/ai/REFERENCE/api-endpoints.md` — **modified** — replace WP-133 webhook row whole per D-11804 (response schema gains `processed` + `reason`; `Authorizing WP` becomes `WP-133, WP-134`); append Library-only row for `processStripeEvent`
- `docs/ai/STATUS.md` — **modified** — append `### WP-134 / EC-140 Executed` block (full purchase-to-entitlement loop now live for cosmetic SKUs)
- `docs/ai/DECISIONS.md` — **modified** — D-13401..D-13405 inserted in numeric order
- `docs/ai/work-packets/WORK_INDEX.md` — **modified** — check off WP-134 with date + commit hash
- Plus governance: `EC_INDEX.md` (this row flips Draft → Done)

`git diff --name-only` lists exactly these 9 production/reference files at session close (10 if EC_INDEX flip lands in the same commit per single-commit topology; 5 + 4 governance if two-commit topology used).

## §2 — Locked Values (do not re-derive)

- **`processStripeEvent` signature (verbatim from WP §Locked contract values):**
  ```
  processStripeEvent(args: {
    eventRecord: StripeEventRecord;
    billingConfig: BillingConfig;
    database: DatabaseClient;
  }): Promise<FulfillmentResult>
  ```
- **`FulfillmentResult` shape (matches WP-052 `Result<T>` verbatim):** `{ ok: true; value: FulfillmentSuccess } | { ok: false; reason: string; code: FulfillmentErrorCode }`. `FulfillmentSuccess = { entitlementGranted: boolean; entitlementKey: EntitlementKey | null; sessionId: string | null; reason: FulfillmentSuccessReason }`.
- **`FulfillmentSuccessReason` closed union (5 values, verbatim):** `'fulfilled' | 'duplicate' | 'unhandled_event_type' | 'unpaid_session' | 'already_processed'`.
- **`FulfillmentErrorCode` closed union (6 values, verbatim):** `'session_lookup_failed' | 'cross_validation_failed' | 'price_not_in_allowlist' | 'entitlement_insert_failed' | 'event_update_failed' | 'session_update_failed'`.
- **Idempotent INSERT clause (with #CONFLICT-TARGET correction):** `INSERT INTO legendary.entitlements (player_id, entitlement_key, source, source_ref) VALUES ($1, $2, 'stripe', $3) ON CONFLICT (player_id, entitlement_key) WHERE revoked_at IS NULL DO NOTHING RETURNING id`. `$1` = bigint `player_id` resolved via the `SELECT player_id FROM legendary.players WHERE ext_id = $1 LIMIT 1` two-query pattern at Phase 3 step 6; `$2` = `sessionRow.entitlement_key` (NOT event metadata); `$3` = Stripe Checkout Session ID (`cs_*`). `RETURNING` row count distinguishes `'fulfilled'` (1 row) from `'duplicate'` (0 rows).
- **Phase 0a structural type guard (locked, post-PS-2):** `isCheckoutSessionCompletedPayload(payload: unknown): payload is { data: { object: { id: string; client_reference_id: string; metadata: { entitlementKey: string }; payment_status: string } } }`. Local function in `processStripeEvent.logic.ts`, NOT exported. Strict shape check — all four fields must be present and primitive-typed. Returns `false` on any mismatch → caller returns `Result.fail({ code: 'cross_validation_failed', reason: 'event payload shape did not match the expected checkout.session.completed structure …' })`. Set `process_error` + leave `processed_at = NULL` per the locked failure semantic.
- **Five-axis cross-validation (post-RS-4 — extends from four to five checks; fail-fast on first mismatch):** (1) `sessionRow.intent_status === 'open'` → else `'session_lookup_failed'` (refuses fulfillment against `'expired'` / `'canceled'` / `'completed'` sessions); (2) session row exists for event session ID → else `'session_lookup_failed'`; (3) `payload.data.object.client_reference_id === sessionRow.account_id` → else `'cross_validation_failed'`; (4) `payload.data.object.metadata.entitlementKey === sessionRow.entitlement_key` → else `'cross_validation_failed'`; (5) `priceAllowlist.has(sessionRow.price_id)` AND `priceAllowlist.get(sessionRow.price_id) === sessionRow.entitlement_key` → else `'price_not_in_allowlist'`.
- **`accountId → player_id` resolution (post-#ENT-FK correction):** `SELECT player_id FROM legendary.players WHERE ext_id = $1 LIMIT 1` runs as Phase 3 step 6 (BEFORE the entitlement INSERT). Miss → `Result.fail({ code: 'cross_validation_failed', reason: 'session row references a player ext_id that does not exist in legendary.players' })`. Mirrors the WP-104 / EC-135 `ownerProfile.logic.ts:123` two-query pattern.
- **Shared row-fetch helper (post-PS-1):** `loadStripeEventRecordByEventId(pool, eventId): Promise<StripeEventRecord | null>`. Local to `billing.routes.ts`, NOT exported. SELECTs the 7 columns (`id, event_id, event_type, payload, received_at, processed_at, process_error`) and maps snake_case → camelCase. Returns `null` on miss (concurrent inconsistency edge case → handler returns 500 `internal_error`). Used by BOTH webhook branches; eliminates duplicate SELECT logic. Consumes the long-lived `pool` — never constructs a new one.
- **`source` value:** `'stripe'` (literal string, member of WP-132 D-13204 closed union `'stripe' | 'admin_grant' | 'comp'`).
- **`source_ref` value:** `<stripe_session_id>` from `eventRecord.payload.data.object.id`.
- **`processed_at` lifecycle (locked, verbatim):** set ONLY on terminal success or intentional no-op (`'fulfilled'` | `'duplicate'` | `'unhandled_event_type'` | `'unpaid_session'` | `'already_processed'`). On any `Result.fail`, `process_error = <full-sentence message>` AND `processed_at = NULL` so the recovery script's `WHERE processed_at IS NULL` selector continues to surface the row. Validation-class failures loop in cron (intentional operator-visibility signal per WP §Non-Negotiable Constraints; not silenced).
- **Authoritative source for fulfillment data (locked):** `legendary.stripe_checkout_sessions` row + `BillingConfig.priceAllowlist` are authoritative for `accountId`, `priceId`, `entitlementKey`. Event payload fields (`payload.data.object.client_reference_id`, `payload.data.object.metadata.entitlementKey`) are **consistency checks only** — must agree with authoritative sources, never read as primary truth.
- **Cross-validation (4 checks, fail-fast on first mismatch):** (1) session row exists for event session ID → else `'session_lookup_failed'`; (2) `payload.data.object.client_reference_id === sessionRow.account_id` → else `'cross_validation_failed'`; (3) `payload.data.object.metadata.entitlementKey === sessionRow.entitlement_key` → else `'cross_validation_failed'`; (4) `priceAllowlist.has(sessionRow.price_id)` AND `priceAllowlist.get(sessionRow.price_id) === sessionRow.entitlement_key` → else `'price_not_in_allowlist'`.
- **Write ordering (locked, 3 writes):** (a) entitlement INSERT → (b) `stripe_checkout_sessions` UPDATE (`intent_status = 'completed'`, `completed_at = now()` WHERE `intent_status = 'open'`) → (c) `stripe_events` UPDATE (`processed_at = now()`, `process_error = NULL` WHERE `id = $1` AND `processed_at IS NULL`). Step (c) is the LAST write on the success / no-op path. SHOULD wrap in `BEGIN; … COMMIT;` (matches WP-104 `PUT /api/me/links` precedent); idempotency at (a) + (b) plus `processed_at IS NULL` survival means a partial-write crash heals on the next recovery-script pass.
- **Webhook response shape (verbatim, D-13402 (a) extension):** `{ received: true; duplicate: boolean; processed: boolean; reason: string | null }`. `processed: true` for every `Result.ok` outcome (including terminal-no-op `'unhandled_event_type'` / `'unpaid_session'` / `'already_processed'`); `processed: false` only for `Result.fail`. `reason` = closed-set string on success or `FulfillmentErrorCode` string on failure.
- **Always-200 posture (D-13404 (a)):** webhook handler returns 200 to Stripe on every signature-verified event regardless of `processStripeEvent` outcome. Fulfillment errors logged to stderr (full-sentence message + `event_id` + error code); recorded-event ledger is the single source of truth for backlog.
- **Duplicate-delivery branches:** when `recordStripeEvent.inserted === false`, handler loads existing row by `event_id` (`SELECT id, event_id, event_type, payload, received_at, processed_at, process_error FROM legendary.stripe_events WHERE event_id = $1`); if `existingRow.processed_at IS NULL` → call `processStripeEvent({ eventRecord: existingRow, … })` (self-heal); if non-NULL → skip processing, return `{ duplicate: true; processed: false; reason: null }`.
- **Recovery script (D-13405 (a)):** path `scripts/process-stripe-events.mjs`; ESM; loads env via `node --env-file=.env`; constructs `BillingConfig` (via `loadBillingConfig` from `billing.config.ts`) + short-lived `pg.Pool` (per-cron-invocation lifetime); SELECT `WHERE processed_at IS NULL ORDER BY received_at ASC LIMIT 100`; per-row `processStripeEvent`; stdout summary `processed: <N>, skipped: <M>, errors: <K>`. **Exit code domain locked:** `0` (scan completed, with or without per-row errors), `2` (startup failure — missing env var, config parse error, initial DB connection failure), `1` (unexpected JS exception escaping `try/finally`). Pool teardown via `try { ... } finally { await pool.end(); }` envelope (RS-1 lock).
- **`process_error` discipline (post-RS-6):** column is operator-internal; soft-cap **2000 chars** at every write site. Future UI exposure requires sanitization WP. Stripe Session ID (`source_ref`) soft-cap **200 chars** (Stripe IDs are <100 chars in practice; defense-in-depth).
- **Test count + suite delta lock (post-Issue 11):** server baseline before WP-134 = `pass 168 / fail 0 / skipped 59 (suites 29, tests 227)`. After WP-134 = `pass 168 + 16 / fail 0 / skipped 59 + 7 / suites 31 / tests 250`. New tests: 13-branch `processStripeEvent.logic.test.ts` coverage (Guards 3 + Phase 0a 5 = 8 in Guards domain; Validation 1 + 4 mismatches = 5 in Validation domain — note: Validation includes the new `intent_status` test + `accountId → player_id` miss test; Write-path 2; Failure-path 4; total 19 logic tests, of which ~7 require DB and inline-skip when `hasTestDatabase` is false). Plus 3 routes tests (self-heal / skip / always-200-fault). **Suite delta lock: `+2`** — one new `describe()` in `processStripeEvent.logic.test.ts`; one new `describe('webhook handler — WP-134 fulfillment')` in `billing.routes.test.ts`.
- **Catalog rows (D-11804 replace-whole-row):** webhook row replaced wholesale (response schema gains `processed` + `reason`; `Authorizing WP: WP-133, WP-134`; Notes cite D-13401 + D-13403 + WP-132 entitlement-key allowlist source); ONE new `Library-only` row appended for `processStripeEvent` (`Authorizing WP: WP-134`; Notes: "called synchronously by the webhook handler post-WP-134; also reachable via `scripts/process-stripe-events.mjs` for recovery").
- **No new npm dependencies.** `stripe@22.1.0` already pinned by WP-133 / D-13303.
- **Transaction requirement (mirrors WP §Non-Negotiable Constraints):** MUST wrap writes (a)–(c) in `BEGIN; … COMMIT;` IF `DatabaseClient` exposes a transaction primitive; MAY fall back to deterministic-ordering-plus-idempotency otherwise. The chosen path is locked in D-13403's DECISIONS.md entry alongside the FK-resolution choice.
- **Failure classes (mirrors WP §Non-Negotiable Constraints):** Transient = `'entitlement_insert_failed'` | `'session_update_failed'` | `'event_update_failed'` (retry expected to eventually succeed). Deterministic validation = `'session_lookup_failed'` | `'cross_validation_failed'` | `'price_not_in_allowlist'` (retry will not succeed without external correction). BOTH classes leave `processed_at = NULL` and set `process_error`.
- **Idempotency dimensions (mirrors WP §Non-Negotiable Constraints):** Event-level (`stripe_events.processed_at`) yields `reason: 'already_processed'`; entitlement-level (`entitlements_active_unique` partial unique index) yields `reason: 'duplicate'`. Independent axes — a single event can hit both reasons on different invocations.
- **Webhook response field invariant (locked):** when `processed: true`, `reason` is ALWAYS a `FulfillmentSuccessReason` string (one of 5 values). When `processed: false`, `reason` is either a `FulfillmentErrorCode` string OR `null` (skip branch only). `reason: null` paired with `processed: true` is an invalid state.

## §3 — Guardrails

- **Single entitlement INSERT site.** `INSERT INTO legendary.entitlements` appears EXACTLY once under `apps/server/src/billing/`, in `processStripeEvent.logic.ts`. §5 grep gate enforces. Any other INSERT location (including `billing.logic.ts` or `billing.routes.ts`) is a scope violation.
- **`ON CONFLICT` clause matches the shipped partial unique index.** Conflict target column names equal the EC-135 / WP-132 partial unique index `(player_id, entitlement_key) WHERE revoked_at IS NULL` — NOT `(account_id, entitlement_key)` as the WP body's shorthand suggests. §5 grep gate confirms `(player_id, entitlement_key)`. #CONFLICT-TARGET correction documented in D-13403.
- **No `payload.line_items` access.** Stripe omits line items from `checkout.session.completed` events without explicit expansion (extra Stripe API call); D-13403 forbids that path. §5 grep gate confirms zero matches under `apps/server/src/billing/`.
- **`processed_at` set ONLY on terminal-outcome paths.** Failure branches (`Result.fail`) MUST leave `processed_at = NULL`. Reviewer confirms by reading the source: zero `processed_at = now()` UPDATE inside any `Result.fail` block. §5 grep gate counts occurrences and reviewer cross-checks.
- **Authoritative source is the session row + allowlist.** `entitlementKey` passed to the INSERT is read from `sessionRow.entitlement_key`, NOT from `event.metadata.entitlementKey`. Event metadata is a consistency check only. Code review verifies the variable provenance.
- **Cross-validation runs BEFORE the entitlement INSERT.** All four checks (session lookup, account match, entitlement-key match, price allowlist match) execute in Phase 2 of `processStripeEvent`; Phase 3 (writes) only runs on all-pass. §5 grep gate confirms keyword presence.
- **Always-200 on signature-verified events.** Webhook handler returns 200 to Stripe even when `processStripeEvent` returns `Result.fail(…)`. Test asserts via injected fault that the response status is 200 with `processed: false`. Returning 5xx triggers Stripe retries which compound the recorded-event ledger.
- **Deps bundle shape unchanged.** `registerBillingRoutes` deps bundle does NOT change shape — `billingConfig` is already present from WP-133. §5 grep gate confirms no new key added to the deps interface.
- **Layer Boundary preserved.** No file under `apps/server/src/billing/` (extended set) imports `boardgame.io`, `@legendary-arena/(game-engine|registry|preplan)`, or any `apps/(arena-client|registry-viewer|replay-producer)` symbol. §5 grep gate enforces.
- **No new npm dependencies.** `git diff apps/server/package.json` shows no `dependencies` block change. Stripe SDK confined to `apps/server/src/billing/` per EC-136 lock — recovery script imports from `apps/server/src/billing/billing.config.ts` (`createStripeClient` factory + `loadBillingConfig`), NOT directly from `'stripe'`.
- **Recovery script ESM + `node --env-file`.** Script extension `.mjs`; uses `node --env-file=.env` invocation pattern; uses `node:`-prefixed built-ins; exits 0 on scan-loop errors; exits 2 on startup failures; pool teardown via `try { ... } finally { await pool.end(); }` envelope.
- **No external Stripe API calls inside `processStripeEvent`.** Zero `stripeClient.*` invocations, zero `fetch(`, zero outbound HTTP from `processStripeEvent.logic.ts`. The processor is a pure DB-mutation path. All Stripe-side data was captured at WP-133 INSERT time (denormalized into `stripe_checkout_sessions`) or arrives in the event payload. §6 grep gate enforces.
- **Recovery script concurrency (MVP scope = single-runner; future-scaling note).** No concurrency primitive at MVP — `LIMIT 100` plus ≤200ms per-row processing means a 15-min cron cadence cannot reasonably overlap (per WP §Non-Negotiable Constraints + WP D-DEC-5 run-overlap note). Future scaling WP may harden the SELECT with `FOR UPDATE SKIP LOCKED`; out of scope for WP-134. The note exists here so a future scaling WP cites this as the precedent rather than re-deriving from scratch.
- **No `new Pool(` inside `billing.routes.ts` or `processStripeEvent.logic.ts` (post-RS-2).** Webhook handler MUST consume the long-lived `pool` from `registerBillingRoutes`'s second positional parameter; processor MUST consume the `database: DatabaseClient` injected via the function args. Recovery script is the ONLY site permitted to construct a new `pg.Pool`, and only because it runs out-of-process per cron invocation.
- **No type-cast escapes after Phase 0a guard (post-Issue 21).** Once `isCheckoutSessionCompletedPayload(payload)` narrows the `unknown` to the structural shape, internal field access MUST use the narrowed type — no `as any` / `as unknown` / `as string` casts in `processStripeEvent.logic.ts`. The guard narrows once; further casts would defeat the narrowing. §6 grep gate enforces.
- **`reason: null` paired with `processed: true` is impossible (post-Issue 5).** Webhook response shape construction MUST use conditional assignment under `exactOptionalPropertyTypes: true` (build base object without `reason`, then assign in `if` blocks per WP-029 / D-2902 precedent). Inline ternary returning `null` on one branch and string on another is rejected by TS strictness. Reviewer-confirmation gate in §6.
- **Phase 0a guard runs FIRST (before all other Phase 1 guards).** The structural shape check is the load-bearing input-narrowing seam — every subsequent field access in Phases 1, 2, 3 assumes the narrowed type. Reordering would either cause a type error or read fields off `unknown` (compile-blocked).

## §4 — SAFE-KNOBS Posture (per `docs/ai/REFERENCE/SAFE-KNOBS.md`)

WP-134 grants entitlements via Stripe-funded purchases. SAFE-KNOBS doctrine applies as a posture lock — none of the doctrine's knob tiers (Tier 1 composition counts, Tier 2 presets / expansions, Tier 3 seed / themes) are touched, AND none of the explicitly non-configurable surfaces (turn structure, rule execution order, keyword behavior, scoring formulas, victory conditions, move legality, randomness resolution, phase sequence) are touched. The lock is structural, not narrative:

- [ ] **NG-1 (no pay-to-win) is structural, not procedural.** The granted `entitlement_key` is sourced from `sessionRow.entitlement_key` which was populated at WP-133 INSERT time from `BillingConfig.priceAllowlist.get(priceId)`. The allowlist's value type is `EntitlementKey` — a closed TS union locked to the WP-132 D-13203 cosmetic-only six-key set (`'supporter_tier_basic_2026'` + 5 evergreen cosmetics). The processor cannot construct a gameplay-affecting key because no such key exists in the union. Compile-time exhaustiveness (WP-132 D-13206 `default: const _: never = key` pattern) is the floor. **Verification:** §5 grep gate confirms no `EntitlementKey` extension in this WP; `pnpm --filter @legendary-arena/server build` exits 0 (TS exhaustiveness compiles clean).
- [ ] **No `process.env.*` reads in production code paths.** All env access happens at startup inside `loadBillingConfig` (WP-133); `processStripeEvent` consumes the frozen `BillingConfig` instance. Recovery script reads env via `node --env-file=.env` then constructs `BillingConfig` once. **Verification:** §5 grep gate `process\.env\.` returns zero matches under `apps/server/src/billing/processStripeEvent.logic.ts` and `apps/server/src/billing/billing.routes.ts` modifications.
- [ ] **No conditional gameplay logic introduced.** Fulfillment is server-layer; engine never imports `apps/server/src/billing/`. The processor mutates only `legendary.entitlements`, `legendary.stripe_checkout_sessions`, and `legendary.stripe_events` — zero engine-state touch. **Verification:** §5 grep gate confirms no `boardgame.io` / `@legendary-arena/(game-engine|registry|preplan)` import under `apps/server/src/billing/`.
- [ ] **Replay determinism (Vision §22, §24) preserved.** Fulfillment runs outside the engine's replay loop; entitlement grants do not feed `ctx.random.*`, do not alter move legality, do not influence scoring. Match composition (`MatchSetupConfig`) and the 9-field composition lock are untouched.
- [ ] **`priceAllowlist` is an ops surface, not a customer-facing safe knob.** The allowlist is server-construct-once via `loadBillingConfig` from `STRIPE_PRICE_ALLOWLIST` env var; modification requires a deploy + DECISIONS.md entry per WP-132 / WP-133 governance. It is NOT a Tier 1 / 2 / 3 customer knob and MUST NOT be promoted to one without a SAFE-KNOBS revision.
- [ ] **No new safe-knob surface introduced.** WP-134 introduces zero new `MatchSetupConfig` fields, zero new envelope fields, zero new presets, zero new themes, zero new expansions. The 9-field composition lock plus the WP-093 `heroSelectionMode` extension remain authoritative.

## §5 — Required `// why:` Comments

- `processStripeEvent.logic.ts` above the Phase 0a `isCheckoutSessionCompletedPayload` guard: cite the `payload: unknown` type lock from WP-133; cite that shape mismatches map to existing `'cross_validation_failed'` rather than introducing a new error code; cite that `process_error` discriminates the shape sub-case from the metadata sub-case for forensic queries.
- `processStripeEvent.logic.ts` above the Phase 2 `intent_status === 'open'` cross-validation check: cite the future-WP `'expired'` / `'canceled'` transition surface; cite that fulfillment against a non-`'open'` session is refused as a defense against delayed webhook delivery race conditions.
- `processStripeEvent.logic.ts` above the Phase 3 step 6 `accountId → player_id` resolution (per #ENT-FK): cite the WP-104 / EC-135 two-query pattern; cite that `legendary.entitlements.player_id` is `bigint` FK to `legendary.players.player_id`, NOT `text` to `ext_id`.
- `processStripeEvent.logic.ts` above the Phase 3 step 8 `ON CONFLICT (player_id, entitlement_key) WHERE revoked_at IS NULL DO NOTHING` clause: cite WP-132's partial unique index as the defense-in-depth primitive; cite the application-layer `DO NOTHING` as the idempotency acceptance signal; cite RETURNING-row-count as the `'fulfilled'` vs `'duplicate'` discriminator. **Grep-gate prose discipline:** do NOT echo the literal `'INSERT INTO legendary.entitlements'` in this comment; the §6 grep gate counts that token as the single-INSERT-site enforcement and a comment echo would inflate the count.
- `processStripeEvent.logic.ts` above any failure branch returning `Result.fail`: cite the recovery-script `WHERE processed_at IS NULL` selector — failures must remain pickable; setting `processed_at = now()` on failure would make events terminally lost.
- `processStripeEvent.logic.ts` above the Phase 3 step 10 `stripe_events` UPDATE (the last write on success / no-op path): cite the locked write-ordering constraint; cite the crash-recovery guarantee — idempotency at steps 8 + 9 plus `processed_at = NULL` survival means a partial-write crash heals on the next recovery-script pass.
- `processStripeEvent.logic.ts` at every `process_error` write site: cite the operator-internal column posture (no UI exposure without future sanitization WP); cite the soft-cap 2000-char discipline to keep recovery-script summaries readable.
- `billing.routes.ts` above the `loadStripeEventRecordByEventId` helper: cite PS-1 resolution path (a) — re-fetch via SELECT after `recordStripeEvent` returns `inserted: true`; cite the shared use across both webhook branches (newly-inserted + duplicate-delivery); cite that `recordStripeEvent` (WP-133) does NOT return the row.
- `billing.routes.ts` above the row-absent edge case (`null` return from the shared helper): cite the concurrent-inconsistency rationale; cite that this is the SOLE exception to the always-200 posture (returns 500 internal_error to signal Stripe to retry).
- `billing.routes.ts` above the duplicate-delivery branch dispatch (`if (!inserted) { … }`): cite Stripe at-least-once webhook delivery; cite self-heal opportunity when `existingRow.processed_at IS NULL` (first-delivery processing failed); cite skip when `existingRow.processed_at IS NOT NULL` (already terminally processed).
- `billing.routes.ts` above the response-shape construction (`reason` field): cite `exactOptionalPropertyTypes: true` strictness; cite WP-029 / D-2902 conditional-assignment precedent (build base object without `reason`, then assign in `if` blocks).
- `billing.routes.ts` above the always-200 fault handler: cite D-13404; cite Stripe-driven retries compound the recorded-event ledger; cite recovery script as the single source of truth for backlog.
- `scripts/process-stripe-events.mjs` above the startup `loadBillingConfig` call: cite the fatal-on-missing-env-vars posture (exit code 2); cite the operator-paging rationale (missing env vars are operator-actionable).
- `scripts/process-stripe-events.mjs` above the `try { ... } finally { await pool.end(); }` envelope: cite RS-1 lock — short-lived per-cron pool; release connections cleanly on every exit path.
- `scripts/process-stripe-events.mjs` above the `LIMIT 100` clause: cite default batch size locked at WP §Locked contract values; cite per-row processing time ≤200ms → ≤20s per cron cycle (15-min cadence cannot reasonably overlap at MVP scale per WP D-DEC-5 run-overlap note).
- `scripts/process-stripe-events.mjs` above the `exit(0)` on scan-loop errors: cite cron-page-on-fatal-only posture; cite per-row errors logged to stderr (operator-visible signal); cite non-zero exit reserved for startup-phase faults only.

## §6 — Verification Gates (run all; every item binary)

- [ ] `pnpm --filter @legendary-arena/server build` exits 0; `pnpm --filter @legendary-arena/server test` exits 0. Engine baseline `679 / 148 / 0` UNCHANGED (engine never touched; 01.5 NOT INVOKED — server-layer-only WP).
- [ ] **Single entitlement INSERT site.** `Select-String -Path "apps\server\src\billing" -Pattern "INSERT INTO legendary\.entitlements" -Recurse` returns exactly one match (in `processStripeEvent.logic.ts`).
- [ ] **`ON CONFLICT` target matches partial unique index.** `Select-String -Path "apps\server\src\billing\processStripeEvent.logic.ts" -Pattern "ON CONFLICT \(player_id, entitlement_key\) WHERE revoked_at IS NULL DO NOTHING"` returns at least one match. Conflict target uses `player_id` (NOT `account_id`).
- [ ] **No `payload.line_items` access.** `Select-String -Path "apps\server\src\billing" -Pattern "line_items" -Recurse` returns no output.
- [ ] **Cross-validation present.** `Select-String -Path "apps\server\src\billing\processStripeEvent.logic.ts" -Pattern "client_reference_id|cross_validation_failed"` returns matches.
- [ ] **No `processed_at = now()` in failure branches.** `Select-String -Path "apps\server\src\billing\processStripeEvent.logic.ts" -Pattern "processed_at = now\(\)"` returns occurrences only on success / no-op paths (Phase 1 unhandled-type / unpaid early returns + Phase 3 step (c)). Reviewer confirms by reading source — zero occurrences inside any `Result.fail` branch.
- [ ] **Webhook handler calls `processStripeEvent`.** `Select-String -Path "apps\server\src\billing\billing.routes.ts" -Pattern "processStripeEvent"` returns at least two matches (newly-inserted branch + duplicate-delivery self-heal branch).
- [ ] **Recovery script invokable.** `node --env-file=.env scripts\process-stripe-events.mjs` against an empty events table prints `processed: 0, skipped: 0, errors: 0` and exits 0.
- [ ] **Catalog rows landed (D-11804).** `Select-String -Path "docs\ai\REFERENCE\api-endpoints.md" -Pattern "WP-133, WP-134|processStripeEvent"` returns matches for both the joint-authoring webhook row and the new Library-only row.
- [ ] **No new migration.** `git diff --name-only data/migrations/` returns empty output.
- [ ] **No new npm dependencies.** `git diff apps/server/package.json` shows no `dependencies` block change.
- [ ] **No `boardgame.io` import.** `Select-String -Path "apps\server\src\billing" -Pattern "from .['\"]boardgame\.io" -Recurse` returns no output.
- [ ] **No engine / registry / preplan import.** `Select-String -Path "apps\server\src\billing" -Pattern "@legendary-arena/(game-engine|registry|preplan)" -Recurse` returns no output.
- [ ] **Stripe SDK confined.** `Select-String -Path "apps","packages","scripts" -Pattern "from 'stripe'" -Recurse` returns matches only under `apps\server\src\billing\` (the recovery script imports `createStripeClient` from `billing.config.ts`, NOT `'stripe'` directly).
- [ ] **No `process.env.*` in `processStripeEvent.logic.ts`.** `Select-String -Path "apps\server\src\billing\processStripeEvent.logic.ts" -Pattern "process\.env\."` returns no output.
- [ ] **No external Stripe API calls in `processStripeEvent.logic.ts`.** `Select-String -Path "apps\server\src\billing\processStripeEvent.logic.ts" -Pattern "stripeClient\.|fetch\(|http\.request|https\.request"` returns no output. The processor is a pure DB-mutation path.
- [ ] **No type-cast escapes after Phase 0a guard.** `Select-String -Path "apps\server\src\billing\processStripeEvent.logic.ts" -Pattern "\bas any\b|\bas unknown\b|\bas string\b"` returns no output. The guard narrows once; further casts would defeat the narrowing.
- [ ] **No `new Pool(` inside `billing.routes.ts` or `processStripeEvent.logic.ts`.** `Select-String -Path "apps\server\src\billing\billing.routes.ts","apps\server\src\billing\processStripeEvent.logic.ts" -Pattern "new (pg\.)?Pool\("` returns no output. Long-lived `pool` consumed via `registerBillingRoutes` second positional parameter; processor consumes `database` via function args.
- [ ] **Phase 0a guard runs FIRST.** Reviewer confirms by reading `processStripeEvent.logic.ts` source: `isCheckoutSessionCompletedPayload` is invoked before any other Phase 1 guard; the function body's first non-trivial statement is the shape check.
- [ ] **`accountId → player_id` resolution present.** `Select-String -Path "apps\server\src\billing\processStripeEvent.logic.ts" -Pattern "SELECT player_id FROM legendary\.players WHERE ext_id"` returns at least one match (Phase 3 step 6).
- [ ] **`intent_status === 'open'` cross-validation present.** `Select-String -Path "apps\server\src\billing\processStripeEvent.logic.ts" -Pattern "intent_status.*open"` returns at least one match.
- [ ] **No `recordStripeEvent` modification.** `git diff apps/server/src/billing/billing.logic.ts` is empty (PS-1 path (a) lock — re-fetch via SELECT, not `RETURNING *` modification).
- [ ] **Recovery script imports from `billing.config.ts`, not `'stripe'`.** `Select-String -Path "scripts\process-stripe-events.mjs" -Pattern "from 'stripe'"` returns no output. `Select-String -Path "scripts\process-stripe-events.mjs" -Pattern "from .*billing\.config"` returns at least one match.
- [ ] **Recovery script `try/finally` envelope.** `Select-String -Path "scripts\process-stripe-events.mjs" -Pattern "await pool\.end\(\)"` returns at least one match inside a `finally` block (reviewer confirms by reading source).
- [ ] **Webhook response field invariant — `reason` never null when `processed: true`.** Reviewer confirms by reading `billing.routes.ts` source: every code path that sets `processed: true` also sets `reason: <closed-set string>` (never `null`); only the duplicate-delivery skip branch (which sets `processed: false`) is permitted to set `reason: null`. `billing.routes.test.ts` asserts the invariant on at least one terminal-no-op path (e.g., `'unhandled_event_type'` → `processed: true; reason: 'unhandled_event_type'`).
- [ ] **Conditional assignment for `reason`.** Reviewer confirms by reading `billing.routes.ts` source: response object built without `reason`, then assigned in `if` blocks per `exactOptionalPropertyTypes: true` discipline (WP-029 / D-2902 precedent). No inline ternary returning `null` on one branch and a string on the other.
- [ ] **Phase 0a guard test coverage (5 tests).** `processStripeEvent.logic.test.ts` has 1 positive (canonical payload narrows successfully) + 4 negatives (one per missing/wrong-typed field of `data.object.{id, client_reference_id, metadata.entitlementKey, payment_status}`). Each negative asserts `Result.fail({ code: 'cross_validation_failed' })` + `processed_at = NULL` + `process_error` set.
- [ ] **`intent_status` non-`'open'` test present.** `processStripeEvent.logic.test.ts` seeds a session row with `intent_status = 'expired'`; asserts `'session_lookup_failed'`. Same for `'canceled'`.
- [ ] **Test count + suite delta lock.** `pnpm --filter @legendary-arena/server test` reports `pass 184 / fail 0 / skipped 66 / suites 31 / tests 250` post-WP-134 (server post = 168+16 / 0 / 59+7 / 31 / 250).
- [ ] **No `EntitlementKey` extension in this WP.** `git diff apps/server/src/entitlements/entitlements.types.ts` is empty (WP-132 closed union unchanged).
- [ ] **`processed: true` for terminal-no-op paths.** Reviewer confirms by reading `billing.routes.ts` source — every `Result.ok` arm sets `processed: true` regardless of `result.value.reason`; only `Result.fail` arms set `processed: false`.
- [ ] **D-DEC codes absent from shipped artifacts.** `Select-String -Path "apps\server\src\billing\processStripeEvent.logic.ts","apps\server\src\billing\billing.routes.ts","apps\server\src\billing\billing.routes.test.ts","scripts\process-stripe-events.mjs","docs\ai\REFERENCE\api-endpoints.md" -Pattern "D-DEC-[1-5]" -Recurse` returns no output. Only `D-134NN` numbers appear in shipped artifacts (per EC-128 / EC-135 / EC-136 lock).
- [ ] **Test branch coverage — 19 branches grouped by domain (post-resolution count).** `processStripeEvent.logic.test.ts` contains assertions covering all four domains:
  - **Guards (Phase 0a + Phase 1 early returns, 8 branches):** Phase 0a positive (canonical payload narrows); Phase 0a negatives × 4 (missing `data.object.id`, non-string `client_reference_id`, missing `metadata.entitlementKey`, non-string `payment_status`); already-processed (`eventRecord.processedAt !== null`); unhandled-event-type (`'payment_intent.succeeded'` etc.); unpaid (`payment_status !== 'paid'`).
  - **Validation (Phase 2 cross-validation, 1 happy + 5 mismatches):** session-lookup-success (happy flow into Phase 3); intent_status-not-open (`'session_lookup_failed'` for `'expired'` and `'canceled'`); session-lookup-miss (`'session_lookup_failed'`); account-mismatch (`'cross_validation_failed'`); entitlement-key-mismatch (`'cross_validation_failed'`); price-allowlist-drift (`'price_not_in_allowlist'`); accountId→player_id-miss (`'cross_validation_failed'` — referential-integrity edge case).
  - **Write-path (Phase 3 success / duplicate, 2 branches):** success-first-fulfillment (`reason: 'fulfilled'`, entitlement row created with `player_id` resolved, session row `intent_status = 'completed'`, event row `processed_at` set); idempotent-rerun (`reason: 'duplicate'`, entitlement row count = 1, session row unchanged).
  - **Failure-path (Phase 3 DB-fault + crash-recovery, 4 branches):** INSERT-fault (`'entitlement_insert_failed'`); session-update-fault (`'session_update_failed'`); event-update-fault (`'event_update_failed'`); crash-recovery (partial-write between steps 8–10; second pass returns `Result.ok({ value: { reason: 'duplicate' } })` and final `processed_at` is set).
  - Every Validation-domain mismatch test AND every Failure-path test asserts `process_error` is set + `processed_at = NULL` (the locked semantic — failures stay pickable by the recovery script).
- [ ] **Recovery selectability test.** Every failure-path test asserts `SELECT * FROM legendary.stripe_events WHERE processed_at IS NULL AND id = <test event id>` returns exactly one row.
- [ ] **Idempotency test.** Running `processStripeEvent` twice on the same event produces entitlement row count = 1.
- [ ] **Routes self-heal + skip tests.** `billing.routes.test.ts` covers both duplicate-delivery branches (existing row `processed_at IS NULL` → calls `processStripeEvent`; existing row `processed_at IS NOT NULL` → skips).
- [ ] **Routes always-200 fault test.** Injected fault causes `processStripeEvent` to return `Result.fail`; response status is 200 with `processed: false; reason: <FulfillmentErrorCode>`.
- [ ] **Tests inline-skip when `hasTestDatabase` is false** (per WP-101 D-5201 §3.1 verbatim option-object pattern).
- [ ] **Scope boundary.** `git diff --name-only` lists exactly the files in §1.

## §7 — Commit Hygiene

- [ ] Commit prefix: `EC-140:` on the code commit (per `01.3-commit-hygiene-under-ec-mode.md` Rule 5; `WP-134:` is forbidden); `SPEC:` on the governance close commit if two-commit topology used (mirrors EC-135 / EC-136 precedent).
- [ ] Vision trailer: `Vision: §3, §11, §14, §15, §765-794, NG-1, NG-2, NG-3, NG-4, NG-5, NG-6, NG-7` per `01.3` Vision Trailer convention (matches WP-134 §Vision Alignment cited clauses).
- [ ] D-11804 catalog obligation lands in same commit as the route change: webhook row replaced wholesale; new Library-only row appended; field names match `00.2-data-requirements.md` byte-for-byte.
- [ ] No `--no-verify`, no `--no-gpg-sign` per `01.3` "Bypassing Hooks".

## §8 — Post-Execution Checks

- [ ] All WP-134 §Acceptance Criteria pass (Library / Routes / Recovery script / Catalog / Tests / Scope Enforcement).
- [ ] D-13401 written verbatim (synchronous-on-webhook posture; cite Stripe 30s timeout + ~50–200ms processing budget; rejected alternatives (b) queue and (c) cron-only).
- [ ] D-13402 written verbatim (response shape extension `{processed, reason}` with `reason` as closed-set string union; cite ops grep-parity rationale; reject (b) WP-133-shape).
- [ ] D-13403 written verbatim (FIVE-axis cross-validation including `intent_status === 'open'` per RS-4 + Phase 0a structural type guard per PS-2 + `accountId → player_id` resolution mapping per #ENT-FK + `(player_id, entitlement_key)` conflict target per #CONFLICT-TARGET + transactional posture (`MUST` if primitive available, `MAY` fall back to ordering+idempotency); cite defense-in-depth + WP-104 / EC-135 two-query precedent + WP-133 `payload: unknown` lock; reject (b) one-axis and (c) no-cross-validation; reject (d) modify `recordStripeEvent` to add `RETURNING *` — locks PS-1 path (a) re-fetch via shared local helper `loadStripeEventRecordByEventId`).
- [ ] D-13404 written verbatim (always-200 on signature-verified events; cite Stripe retry-compound risk during incidents; reject (b) 5xx-on-fault).
- [ ] D-13405 written verbatim (manual + Render Cron @ 15min; **two-phase lifecycle posture** per PS-3 — startup-fatal on missing env vars (exit 2), scan-loop-tolerant on per-row errors (exit 0); pool teardown via `try { ... } finally { await pool.end(); }` per RS-1; Stripe SDK confinement preserved per EC-136 lock; cite WP §D-DEC-5 run-overlap note for future-scaling `FOR UPDATE SKIP LOCKED` precedent; reject (b) manual-only and (c) in-process timer).
- [ ] `STATUS.md` `### WP-134 / EC-140 Executed` block at top of `## Current State` cites: `processStripeEvent.logic.ts` + recovery script + webhook handler self-heal/skip branches + catalog row replacement + each D-134NN locked option + explicit "full purchase-to-entitlement loop is now LIVE for cosmetic SKUs only — refunds, subscriptions, admin grants remain out of scope per WP-133 D-13307 / WP-134 §Out of Scope" lock.
- [ ] `WORK_INDEX.md` WP-134 row checked off with date + commit hash.
- [ ] `EC_INDEX.md` EC-140 row flipped `Draft` → `Done {YYYY-MM-DD}` with commit hash.
- [ ] 01.5 NOT INVOKED (server-layer-only WP per WP-101 / WP-102 / WP-104 / WP-115 / WP-126 / WP-131 / WP-132 / WP-133 sibling-helper precedent — engine baseline UNCHANGED).
- [ ] 01.6 post-mortem OPTIONAL per server-layer-only WP precedent. Author IFF execution surfaces a design tension worth capturing — e.g., #ENT-FK / #CONFLICT-TARGET correction class triggering a new variant; D-13403 held with contradiction; the recovery-script-vs-inline split exposing a scaling concern earlier than expected.

## Common Failure Smells

- **⚠ #ENT-FK (RESOLVED at WP §IMPORTANT — failure mode prevented by construction).** The `(account_id, ...)` column shorthand in the WP-134 v1.0 draft has been corrected at WP level. The locked INSERT is `(player_id, entitlement_key, source, source_ref)` with `player_id` resolved via the WP-104 / EC-135 two-query pattern. If a future drafter ever reverts to `account_id`, the INSERT will fail at execution time — but EC-140 §6 grep gates and the WP §IMPORTANT lock prevent the regression.
- **⚠ #CONFLICT-TARGET (RESOLVED at WP §IMPORTANT).** `ON CONFLICT (player_id, entitlement_key) WHERE revoked_at IS NULL DO NOTHING` matches the shipped partial unique index byte-for-byte. Reverting to `(account_id, ...)` would fail at execution time.
- **⚠ PS-1 (RESOLVED via shared `loadStripeEventRecordByEventId` helper).** The WP-134 v1.0 draft's `recordStripeEvent returns the inserted row via RETURNING *` claim is corrected. The shared helper re-fetches the row by `event_id` after `recordStripeEvent` reports `inserted: true`. If a future drafter modifies `recordStripeEvent` to add `RETURNING *`, that becomes a WP-133 contract change — discouraged by `.claude/rules/work-packets.md` and rejected by D-13403.
- **⚠ PS-2 (RESOLVED via Phase 0a structural type guard).** Without the `isCheckoutSessionCompletedPayload` guard, `payload.data.object.*` field access on `unknown` either fails at compile time or requires `as any` casts that defeat the WP-133 type lock. The Phase 0a guard is the only sanctioned narrowing seam.
- Entitlement INSERT in any file other than `processStripeEvent.logic.ts` (e.g., `billing.logic.ts`, `billing.routes.ts`) → §3 single-INSERT-site guardrail violated. Remove and confine.
- `payload.line_items` reference anywhere under `apps/server/src/billing/` → forbidden expansion path; D-13403 forbids relying on Stripe API line-item expansion. Use `sessionRow.entitlement_key` (denormalized at WP-133 INSERT time) instead.
- `processed_at = now()` UPDATE inside any `Result.fail` branch → `processed_at` lifecycle violated; recovery script will skip the failed event, making it terminally lost. The locked semantic is failure-leaves-NULL.
- `EntitlementKey` extension landed in this WP → NG-1 structural defense bypassed; WP-132 closed union must remain unchanged. Adding a new key requires a separate WP + DECISIONS.md entry per SAFE-KNOBS doctrine; not a WP-134 task.
- `event.type` filter inside the WP-133 webhook handler relocated into `processStripeEvent` instead of the unhandled-type early return → masks events the recovery script may need to re-pick. Use the early-return guard pattern from WP §A Phase 1.
- Webhook handler returns 5xx on `processStripeEvent` failure → Stripe retry storm during incidents; D-13404 forbids. Always 200 on signature-verified events; log to stderr; let the recovery script handle backlog.
- Stripe SDK imported directly from the recovery script (`from 'stripe'` in `scripts/process-stripe-events.mjs`) → Layer Boundary violation per EC-136 lock. Import `createStripeClient` from `apps/server/src/billing/billing.config.ts` instead.
- Recovery script exits non-zero on per-row errors → cron pages on transient faults; D-13405 / WP §Constraints lock requires exit 0 even on per-row errors (non-zero reserved for fatal startup faults like missing env vars).
- D-DEC decision codes (`D-DEC-1` through `D-DEC-5`) appear in shipped production code, test files, recovery script, or catalog rows → draft-time placeholders leaked into shipped artifacts (per EC-128 / EC-135 / EC-136 lock). Replace all with executed `D-134NN` numbers.
- `processed: false` set for a `Result.ok` outcome (e.g., `'unhandled_event_type'` or `'unpaid_session'`) → semantic drift; the locked rule is `processed: true` for every terminal outcome reached without error, regardless of `result.value.reason`. Only `Result.fail` sets `processed: false`.
