# 01.4 — Pre-Flight Invocation (Work Packet Readiness & Scope Lock)

> **REFERENCE DOCUMENT — Not an execution prompt. Reusable governance template.**
>
> **Subordination:** This document is subordinate to:
>
> 1. `.claude/CLAUDE.md` (Execution Checklists section)
> 2. `docs/ai/ARCHITECTURE.md`
> 3. `docs/ai/execution-checklists/EC-TEMPLATE.md`
>
> If this document conflicts with any of the above, the higher-authority
> document wins.

---

## Purpose

A **pre-flight invocation** is a mandatory readiness gate that runs **before**
a session execution prompt is generated for a Work Packet.

It exists to:

- Confirm the WP is **sequenced correctly** (all prerequisites complete)
- Confirm the repo is **green** (build/tests pass) at the starting point
- Lock **scope boundaries**, allowed files, and forbidden behavior
- Identify and resolve **risks / ambiguities** *before* implementation begins
- Verify **contract fidelity** between dependency WPs and the current WP
- Produce a **binary verdict**: READY / NOT READY
- Authorize (or deny) generation of the WP's **session execution prompt**

Pre-flight is an **audit artifact**. It must be written so a reviewer can
understand why execution was authorized without re-reading transcripts.

"Green" means all relevant package builds succeed and all tests pass
with no pending EC violations.

---

## Review Order & Authority (Normative)

Pre-flight review must proceed in the following order:

1. **Execution Checklist (EC)**
2. **Work Packet (WP)**
3. **Invocation / Session Execution Prompt**

Rules:

- The **EC is the primary authority** for execution correctness.
- The **WP must justify and conform to the EC**, not reinterpret it.
- The **invocation prompt is a derived artifact** and must not introduce
  new constraints, scope, or interpretations beyond EC + WP.

If an inconsistency is found:
- EC errors must be resolved first
- Then WP inconsistencies
- Invocation prompt issues are addressed last

Pre-flight fails if any lower-level artifact contradicts a higher one.
Lower-authority documents must be corrected; they may not "work around"
or compensate for higher-level errors.

---

## When Pre-Flight Is Required

Pre-flight should be run for every WP.

It is **mandatory** when the WP:

- Mutates runtime state (`G`) or implements moves / phase hooks
- Adds or consumes runtime-visible fields (often triggers 01.5)
- Wires behavior into `game.ts` or other runtime orchestration
- Introduces new "engine-wide" contracts that future WPs depend on
- Changes test expectations or introduces integration tests
- Touches infrastructure, deployment, or database migrations

**Foundation Prompt prerequisite:** All 4 Foundation Prompts
(FP-00.4 -> FP-00.5 -> FP-01 -> FP-02) must have completed
successfully before any WP may execute. Foundation Prompts are
**stop-on-failure gates** — if any FP fails, ALL downstream work is
blocked. This is distinct from the per-WP lint gate, which checks
individual WP quality. Pre-flight may assume FPs are complete unless
the WP touches infrastructure that an FP established (database
schema, R2 data, server wiring, environment variables).

---

## Work Packet Execution Workflow

This template is **step 1** of a 10-step workflow (steps 0-8, plus step
1b). Each step must complete before the next may begin.

| Step | Gate | When | Document |
|---|---|---|---|
| 0 | Session context | Start of session, before pre-flight | `docs/ai/session-context/session-context-wpXXX.md` |
| 1 | Pre-flight (`01.4`) | Before session prompt | `docs/ai/REFERENCE/01.4-pre-flight-invocation.md` |
| 1b | Copilot check (`01.7`) | After pre-flight READY verdict, before session prompt | `docs/ai/REFERENCE/01.7-copilot-check.md` |
| 2 | Session prompt | Before execution | `docs/ai/invocations/session-wpXXX-*.md` |
| 3 | Execution | New session, EC-mode | Runs the session prompt under EC-mode |
| 4 | Post-mortem (`01.6`) | After EC passes, same session | `docs/ai/REFERENCE/01.6-post-mortem-checklist.md` |
| 5 | Pre-commit review | Separate session, gatekeeper | `docs/ai/prompts/PRE-COMMIT-REVIEW.template.md` |
| 6 | Commit | Commit hygiene (`01.3`) | `docs/ai/REFERENCE/01.3-commit-hygiene-under-ec-mode.md` |
| 7 | Lessons learned | After commit, same or new session | `01.4` §Lessons Learned Template (below) |
| 8 | Session context | After lessons, before next WP | `docs/ai/session-context/session-context-wpXXX.md` |

**Step sequencing rules:**

- **Step 0 -> 1:** Step 0 loads the session context file produced by the
  **prior** WP's step 8. This file contains test baselines, dependency
  chain status, key artifacts, architectural patterns, and relevant
  decisions — everything needed to begin pre-flight without re-deriving
  project state from scratch. If no session context file exists (e.g.,
  first WP in a phase, or the prior WP predates this workflow), skip
  step 0 and begin pre-flight from the authority documents directly.
  Session context files live in `docs/ai/session-context/`.
- **Step 1 -> 1b:** Step 1b (copilot check) runs in the **same session**
  as pre-flight, only after step 1 produces a **READY TO EXECUTE**
  verdict. It is mandatory for Runtime Wiring, Behavior / State Mutation,
  and Infrastructure & Verification WPs; recommended but optional for
  Contract-Only WPs. See `01.7-copilot-check.md` for the 30-issue lens
  and disposition rules.
- **Step 1b -> 2:** Step 2 may only begin after step 1b returns
  `CONFIRM`. A `HOLD` disposition requires applying scope-neutral fixes
  and re-running the copilot check (no pre-flight re-run). A `SUSPEND`
  disposition suspends the pre-flight verdict; if resolution changes
  scope, step 1 must be re-run before step 1b.
- **Step 2 -> 3:** Step 3 runs in a **new Claude Code session** — not the
  session that generated the prompt.
- **Step 3 -> 4:** Step 4 (post-mortem) runs in the **same session** as
  execution, after EC passes. It actively inspects code and may trigger
  fixes. Mandatory for WPs that introduce new contracts, projections,
  setup artifacts, or long-lived abstractions; recommended but optional
  for simpler WPs (see `01.6` for criteria).
- **Step 4 -> 5:** Step 5 runs in a **separate session** as a
  non-implementing gatekeeper. It assumes verification has passed and
  does not re-run commands.
- **Step 5 -> 6:** Step 6 follows commit hygiene rules (`EC-###:` prefix,
  hook validation).
- **Step 6 -> 7:** Step 7 captures lessons learned from the WP execution
  that should inform future pre-flight templates, session prompts, or
  governance. Updates the pre-flight precedent log and any affected
  REFERENCE documents. May also update `.claude/rules/` if a new
  enforcement pattern was discovered.
- **Step 7 -> 8:** Step 8 produces a session context file for the **next**
  WP in the dependency chain. Saved to
  `docs/ai/session-context/session-context-wpXXX.md` where `XXX` is the
  next WP. This file is the bridge between sessions — it captures the
  completed WP's state so the next session starts with full context
  instead of re-deriving it from scratch. Step 8's output becomes the
  next WP's step 0 input.

---

## Pre-Flight Template

The sections below are the template. Replace `WP-XXX` with the actual Work
Packet ID. Fill in all sections. Do not skip any.
Partial or placeholder responses invalidate the pre-flight.

---

### Pre-Flight Header

**Target Work Packet:** `WP-XXX`
**Title:** `<short title>`
**Previous WP Status:** `<e.g., WP-008A Complete (YYYY-MM-DD)>`
**Pre-Flight Date:** `<YYYY-MM-DD>`
**Invocation Stage:** Pre-Execution (Scope & Readiness)

**Work Packet Class (choose one):**

- Contract-Only (types/contracts/tests only)
- Runtime Wiring (limited runtime wiring based on existing contracts)
- Behavior / State Mutation (implements behavior; mutates `G`; uses framework ctx)
- Infrastructure & Verification (harnesses, replay, testing infra, verification
  tooling — has runtime logic but does not mutate `G` in gameplay, does not wire
  into `game.ts`, does not add moves or phases)

The selected class determines which sections below are mandatory:
- **All classes:** Dependency Check, Input Data Traceability, Structural Readiness, Scope Lock, Test Expectations, Risk Review
- **Runtime Wiring + Behavior:** also Runtime Readiness Check, Maintainability & Upgrade Readiness
- **Behavior only:** also Mutation Boundary Confirmation, Dependency Contract Verification
- **Infrastructure & Verification:** also Runtime Readiness Check, Dependency
  Contract Verification, Maintainability & Upgrade Readiness (infrastructure WPs
  consume many engine APIs and must verify all call targets are importable)

---

### Pre-Flight Intent

Perform a pre-flight validation for `WP-XXX`.

- Not implementing.
- Not generating code.
- Validating readiness and constraints only.

If a blocking condition is found, pre-flight must return **NOT READY** and stop.

---

### Authority Chain (Must Read)

Before making a verdict, read and follow these documents **in order**:

1. `.claude/CLAUDE.md`
   - EC-mode rules
   - Bug handling rules
   - Commit discipline

2. `docs/ai/ARCHITECTURE.md`
   - Layer boundaries
   - Engine vs framework rules

3. `docs/01-VISION.md`
   - Primary Goals (#1-5), Secondary Goals (#6-19),
     Scoring & Skill Measurement (#20-26), Non-Goals (NG-1..7),
     Financial Sustainability covenant
   - Authoritative for fairness, scoring, identity, monetization, longevity
   - Pre-flight review is one of the named uses of this document
     (Vision §"How This Document Is Used")

4. `docs/03.1-DATA-SOURCES.md`
   - Authoritative input data inventory
   - Provenance, storage location, and trust model for all inputs

5. `docs/ai/REFERENCE/02-CODE-CATEGORIES.md`
   - Code category definitions and boundary rules
   - Maps directories to categories with allowed/forbidden behaviors

6. `docs/ai/execution-checklists/EC-XXX-*.checklist.md`
   - Governing execution checklist(s)

7. `docs/ai/work-packets/WP-XXX-*.md`
   - The authoritative WP specification

8. Any explicitly referenced dependencies (prior WPs, reference docs, decisions)

If conflicts exist, **higher-authority documents win**.
Lower-authority documents must be corrected; they may not "work around"
or compensate for higher-level errors.

ARCHITECTURE.md wins over 01-VISION.md only on layer-boundary questions.
For intent, fairness, scoring, identity, monetization, and longevity, the
vision is authoritative and overrides the WP.

---

### Vision Sanity Check

> **Required for all WP classes.** This is a short, evidence-bearing check
> that the WP is consistent with `docs/01-VISION.md` before execution begins.
> It is not a re-statement of the vision — it is a citation that the WP's
> changes touch known clauses and do not violate any of them.

The pre-flight output **must** contain a Vision Sanity Check block with the
following fields. Use clause numbers, never paraphrases.

- [ ] **Vision clauses touched by this WP** — explicit list, e.g.
      `§3 (Trust & Fairness), §22 (Deterministic Eval), §24 (Replay-Verified
      Integrity), NG-1 (No Pay-to-Win)`. If the WP touches none of the §17.1
      trigger surfaces in `00.3-prompt-lint-checklist.md`, write:
      `None — structural / harness / build-only WP.`
- [ ] **Conflict assertion** — exactly one of:
  - `No conflict: this WP preserves all touched clauses.`
  - `Conflict: <clause> — <reason> — resolved by <DECISIONS.md entry>.`
- [ ] **Non-Goal proximity** — for any WP touching identity, payment,
      cosmetics, persuasion, scarcity, leaderboards, or competitive surfaces:
      explicit confirmation that none of NG-1..7 are crossed. Otherwise
      write `N/A — WP touches no monetization or competitive surface.`
- [ ] **Determinism preservation** — for any WP touching scoring, replay,
      RNG, simulation, or PAR: explicit confirmation that the change is
      deterministic and replay-faithful (Vision §22). Otherwise write
      `N/A — WP touches no determinism-bearing surface.`
- [ ] **WP `## Vision Alignment` section** — if `00.3 §17.1` triggers
      apply, confirm the WP itself contains a `## Vision Alignment` block.
      If the block is missing, pre-flight is **NOT READY** — fix the WP
      first.

**Verdict rule:** If any vision clause is violated, or a triggering WP
lacks the `## Vision Alignment` block, pre-flight returns **NOT READY**.
Vision conflicts are not "work around" items — they require either a
DECISIONS.md entry approving the deviation or a WP rewrite.

---

### Dependency & Sequencing Check

Confirm **all prerequisite work is complete** and recorded as such.

| WP | Status | Notes |
|---|---|---|
| WP-AAA | pass / fail | |
| WP-BBB | pass / fail | |

**Rule:** If any prerequisite WP is incomplete, pre-flight is **NOT READY**.

---

### Dependency Contract Verification

**Required when the WP consumes types, field names, helper functions, or
constants defined by a prior WP.**

For each dependency contract consumed by this WP, verify:

- [ ] **Type/field names match exactly** between the dependency WP's code
  and this WP's text. Read the actual source files — do not trust the WP
  text alone.
- [ ] **Function signatures are compatible** with how this WP intends to
  call them. Verify parameter types, return types, and whether the function
  is a pure helper vs a move function (move functions take `MoveContext`
  and perform validation/gating — they cannot be called from helpers).
- [ ] **Move classification is correct** — if the WP adds new moves, verify
  whether they are core moves (registered in `CoreMoveName` / `CORE_MOVE_NAMES`)
  or domain moves with internal stage gating. Do not expand `CoreMoveName`
  without explicit authorization. Domain moves use `G.currentStage` checks
  internally — this is the established pattern from WP-014A/016.
- [ ] **Field paths in G are verified** against `LegendaryGameState` in
  `types.ts`. Do not assume field paths from WP text — read the actual
  type definition.
- [ ] **Helper return patterns are understood** — pure helpers return new
  values (not void). The caller must assign the return value back to `G`.
  Treating a pure helper as a void mutator is a silent no-op.
- [ ] **Optional fields are identified** — if a consumed type has optional
  fields (e.g., `effects?: HeroEffectDescriptor[]`), the WP must handle
  `undefined` explicitly.
- [ ] **Data source identity verified** — if the WP loads files from disk,
  R2, or database, verify that the correct file is being loaded. Files with
  similar names but incompatible schemas (e.g., `sets.json` vs
  `card-types.json`) can be silently confused — Zod validation may pass
  with zero results instead of throwing. Check that loader code references
  the correct source by name, not just by path pattern (WP-003 Defect 1,
  D-1203).
- [ ] **TypeScript types accommodate real data quirks** — if the WP
  consumes card data, verify that TypeScript types are wide enough for
  actual data. Card fields may contain modifier strings (`"2+"`, `"2*"`),
  nulls, or unexpected types that compile against Zod schemas but fail
  narrow TypeScript interfaces. Read `packages/registry/src/schema.ts`
  comments for known quirks before defining or consuming card types
  (WP-003 Defect 2, D-1204).
- [ ] **Schema-engine alignment verified** — if the WP touches configuration
  schemas (JSON, Zod, or similar), verify that schema field names, types,
  and constraints match the corresponding TypeScript contract exactly.
  Schema-engine drift is a stop-ship class failure: mismatched field names
  or missing fields break replay determinism (D-1244 through D-1248).
- [ ] **Handler ownership is explicit** — if the WP defines data-only
  contracts (e.g., hook definitions, effect descriptors), verify that the
  WP explicitly states where runtime handler functions live. Data stored in
  `G` must never contain functions. Handler code belongs in an
  `ImplementationMap` or equivalent runtime-only structure outside `G`
  (D-1232).
- [ ] **Persistence classification is clear** — if the WP adds new fields
  to `G`, verify that ARCHITECTURE.md Section 3 documents which persistence
  class each field belongs to: runtime state (in G, never persisted),
  configuration (MatchSetupConfig, persisted separately), or snapshot
  (audit record with counts only). Derived fields (e.g., endgame outcome)
  must never be stored. Setup-only data (e.g., `hookRegistry`) must be
  excluded from snapshots.

- [ ] **WP scope pre-validated against ARCHITECTURE.md** — if the WP's
  scope section specifies data structures, field layouts, or storage
  locations, verify they match ARCHITECTURE.md. WP scope sections are
  subordinate to architecture — if they contradict, the WP must be
  corrected before execution (D-1212: WP-005B specified flat global
  piles but ARCHITECTURE.md required nested `G.piles`).
- [ ] **Framework API workarounds are documented** — if the WP requires
  a pattern that works around a framework limitation (e.g., module-level
  holders, CJS bridge imports), the workaround must be documented in
  DECISIONS.md as known tech debt with a `// why:` comment in code.
  Do not hide framework friction — make it visible and flagged for
  future resolution (D-1213, D-1206).
- [ ] **New types defined in dedicated contract files** — if the WP
  introduces types that will be consumed by multiple files or future WPs,
  they must be defined in a dedicated `*.types.ts` file, not inline in
  implementation code. Inline definitions create consolidation debt when
  the next WP needs the same types (D-1216: `PlayerZones` and
  `CardExtId` defined inline in WP-005B, had to be moved to
  `zones.types.ts` during WP-006A).

- [ ] **Immutable file designations respected** — if the WP references
  files marked immutable by prior WPs (e.g., `schema.ts`, `shared.ts`,
  `localRegistry.ts` per WP-003), verify the WP does not modify them.
  Changes to immutable files require WP-level authorization and a
  DECISIONS.md entry — not a "fix it while I'm here" change.
- [ ] **Cross-service identifiers use ext_id strings** — if the WP
  introduces or consumes card/entity references that cross package or
  storage boundaries, they must use `ext_id` text strings, never numeric
  foreign keys. Numeric IDs couple to seeding order and break on
  re-seeds (D-1201, D-1202).

- [ ] **Locked value string literals match actual code** — if the WP's
  locked values table contains string literals (condition types, keyword
  names, enum values, discriminant tags), verify them against the code
  that *produces* those strings, not just the code that consumes them.
  WP text may use aspirational or renamed labels that never appear in
  `G` at runtime. An evaluator built against WP names instead of actual
  code strings will silently fail to match any real data
  (WP-023 precedent, D-2304).
- [ ] **Runtime data availability verified for all evaluators** — if the
  WP introduces evaluators, dispatchers, or condition checkers that read
  data from `G`, verify that the required data fields *actually exist*
  in `G` at the time they will be read. Check the actual type definitions
  (`LegendaryGameState`, `CardStatEntry`, etc.), not the WP's claims
  about what data is available. A WP may assume setup-time resolution
  that was never implemented by prior WPs. If data is missing and the
  scope lock does not permit adding it, the WP must be narrowed or split
  (WP-023 precedent, D-2302).
- [ ] **Setup-time data source verified for all extractors** — if the WP
  introduces setup-time functions that extract structured data from
  registry card fields (e.g., keyword classification from ability text),
  verify that the source data actually contains the expected information.
  Read actual card JSON files — do not trust WP claims about data
  availability. A WP may assume structured metadata exists when only
  free-text ability strings are available, or may assume a keyword
  appears in the data when it refers to a different mechanic with the
  same name. If data is partially available, use the safe-skip pattern:
  implement the full extraction structure but return empty results for
  data-absent types (WP-025 precedent, D-2504).
- [ ] **WP function signatures match established conventions** — if the
  WP specifies function signatures for setup-time functions that accept
  registry data, verify they match the established convention for that
  pattern. The engine convention is `registry: unknown` with a local
  structural interface and runtime type guard (e.g.,
  `CardStatsRegistryReader` in `economy.logic.ts`). WP text may use
  direct registry types (e.g., `CardRegistry`) that violate the layer
  boundary. Correct the WP before execution (WP-025 precedent, D-2504).

- [ ] **WP file paths verified against actual filesystem** — if the WP's
  "Files Expected to Change" or scope sections reference file paths, verify
  those files actually exist in the codebase. WP text may reference intended
  file locations that were never created, or code that was later consolidated
  into a different file. Read the actual filesystem — do not trust WP path
  claims. If a WP says to create or modify `src/setup/foo.ts` but `foo()`
  actually lives in `src/rules/bar.ts`, the WP must be corrected before
  execution (WP-024 precedent, D-2405).
- [ ] **WP file paths verified against code category rules** — if the WP
  creates new files, verify the target directory matches the code category
  defined in `docs/ai/REFERENCE/02-CODE-CATEGORIES.md`. A WP may place a
  setup-time builder in a domain directory (e.g., `src/scheme/`) when code
  categories require it in `src/setup/`. Category violations create
  precedent drift that compounds across future WPs. Correct the WP file
  paths before execution (WP-026 precedent, D-2603).
- [ ] **WP gating assumptions verified as true** — if the WP's Assumes
  section contains statements marked as blocking (e.g., "if any of the
  above is false, this packet is BLOCKED"), verify each assumption against
  the actual codebase and data. A WP may assert that structured metadata
  exists in the registry when only free-text ability strings are available.
  If a gating assumption is provably false, the WP must be corrected
  before execution — either remove the blocking gate, convert the
  assumption to a safe-skip MVP constraint, or defer the WP entirely.
  Do not execute a WP whose own blocking assumptions are false (WP-026
  precedent: "Scheme card data includes setup instruction metadata"
  was false — converted to safe-skip with builder returning `[]`).
- [ ] **Decision IDs not referenced before creation** — if the WP or EC
  references a DECISIONS.md entry by ID (e.g., D-0603), verify that entry
  actually exists. WP/EC text may reference placeholder decision IDs that
  were never assigned. Use title-based references (e.g., "Representation
  Before Execution" decision) with the ID as a navigational aid, not a
  hard dependency. If the decision doesn't exist yet, either create it
  before execution or decouple the reference from the specific ID
  (WP-026 precedent: EC-026 referenced D-0603 which did not exist;
  resolved by creating D-2601 and using title-first references).
- [ ] **Decision ID search accounts for character encoding** — when
  verifying that a decision ID exists in DECISIONS.md, grep for both
  regular hyphens (`-`) and em-dashes (`‑`). DECISIONS.md uses em-dashes
  in headings (e.g., `D‑0301`) but WP/EC text typically uses regular
  hyphens (`D-0301`). A grep for `D-0301` will miss `D‑0301` and
  produce a false "not found" result. Always search by title keyword as
  a fallback (e.g., grep for "UI Consumes Projections" instead of just
  the ID). WP-028 precedent: pre-flight incorrectly reported D-0301 and
  D-0302 as missing because grep matched only regular hyphens.
- [ ] **Projection aliasing verified for all derived outputs** — if the
  WP introduces a function that reads `G` and returns a derived object
  (projection, snapshot, view model), verify that no returned array or
  object is a direct reference to a mutable `G` field. Aliasing allows
  consumers to mutate `G` through the returned reference, violating
  purity. Standard tests cannot detect this — it requires tracing each
  returned value to its source. If the source is a `G` field, a spread
  copy is required (WP-028 precedent: `cardKeywords` aliasing discovered
  in post-mortem, fixed with `[...cardKeywords]`).

- [ ] **Functions the WP calls are actually exported** — if the WP says
  to "call", "import", or "use" a function, verify that function is
  exported from its module. Local/non-exported functions cannot be
  imported by other files. If a function is not exported, the WP must
  document an alternative approach: call the underlying helper directly,
  reconstruct equivalent behavior, or request the function be exported
  (which requires modifying the source file and updating the WP scope).
  Read the actual source file — do not trust WP claims about
  importability (WP-027 precedent: `advanceStage` was a local function
  in `game.ts`, not exported. Resolved by calling `advanceTurnStage`
  from `turnLoop.ts` directly, D-2705).
- [ ] **WP approach does not require forbidden imports** — if the WP
  describes calling framework APIs (e.g., `Game.setup()`,
  `boardgame.io/testing`) or package-level APIs (e.g., `CardRegistry`
  from `@legendary-arena/registry`), verify whether the calling file is
  permitted to import those APIs under its code category rules. If the
  file must be pure (no `boardgame.io` imports) or must not cross layer
  boundaries (no registry imports in engine code), the WP must use
  alternative approaches (direct builder functions, local structural
  interfaces, mock contexts). This is a transitive import check — even
  if the WP text doesn't say `import boardgame.io`, the approach may
  require it implicitly (WP-027 precedent: `replayGame` could not call
  `Game.setup()` because replay files must not import boardgame.io;
  resolved by calling `buildInitialGameState` directly, D-2702.
  WP-025/026 precedent: `CardRegistry` replaced with `registry: unknown`
  + local structural interface).

- [ ] **Filter/consumer input type contains all needed data** — if the
  WP introduces a filter, consumer, or post-processing function that
  operates on a derived type (e.g., `UIState`) rather than on `G`,
  verify that the derived type contains ALL data the function needs to
  expose or redact. If data is missing from the type but exists in `G`,
  the upstream producer must be modified to include it — and the
  upstream producer file must be added to the WP's scope. This is a
  common scope gap for filter/consumer WPs: the WP describes what the
  filter does but assumes its input type already has the necessary data.
  Without pre-flight verification, the executing agent discovers
  mid-implementation that the filter cannot provide required data.
  Mitigation: read the actual input type definition and compare against
  the filter's information visibility matrix. If any visible data is
  absent, add the upstream producer to the scope under 01.5 wiring
  allowance (WP-029 precedent: `UIPlayerState` had no `handCards` field;
  `buildUIState` needed modification to populate it. Neither WP-029 nor
  EC-029 originally included `uiState.build.ts` in their file lists).

- [ ] **Wiring-site infrastructure singletons exist at the proposed call
  site.** If the WP introduces a function that takes a long-lived runtime
  singleton (`pg.Pool`, `CardRegistry`, `ParGate`, registry reader,
  similar) as a caller-injected parameter, AND the WP wires the function
  into a server-entry file (`apps/server/src/server.mjs`,
  `apps/server/src/index.mjs`, or equivalent), pre-flight MUST grep the
  named entry-point file for the singleton's constructor (`new Pool(`,
  `new pg.Pool(`, `createRegistryFromLocalFiles(`, `createParGate(`, etc.)
  before declaring READY. Verify three things:
  1. The constructor is **present** in the wiring-site file.
  2. The constructed singleton is **long-lived** — created during
     `startServer()` (or the equivalent startup orchestrator), held
     across requests, NOT created and `.end()`-ed inside a single
     `try…finally` block. (Short-lived singletons created per startup
     query — e.g., the rules-loader pool — are NOT suitable wiring
     targets for request-handler functions.)
  3. The constructed singleton is **in lexical scope** at the proposed
     call site. (A singleton created inside a closure is unreachable
     from the outer scope where the new wiring lands.)

  If any of (1), (2), or (3) is false, surface a PS-N "wiring-site
  availability" note before declaring READY. Two acceptable resolution
  paths: (a) defer the wiring to a future request-handler WP that owns
  long-lived singleton lifecycle, ship the function un-wired (precedent:
  WP-053 `submitCompetitiveScore` ships fail-closed with no production
  caller; WP-102 `registerProfileRoutes` ships exported but un-wired
  per D-10202); (b) request authorization to expand WP scope to include
  singleton creation in the wiring-site file (mid-execution amendment +
  new D-NNNN documenting the infrastructure-config decisions the
  amendment forces — pool max, idleTimeoutMillis, SIGTERM ordering,
  etc.). Path (a) is the smaller-blast-radius default; path (b) is
  appropriate when no future request-handler WP is plausible within
  the project's planning horizon.

If any name, field, or signature in the WP text does not match the actual
code, the WP must be corrected **before** execution. This is a blocking
pre-flight finding.

**Precedent (WP-022):** Pre-flight discovered keyword literal drift
(`gainAttack` vs `'attack'`, `amount` vs `magnitude`), a helper-vs-move
confusion (`drawCards` is a move function, not a callable helper), a
missing `playerID` parameter, and an unverified field path (`G.ko`).
All were corrected before execution. Without this verification, the
executing agent would have used wrong names and produced broken code.

**Precedent (WP-016):** Pre-flight v1 identified a design blocker —
the WP said to add moves to `CoreMoveName` but also said not to modify
that contract type. Pre-flight v2 resolved this by recognizing that
WP-014A already established the domain-move pattern (internal stage
gating without expanding `CoreMoveName`). The blocker dissolved once
the existing pattern was applied. Lesson: check whether prior WPs
already solved the design problem before declaring a blocker.

**Precedent (WP-023):** Pre-flight discovered two blocking issues.
(1) **String literal drift in locked values:** the WP's condition type
table used `requiresColor` but `heroAbility.setup.ts` produces
`heroClassMatch`. The evaluator would have silently failed to match any
real condition data. (2) **Data availability gap:** the WP assumed
team/class data was available in `G.cardStats` for condition evaluation,
but `CardStatEntry` only has `attack/recruit/cost/fightCost`. The data
exists in the registry but was never resolved into `G` at setup time,
and the WP's "Files Expected to Change" didn't include the files needed
to add it. Resolution: Option A (narrow scope) — implement all 4
evaluators but `heroClassMatch` and `requiresTeam` return `false`
(safe-skip placeholder) until a follow-up WP resolves the data into G.
`requiresKeyword` and `playedThisTurn` were fully functional with
existing G data. Lesson: verify locked value strings against the code
that *produces* them, and verify that all data read by evaluators
actually exists in G — not just that the G fields the WP names exist.

**Precedent (WP-102):** Pre-flight discovered the route handler signature
specified Express (`app: Express`, `res.status().json()`) but the actual
server uses Koa via boardgame.io's `Server({...}).router`; PS-1 corrected
the signature to `registerProfileRoutes(router: KoaRouter, database)`.
A second issue (PS-2) — WP-102 §E modified `apps/arena-client/src/router/
routes.ts`, but the file does not exist; arena-client uses a hand-rolled
query-string router in `App.vue` (`type AppRoute = 'fixture' | 'live' |
'lobby'`) and `vue-router` is not in the dependency graph. WP-102 §Goal
also forbids new npm dependencies, so adding `vue-router` was off the
table. Resolution: extend `AppRoute` with `'profile'`, parse
`?profile=<canonical>` via the existing `parseQuery()` helper, render
`<PlayerProfilePage :handle="..." />` lazy-loaded via
`defineAsyncComponent`. The path-based `/players/:handle` URL shape was
deferred to a future routing-WP that introduces `vue-router`. Lesson:
verify framework / runtime singletons in actual codebase before locking
a wiring contract. The Wiring-Site Infrastructure Singletons bullet
above formalizes the pre-flight check that would have caught WP-102
PS-1 / PS-2 at pre-flight time rather than mid-execution.

**Precedent (WP-102 mid-execution amendment, D-10202):** Pre-flight
self-flagged a pool-lifecycle gap in the session prompt's §Goal #5
("Pool lifecycle gate") but framed it as a runtime check, not a
pre-flight gate. The runtime check fired correctly mid-execution
(executor STOPped, escalated, Option A authorized — defer route
registration to a future request-handler WP). Lesson: the runtime check
is the right safety net but it is not a substitute for a pre-flight
gate. The Wiring-Site Infrastructure Singletons bullet above is the
canonical fix — pre-flight greps `apps/server/src/server.mjs` for
`new Pool(` before declaring READY when a WP introduces a
`DatabaseClient`-injecting handler whose wiring lands in `server.mjs`.
A future request-handler WP that creates a long-lived `pg.Pool` will
both wire the deferred WP-102 route and unblock any other deferred
handler packets in the same scope.

---

### Input Data Traceability Check

This section verifies that all input data influencing this WP's behavior
is identifiable, located, and governed.

Answer **YES / NO** to each:

- [ ] All non-user-generated inputs consumed by this WP are listed in
  `docs/03.1-DATA-SOURCES.md`
- [ ] The storage location for each input is known
  (Git JSON, Cloudflare R2, PostgreSQL, in-memory only, etc.)
- [ ] It is possible to determine which data source(s) would need to be
  inspected if this WP's behavior is incorrect
- [ ] The WP does not introduce "implicit data" (hardcoded literals that
  actually originate from external datasets)
- [ ] Any setup-time derived fields introduced or modified by this WP
  are listed under **Setup-Time Derived Data (In-Memory)** in
  `docs/03.1-DATA-SOURCES.md` (or explicitly noted as new and will be
  added during post-execution documentation)

Any **NO** must be explained.

A **NO** does not block execution by default, but multiple NO answers
indicate unacceptable maintenance and debugging risk and may convert
the verdict to **DO NOT EXECUTE YET**.

---

### Structural Readiness Check (Types & Contracts)

Answer **YES / NO** to each:

- [ ] All prior WPs compile and tests pass (record counts if known)
- [ ] No known EC violations remain open
- [ ] Required types/contracts exist and are exported as referenced by `WP-XXX`
- [ ] No naming or ownership conflicts (no duplicate "engine-wide" types)
- [ ] No architectural boundary conflicts anticipated at the contract level
- [ ] If the WP touches database schemas or migrations: existing migrations
  are idempotent (`CREATE TABLE IF NOT EXISTS`, `INSERT ... ON CONFLICT`)
  and the WP's changes follow the same pattern
- [ ] If the WP depends on R2 data: `pnpm validate` passes with zero
  errors (warnings are acceptable and do not block)
- [ ] If the WP reads data from G fields: the actual type definitions
  contain the subfields the WP needs (do not trust WP claims — read the
  actual interface). A field may exist in `LegendaryGameState` but lack
  the subfields the WP assumes (e.g., `G.cardStats` exists but
  `CardStatEntry` may not have team/class fields — WP-023 precedent)
- [ ] **Consumer-signature scan (required when introducing `readonly`,
  variance tightening, or narrowing a shared-type field).** For every
  field the WP plans to tighten — e.g., `T[]` → `readonly T[]`, or
  `string` → a branded alias — grep for *production* call sites that
  pass the field into a function typed with the mutable / wider form.
  A covariance-only scan (test-factory construction sites) is **not
  sufficient**: assignment covariance lets mutable arrays flow into
  `readonly` positions, but function parameters are contravariant and
  fail in the opposite direction. The concrete scan pattern for each
  tightened field `G.<fieldName>`:
  ```bash
  # Find production call sites passing G.<fieldName> into a callee.
  # Then read each callee's signature to confirm it accepts the
  # tightened type. Any callee still typed with the wider form is a
  # ripple site — either in the WP's allowlist or blocking pre-flight.
  grep -rnE "\b(G|gameState)\.<fieldName>\b" --include="*.ts" packages/game-engine/src/ | grep -v "\.test\.ts"
  ```
  Every ripple site must either (a) be added to the WP's `## Files
  Expected to Change` allowlist with an explicit widen-the-callee
  entry, or (b) be deferred to a separate consumer-widening WP that
  runs before this one. **Precedent:** WP-087 pre-flight §3.5 scanned
  test-factory construction sites only and missed seven ripple call
  sites in `game.ts`, `hero/heroConditions.evaluate.ts`,
  `hero/heroEffects.execute.ts`, and `villainDeck/villainDeck.reveal.ts`.
  The `readonly` tightening was reverted mid-session per D-8702 and
  ultimately superseded by a test-time drift scan per D-8705. A
  consumer-signature scan at pre-flight time would have surfaced the
  ripple before session start and let the drafter choose between
  allowlist expansion, consumer-first decomposition, or dropping the
  `readonly` portion of the WP's scope.

Any **NO** must be explained. If any answer is **NO**, pre-flight is **NOT READY**.

---

### Runtime Readiness Check (Mutation & Framework)

**Required when Work Packet Class = Runtime Wiring or Behavior / State Mutation.**
Skip this section for Contract-Only WPs.
(See also: `docs/ai/REFERENCE/01.5-runtime-wiring-allowance.md`)

Answer **YES / NO** to each:

- [ ] The expected runtime touchpoints are known (e.g., `game.ts`, phase hooks, moves map)
- [ ] Framework context requirements are understood (e.g., `ctx.events.*`, `ctx.random.*`)
- [ ] Existing test infrastructure can support required mocks without modifying locked helpers
- [ ] Any runtime wiring allowance needs are anticipated (01.5 referenced if applicable)
- [ ] No architecture boundary violations are expected (engine vs framework separation)
- [ ] Integration point code has been read and the call site is confirmed
  (e.g., which function destructures what, what parameters are available)
- [ ] If the WP adds a new move or hook: stage gating requirements are
  identified (which `G.currentStage` values allow the action)
- [ ] If the WP performs multi-step mutations: the ordering is safe
  (state is not removed from a source before the destination is confirmed
  valid — avoid silent data loss)
- [ ] If the WP uses registry data: it flows through setup-time resolution
  into `G`, not runtime registry access (registry boundary)
- [ ] If the WP involves phase transitions (`ctx.events.setPhase`): any
  observability flags in `G` are set **before** the transition call, not
  after. Reversed ordering causes race conditions in multiplayer (D-1238).
- [ ] If the WP handles simultaneous or conflicting conditions: evaluation
  order is explicitly defined and documented (e.g., loss conditions checked
  before victory — D-1235).
- [ ] If the WP introduces a degradation path for unknown/unsupported
  types: the strategy is explicit (warn + continue vs skip vs throw).
  Default: warn via `G.messages` and continue — never crash the game loop
  (D-1234).
- [ ] If the WP calls move functions outside boardgame.io's framework
  context (e.g., replay harness, simulation, testing infrastructure):
  required mock context shapes are identified. Move functions destructure
  `FnContext<LegendaryGameState> & { playerID }` from boardgame.io — a
  local structural interface must be defined with the minimum fields moves
  actually use (G, playerID, ctx, events, random). Events may be no-ops;
  random must provide deterministic Shuffle. Identify which moves the WP
  will dispatch and verify their destructuring patterns in the actual
  source code (WP-027 precedent: `ReplayMoveContext` local interface).
- [ ] If the WP assumes specific mock or PRNG capabilities: verify that
  `makeMockCtx` (or the intended mock) actually supports the assumed
  behavior. `makeMockCtx` provides a deterministic reverse-shuffle, NOT
  a seeded PRNG — different seed values produce identical shuffle output.
  If the WP requires seed-faithful determinism, that is a capability gap
  requiring either new test infrastructure or a safe-skip MVP where the
  seed field is stored but not used. Document the gap explicitly
  (WP-027 precedent: MVP uses mock shuffle; seed stored for future
  seed-faithful replay, D-2704).

Any **NO** must be explained. If any answer is **NO**, pre-flight is **NOT READY**.

---

### Established Patterns to Follow (Locked Precedents)

The following patterns are validated by prior WPs and must be reused rather
than reinvented:

- Inline mock contexts for tests requiring framework features (e.g.,
  `ctx.events`, `ctx.random`) — do NOT modify shared helpers
- Passing `ctx` directly where it is structurally compatible with narrower
  interfaces (e.g., `ShuffleProvider`)
- Pure helper functions return new values; moves assign results into `G`
  under Immer
- Mutating `G` occurs only inside move implementations or
  framework-authorized contexts (phase hooks, `onBegin`)
- No changes to locked helpers unless a WP explicitly authorizes it
- Using `ctx: unknown` with local interface narrowing to avoid
  `boardgame.io` imports in pure helper files (WP-022 precedent)
- Domain moves (non-core) enforce stage gating internally via
  `G.currentStage` check, leaving `CoreMoveName` unchanged as the game
  grows (WP-014A/016 precedent)
- Local structural interfaces (e.g., `VillainDeckRegistryReader`,
  `CardStatsRegistryReader`) instead of importing registry types — with
  runtime type guards for partial test mocks (WP-014B precedent)
- Setup-time registry resolution into `G` fields (e.g.,
  `G.villainDeckCardTypes`, `G.cardStats`, `G.heroAbilityHooks`) so moves
  never query registry at runtime (WP-014B/018/021 precedent)
- Multi-step mutation ordering: confirm destination is valid before
  removing from source. Deferred removal prevents silent data loss
  (WP-015A precedent — card-drop fix)
- Fail-closed handling: missing classifications, insufficient resources,
  empty piles all produce deterministic no-ops with messages — never
  silent corruption (WP-014A/016/017/018 precedent)
- Micro-patch packets (e.g., WP-015A) for bugs discovered in completed
  WPs, rather than modifying the original WP or its contract files
- Two-step declarative + deterministic execution: declare intent as data
  (hook definitions, effect descriptors), then apply deterministically
  (executeRuleHooks collects effects, applyRuleEffects mutates G).
  Enables replay inspection without mutation (WP-009B precedent, D-1233)
- Data-only contracts in `G`, handler functions in `ImplementationMap`
  outside `G`. Never store functions, closures, or class instances in
  game state (WP-009A/009B precedent, D-1232)
- Observability flags set in `G` before phase transitions — flag first,
  then `ctx.events.setPhase()`. Reversed ordering causes multiplayer
  race conditions (WP-011 precedent, D-1238)
- Graceful degradation for unknown types: warn via `G.messages` and
  continue execution — never crash the game loop (WP-009B precedent,
  D-1234)
- Fail-closed configuration: `additionalProperties: false` on all
  config schemas, hard engine limits in schema constraints, explicit
  schema-engine alignment tests (Phase 3 exit gate, D-1244)
- Persistence class separation: runtime state (in `G`, never persisted),
  configuration (`MatchSetupConfig`, persisted separately), snapshots
  (audit records with counts only, no full card arrays, no derived
  fields). Each class has different serialization rules (WP-013
  precedent, ARCHITECTURE.md Section 3)
- Loss-before-victory evaluation order when conditions can be
  simultaneous — rulebook authority resolves ambiguity, not inference
  (WP-010 precedent, D-1235)
- Virtual card ext_id formats are locked conventions:
  `henchman-{slug}-{NN}` (zero-padded), `scheme-twist-{slug}-{NN}`,
  `bystander-villain-deck-{NN}`. Virtual cards exist only in `G`, never
  in the registry. Any deviation breaks replay targeting and audit trails
  (WP-014B precedent, D-1410 through D-1413)
- Fail-closed vs silent no-op distinction: structural invariant
  violations (malformed city, missing classification) produce immediate
  failure with `G.messages` entry. Unmet gameplay conditions (insufficient
  attack, empty supply pile) produce silent no-op (return void). These
  are different failure modes with different observability requirements
  (WP-003/014A/015A/016 precedent)
- Top-of-pile convention: `pile[0]` is always top of pile across all
  zone arrays. Consistent array semantics for deck draws, supply
  consumption, and reveal operations (D-1705)
- Drift-detection tests for every canonical array + union pair: the
  array must match the union type exactly. Adding a new phase, stage,
  keyword, effect, or card type requires updating BOTH the union and its
  canonical array. Tests enforce parity at build time (WP-007A/009A/014A/
  021 precedent)
- WP splitting when registry schema gaps block implementation: if a WP
  discovers that registry data is incomplete or ambiguous, split the WP
  rather than inventing data. Separate the mechanics (which can proceed)
  from the data resolution (which requires decisions). WP-014A/B split
  is the canonical example — reveal pipeline separated from deck
  composition so Phase 4 could proceed despite schema gaps
- A/B packet pattern: contracts before implementation. Define types,
  validators, and canonical arrays in an A packet before implementing
  logic in the B packet. This locks the API surface before implementation
  makes it hard to change. Tests verify the contract before any logic
  runs. Subsequent WPs can depend on stable contracts (WP-005A/B,
  006A/B, 007A/B, 008A/B — emergent Phase 1 pattern, now project norm)
- New types consumed by multiple files must be defined in dedicated
  `*.types.ts` contract files, not inline in implementation code. Inline
  definitions create consolidation debt when the next WP needs the same
  types (WP-005B/006A precedent, D-1216)
- Framework workarounds documented as explicit tech debt in DECISIONS.md
  with `// why:` comments. Do not hide framework friction behind clean
  APIs — make the workaround visible and flagged for future resolution
  (D-1213 module-level registry holder, D-1206 CJS bridge)
- Test helpers must include state isolation mechanisms (reset hooks,
  `clearRegistryForSetup()`). Shared module-level state in test helpers
  can pollute across tests and break determinism proofs (WP-005B
  revision pass)
- Cross-service identifiers use `ext_id` text strings, never numeric
  foreign keys. Numeric IDs couple to database seeding order and break
  all saved configurations on re-seeds. This applies to
  `MatchSetupConfig` fields, `game_sessions` columns, and all zone
  contents (WP-002 precedent, D-1201, D-1202)
- Files designated immutable by prior WPs cannot be modified without
  WP-level authorization and a DECISIONS.md entry. Immutable files
  include `schema.ts`, `shared.ts`, `localRegistry.ts` (WP-003).
  "Fix it while I'm here" changes are forbidden on immutable files.
- End-to-end architectural proof before feature work: WP-004 wired
  real packages into the server and proved the architecture works
  before Phase 1 features started. New architectural layers should be
  proven with a wiring WP before feature WPs build on them.
- Governance artifacts are load-bearing: DECISIONS.md records permanent
  choices that block future mistakes; REFERENCE documents coordinate
  across sessions; Execution Checklists enforce quality gates. All
  three must exist and be used operationally — they are not optional
  documentation (WP-001 precedent)
- Idempotent infrastructure: all DDL uses `CREATE TABLE IF NOT EXISTS`,
  all inserts use `INSERT ... ON CONFLICT`. Migrations are safe to
  re-run. No rollback mechanism exists — manual recovery via `psql`
  if needed. This is intentional simplicity (FP-01/02 precedent)
- R2 validation errors vs warnings: errors block execution (missing
  data, structural failures), warnings do not (known data quirks like
  `[object Object]` abilities, cross-set duplicate slugs, missing VP).
  `pnpm validate` exit code reflects this distinction (FP-00.5
  precedent)
- PostgreSQL namespace: all tables under `legendary.*` schema, never
  `public.*`. Primary keys use `bigserial`. Cross-service identifiers
  use `text` (not numeric FK). Image URLs use hyphens, never
  underscores (FP-01/02 precedent)
- Foundation Prompts are stop-on-failure gates that block ALL downstream
  work. They are distinct from per-WP lint gates. FP ordering is
  strict: 00.4 -> 00.5 -> 01 -> 02. Each validates a layer of the
  environment that subsequent layers depend on (FP precedent)
- Safe-skip placeholder pattern for data gaps: when a WP's evaluators
  or dispatchers require data that doesn't exist in G yet, implement
  the full evaluator structure but return safe defaults (`false` for
  conditions, no-op for effects) for blocked types. This preserves the
  extension seam (future WPs enable blocked types by adding data, not
  by refactoring evaluators), keeps functional types working, and
  follows the same pattern as WP-022's unsupported-keyword skip. The
  WP's scope narrows rather than expands — no new G fields or setup
  code changes are needed. Each placeholder must have a `// why:`
  comment explaining what data is missing and which follow-up WP will
  resolve it (WP-023 precedent, D-2302)
- Safe-skip for effect type gaps: when a WP's handler needs to produce
  an effect that has no corresponding `RuleEffect` type yet (e.g.,
  wound card movement requires `'gainWound'`), use available effect
  types (`modifyCounter` + `queueMessage`) for tracking and
  observability, and defer the full behavior to a future WP that adds
  the new effect type. The handler structure is complete — only the
  effect vocabulary is incomplete. This extends the WP-023 safe-skip
  pattern from condition evaluators to effect production. Document
  the gap with a `// why:` comment and a DECISIONS.md entry
  (WP-024 precedent, D-2403)
- Inline behavior as effect type workaround: when a WP's behavior
  requires an effect type that doesn't exist (e.g., `gainWound`) AND
  the codebase already has an inline implementation of that behavior
  (e.g., escape wounds call `gainWound` directly in the reveal
  pipeline), prefer the inline pattern over counter-only tracking.
  The inline approach delivers functional MVP behavior instead of
  deferred tracking. This refines the D-2403 safe-skip pattern: use
  counter-only tracking when no inline precedent exists, but use the
  inline precedent when one does. The function must still be a pure
  helper called from within a move context (framework-authorized
  mutation). Document the inline choice with a `// why:` comment and
  a DECISIONS.md entry (WP-025 precedent, D-2503)
- Setup-time data extraction with safe-skip for missing sources: when
  a WP introduces setup-time functions that classify cards by parsing
  registry data (e.g., keyword extraction from ability text), not all
  classification types may have data sources. Implement the full
  classification structure but return empty results for types with no
  data source. This extends the WP-023 safe-skip pattern from runtime
  evaluators to setup-time extractors. Each missing data source must
  have a `// why:` comment and a DECISIONS.md entry. Tests use
  synthetic data to prove the mechanics work independently of data
  availability (WP-025 precedent, D-2504)
- Safe-skip for type constraint gaps: when a WP defines an instruction
  type or handler in a closed union but the underlying type system
  prevents functional implementation (e.g., `modifyCitySize` defined in
  `SchemeSetupType` but `CityZone` is a fixed 5-tuple that cannot be
  resized), the handler must log a warning to `G.messages` and return
  `G` unchanged — no mutation, no throw. The type exists in the union
  so the executor infrastructure is complete, but functional behavior
  is deferred until the blocking type constraint is resolved by a future
  WP. This extends the safe-skip pattern from data availability gaps to
  type system constraints. Document the gap with a `// why:` comment
  and a DECISIONS.md entry (WP-026 precedent, D-2602)
- Stub replacement cascades into integration tests: when a WP replaces
  stub handlers (established by an earlier WP as proof-of-concept)
  with real handlers, integration tests that assert stub-specific
  behavior (message strings, trigger names, counter deltas) require
  value-only updates under the 01.5 runtime wiring allowance. This
  is a predictable consequence — pre-flight should identify which
  integration tests assert stub behavior and authorize the assertion
  updates in advance. No new tests or logic may be added under this
  allowance (WP-024 precedent, D-2404)
- Framework bypass for infrastructure WPs: when a WP needs to construct
  initial game state without calling `Game.setup()` (which requires
  importing boardgame.io), call `buildInitialGameState` directly with a
  `SetupContext` from `makeMockCtx`. This avoids framework dependency
  while producing identical initial state. Document the bypass in
  DECISIONS.md with a `// why:` comment. The pattern applies whenever
  infrastructure code needs setup-time state construction outside the
  boardgame.io lifecycle (WP-027 precedent, D-2702)
- Move dispatch outside framework via static MOVE_MAP: when a WP
  executes moves outside boardgame.io's runtime (replay, testing,
  simulation), build a static map from move name strings to imported
  move functions. Use a local structural interface (e.g.,
  `ReplayMoveContext`) with no-op events and deterministic shuffle —
  no boardgame.io import in the dispatching file. Non-exported moves
  (e.g., `advanceStage` local to `game.ts`) must be reconstructed from
  their underlying pure helpers (e.g., `advanceTurnStage` from
  `turnLoop.ts`). Unknown move names produce a warning in `G.messages`
  and are skipped — never throw (WP-027 precedent, D-2705)

- Local structural interface for framework ctx subset: when a WP
  introduces a function that reads boardgame.io `Ctx` fields but must
  not import `boardgame.io`, define a local structural interface with
  only the fields the function actually reads (e.g., `UIBuildContext
  { readonly phase, readonly turn, readonly currentPlayer }`). Mark
  fields `readonly`, do not add optional fields, do not widen beyond
  what the function uses. Tests supply a plain object literal matching
  the interface — not `makeMockCtx` (which returns `SetupContext`, a
  different shape). This extends the WP-022/027 local interface pattern
  from registry readers and move contexts to framework context consumers
  (WP-028 precedent, D-2801)
- Projection aliasing prevention: when a WP produces a derived
  projection (UIState, snapshot, view model) by reading `G` fields,
  every returned array or object must be a **new copy**, not a reference
  to a mutable `G` field. Use spread operator (`[...array]`,
  `{...object}`) for mutable sources. Aliasing bugs allow consumers to
  mutate `G` through the returned reference, violating purity and
  determinism. Standard tests (including `JSON.stringify` equality
  checks) cannot detect aliasing — it requires line-by-line inspection
  of return paths. Treat aliasing as a mandatory post-mortem check for
  all projection WPs (WP-028 precedent — `cardKeywords` aliasing
  discovered and fixed in post-mortem)
- Lifecycle isolation for non-framework code: when a WP introduces new
  functions that are NOT part of the boardgame.io lifecycle (projections,
  snapshots, replay consumers), the session prompt must explicitly
  prohibit wiring into `game.ts`, moves, or phase hooks. Without this
  constraint, Claude may "helpfully" wire new code into the lifecycle
  (WP-028 precedent — `buildUIState` lifecycle prohibition)
- Projection purity as a strict contract: pure projection functions
  must not cache, memoize, close over `G`, or retain state between
  calls. These are forbidden *permanently*, not just at MVP. The
  docstring must include a "Forbidden behaviors" block to prevent future
  optimization attempts from breaking purity (WP-028 precedent)
- Contract enforcement tests (not illustrative tests): when a WP
  introduces a long-lived contract (UIState, MatchSetupConfig,
  ReplayInput), tests must be framed as **contract enforcement** — if
  tests fail, the implementation is incorrect. Do NOT weaken assertions
  to make tests pass. Add a test-intent statement to the session prompt
  so the executing agent understands the tests are non-negotiable
  (WP-028 precedent)
- Upstream producer modification for downstream filter: when a WP
  introduces a filter or consumer that needs data not present in the
  upstream producer's output type, the upstream producer must be modified
  to include that data. The filter operates on the produced type — it
  cannot add data that doesn't exist. During pre-flight, verify that
  the filter's input type contains ALL data the filter needs to expose
  to any audience. If data is missing, add the upstream producer to the
  WP's file scope under 01.5 wiring allowance. Use spread copies in
  the producer to prevent aliasing (WP-029 precedent: `buildUIState`
  needed `handCards: [...zones.hand]` so `filterUIStateForAudience`
  could expose hand ext_ids to the active player. Neither WP-029 nor
  EC-029 originally listed `uiState.build.ts` — pre-flight caught
  this as a blocking scope gap)
- Top-level field redaction by audience: when a UIState field is not
  per-player but should be audience-restricted (e.g., turn economy),
  the filter zeros or replaces the field based on audience kind. Use
  a const sentinel value (e.g., `REDACTED_ECONOMY`) spread-copied
  into the result to maintain type stability. Do not use `undefined`
  or optional typing for always-present fields — zero the values
  instead (WP-029 precedent: economy zeroed for non-active player
  and spectator audiences, D-2903)
- Filter aliasing is lower-risk than projection aliasing: filters
  that operate on projections (UIState) rather than on G share
  references with the input projection, not with mutable game state.
  Shallow copies of input arrays and objects are sufficient when the
  entire pipeline is pure. Deep cloning is unnecessary unless the
  filter's consumer is expected to mutate the result. Post-mortem
  should still verify no aliasing, but severity is lower than
  G-referencing projections (WP-029 precedent: shallow copies in
  `filterUIStateForAudience` confirmed sufficient by post-mortem)
- `exactOptionalPropertyTypes` TypeScript strictness: when building
  an object with an optional field, a ternary that returns
  `T[] | undefined` is not assignable to `field?: T[]` under
  `exactOptionalPropertyTypes: true`. Use conditional assignment
  instead: build the base object without the field, then assign it
  in an `if` block. This avoids the `undefined` value in the union.
  (WP-029 precedent: `preserveHandCards` had to use conditional
  assignment for `handCards` instead of inline ternary)
- Named union for related function parameters: when two or more
  functions exchange a literal value that can drift (e.g., an outcome
  string produced by one function and consumed by another), define it
  as a named `type` union in the shared contract file and use the
  named type in every signature that touches it. Inline literal unions
  in individual signatures allow drift — a producer might return
  `'victory'` while a consumer accepts `string`, letting `'Victory'`
  or `'win'` compile silently. A named union forces compile-time
  consistency across all call sites. Apply this when the same literal
  union appears in more than one public function signature (WP-030
  precedent: `ScenarioOutcome = 'victory' | 'defeat' | 'incomplete'`
  shared between `evaluateScenarioOutcome` return type and
  `advanceCampaignState` outcome parameter, introduced during pre-
  flight tightening after external review flagged the drift risk)
- Explicit "01.5 NOT INVOKED" declaration for purely additive WPs:
  when a WP is Contract-Only and does not trigger any of the 01.5
  conditions (new `LegendaryGameState` field, `buildInitialGameState`
  shape change, new `LegendaryGame.moves` entry, new phase hook), the
  session prompt must include an explicit section stating that the
  01.5 runtime-wiring allowance is **NOT INVOKED**, with the four
  criteria enumerated and marked absent. This prevents the executing
  agent from (a) silently invoking the allowance as a loophole to
  touch off-allowlist files, or (b) being confused about whether the
  allowance was meant to apply. The session prompt should also
  explicitly cite 01.5 §Escalation: *"It may not be cited retro-
  actively in execution summaries or pre-commit reviews to justify
  undeclared changes."* If an unanticipated structural break appears
  mid-execution, the agent must stop and escalate, not force-fit.
  (WP-030 precedent: the first purely-additive WP to explicitly
  declare 01.5 not-invoked in its session prompt and have the post-
  mortem affirm that no files outside the allowlist were touched)
- Reserved parameter pattern for signature stability: when a function
  accepts a parameter whose value is not used in the MVP implementa-
  tion but is part of a locked API shape that future WPs will extend
  (e.g., `advanceCampaignState(state, scenarioId, outcome, rewards)`
  where `outcome` will drive branching logic in a future WP but is
  currently unused), reference the parameter in a `void parameter;`
  no-op statement at the top of the function body with a `// why:`
  comment explaining that the parameter is reserved for future
  branching logic. This avoids TypeScript unused-parameter warnings,
  preserves the locked API shape, and avoids the cost of widening the
  signature later. Do NOT delete the parameter or prefix it with `_`
  — both approaches require changing call sites when the parameter
  becomes meaningful. (WP-030 precedent: `advanceCampaignState`
  accepts `outcome: ScenarioOutcome` which is reserved for future
  branching; MVP uses `void outcome;`)
- Code-category classification for new engine subdirectories as
  pre-flight pre-condition: when a WP creates files in a new
  `packages/game-engine/src/<dir>/` that is not listed in
  `02-CODE-CATEGORIES.md`, the pre-flight must flag it as a blocking
  finding and require a DECISIONS.md entry classifying the directory
  before the session prompt is generated. The classification follows
  D-2706 (replay) and D-2801 (ui) template: cite purity, no I/O, no
  `boardgame.io`/registry imports, and follow-all-engine-category-
  rules as the rationale. Use `Status: Immutable` and a `### D-NNNN`
  heading. This is codified in §Code Category Boundary Check; the
  pattern here is the **workflow** — catch during pre-flight as a
  PS-#, resolve by creating the decision before execution starts,
  log the resolution in the pre-flight's Authorized Next Step section
  (WP-027 precedent: D-2706 for replay; WP-028 precedent: D-2801 for
  ui; WP-030 precedent: D-3001 for campaign, resolved as PS-1 during
  pre-flight; WP-031 precedent: D-3101 for invariants, resolved as
  PS-1 during pre-flight — fourth instance of the same pattern)
- Pre-flight must verify "scan-G-and-throw" invariants against the
  concrete output of upstream builders, not just against the
  abstract type system: when a WP introduces a check function that
  iterates `G` (or any field of `G`) and throws on a specific
  predicate, pre-flight must trace the predicate against what
  `buildInitialGameState` (and any other builder that produces valid
  `G`) actually emits. Type-system-level reasoning is necessary but
  not sufficient — the type may declare `CardExtId = string` and
  permit any string, while the concrete builder reuses a small set
  of fungible token strings (8 copies of `'starting-shield-agent'`
  per player deck, 30 copies of `'pile-bystander'` per supply pile,
  etc.) that would false-positive a literal cardId-uniqueness check
  on every valid G. The pre-flight Dependency Contract Verification
  step must include a "concrete output trace" check for any
  scan-and-throw invariant: read the upstream builder, identify the
  shape of the values it actually emits, and confirm the predicate
  classifies them correctly. (WP-031 precedent: original
  `checkNoCardInMultipleZones` spec said "no CardExtId appears in
  more than one zone" without verifying against `pilesInit.ts`
  `createPileCards` and `buildStartingDeckCards`. The executor
  caught the conflict at implementation time and required a
  mid-session amendment + new D-3103 to narrow the check to
  fungible-exclusion semantics. Future "scan-G" WPs must trace
  builder output during pre-flight.)
- Setup-only wiring scope as the default for runtime-wiring WPs
  whose checks could plausibly run at multiple lifecycle points:
  when a WP's pipeline (e.g., a new orchestrator, validator, or
  observer) could plausibly fire at setup return, after each move,
  on phase transitions, or on turn boundaries, pre-flight must
  evaluate each candidate point against the existing test baseline
  and lock a single point. Per-move and per-transition wiring
  should be deferred to a follow-up WP unless the executor can
  prove the broader wiring will not regress any existing test.
  Reasons: (a) per-move wiring runs the new pipeline on every
  test that flows through a move, multiplying the failure
  surface; (b) handcrafted partial-G tests may fail invariants
  that don't apply to their narrow scope; (c) the broader wiring
  decision benefits from real-world performance data that doesn't
  exist before the MVP ships. Lock the single point in pre-flight
  via a DECISIONS.md entry; defer the broader wiring to a
  follow-up WP. (WP-031 precedent: D-3102 locks "Option B
  setup-only" for `runAllInvariantChecks`. Per-move wiring is
  deferred to a follow-up WP. The decision is captured as PS-2
  during pre-flight, before the session prompt is generated.)
- Lifecycle check parameter widening to `| undefined` for
  mock-context compatibility: when a check function reads a
  framework lifecycle field (e.g., `boardgame.io`'s `Ctx.phase`
  or `Ctx.turn`) and the engine's existing test mock helpers
  (`makeMockCtx`) do not set the field, the check function's
  parameter type must be widened to `string | undefined` /
  `number | undefined` even though the framework type declares
  the field as non-null. The compile-time type comes from the
  framework declaration; the runtime value comes from the mock
  cast in tests. The check function must short-circuit (silent
  skip) when the field is `undefined` — lifecycle validity
  cannot be asserted on an absent field. Add a `// why:` comment
  citing the runtime-vs-compile-time gap. Do NOT modify
  `makeMockCtx` to add the missing fields, and do NOT widen the
  framework-context interface used elsewhere — both would have
  ripple effects on unrelated tests. The widening lives only on
  the check function's parameter signature. (WP-031 precedent:
  Amendment A-031-03 widened `checkValidPhase` and
  `checkTurnCounterMonotonic` parameter types from `string` /
  `number` to `string | undefined` / `number | undefined`
  because `makeMockCtx` does not set `phase` or `turn` even
  though `boardgame.io` Ctx declares them non-null. The local
  `InvariantCheckContext` interface remained `phase?: string;
  turn?: number;` per pre-flight RS-2 lock.)
- Scope-neutral mid-execution amendment as a permitted operation
  under EC mode: when an executor halts at a §Implementation Task
  boundary because the WP spec is incompatible with reality (e.g.,
  the task instructs "build a Set of CardExtIds" against a `G`
  whose builder emits non-unique CardExtIds), the executor may
  request user authorization for a mid-execution amendment. The
  amendment qualifies as **scope-neutral** if and only if (a) it
  does not add or remove files from the WP allowlist, (b) it does
  not change the locked test count, (c) it does not change the
  wiring decision, (d) it does not change the category taxonomy
  or any drift-pinned canonical array, and (e) the scope-neutral
  changes are captured in: WP §Amendments section, a new
  DECISIONS.md entry, a re-confirmed pre-flight (with new RS
  entries + a new PS-#), and a re-confirmed copilot check (with
  new findings marked PASS after the amendment). The executor
  must not silently apply the change — every artifact in the
  audit trail must reflect the amendment. (WP-031 precedent:
  Amendments A-031-01/02/03 + new D-3103 + pre-flight RS-9/10/11
  + PS-3 + copilot check Re-Run 2026-04-15 with Findings
  #31/32/33. Same 11-file allowlist, same +10 test count, same
  D-3102 wiring, same 5-category taxonomy. Authorized by the
  user before any check-implementation code was committed.)
- Post-mortem fix as in-allowlist refinement (distinguishes from
  01.5 invocation): when the post-mortem (step 4 per
  `01.6-post-mortem-checklist.md`) discovers a hidden-coupling
  risk, an aliasing bug, or a comment/`why:` gap inside an
  already-allowlisted file, the fix may be applied immediately
  in the same session as a strict in-allowlist refinement. This
  is **not** a 01.5 runtime-wiring-allowance edit — 01.5 is for
  off-allowlist edits required by structural integration. An
  in-allowlist refinement does not require 01.5 invocation; the
  post-mortem document records the fix under §10 "Fixes applied
  during post-mortem". The post-commit traceability is the
  post-mortem doc itself. (WP-031 precedent: the post-mortem
  Section 6 hidden-coupling fix replaced inline string literals
  for the six fungible token CardExtIds in
  `gameRules.checks.ts` with named imports from
  `../setup/buildInitialGameState.js` (which re-exports the
  pile constants from `pilesInit.ts`). The fix stayed inside
  the WP allowlist, was strictly behavior-preserving, and was
  re-verified at 358/94/0. Recorded under post-mortem §10
  Fixes applied.)
- Test file `describe()` wrapping when suite count is locked:
  when a WP's pre-flight Test Expectations locks both a test
  count AND a suite count (e.g., 348/93 → 358/94), the test
  file authoring convention must be explicit about whether the
  new tests live inside one `describe()` block (adds +1 suite),
  multiple `describe()` blocks (adds +N suites), or as
  top-level `test()` calls (adds +0 suites). Bare top-level
  `test()` calls do NOT register as suites in `node:test` —
  the suite count only increments per `describe()`. Pre-flight
  must specify the structural choice so the executor doesn't
  produce 358/93 instead of 358/94. (WP-031 precedent: the
  initial implementation used 10 bare `test()` calls and
  produced 358/93. The executor wrapped them in one
  `describe('runtime invariants (WP-031)')` block to satisfy
  the locked 358/94 baseline. Future test-count-locked WPs
  must specify wrapping intent in pre-flight.)
- Caller-injected dependency arrays for transport-agnostic
  validation: when a WP introduces a validation function that needs
  to check a value against a set of valid options (e.g., registered
  move names), but importing the canonical source would violate
  layer boundaries (e.g., importing `game.ts` requires
  `boardgame.io`), inject the valid set as a parameter:
  `validMoveNames: readonly string[]`. The caller (server layer,
  test fixture) provides the array. This extends the "local
  structural interface" pattern (D-2801) from types to data: just
  as `IntentValidationContext` avoids importing `Ctx`, the injected
  array avoids importing the moves registry. Tests supply the array
  explicitly. The validator has no knowledge of which moves exist —
  it is fully transport-agnostic. (WP-032 precedent: pre-flight
  RS-2 discovered `CORE_MOVE_NAMES` has only 3 of 10 registered
  moves; resolved by adding `validMoveNames` parameter instead of
  creating a new `ALL_MOVE_NAMES` constant or importing `game.ts`)
- Audit grep patterns must escape regex specials when used as
  binary "no output" gates: pre-flight verification steps that
  use `git grep <pattern>` to enforce "no output" for forbidden
  imports must escape regex special characters in the pattern,
  or the grep will match unintended strings. Specifically: the
  literal `boardgame.io` contains a `.` which `git grep` (and
  ripgrep) treat as "any character", so the pattern matches
  `boardgame.io`, `boardgame_io`, `boardgameXio`, etc. The
  intended check is for the literal import line, so the pattern
  should be either `from ['"]boardgame\.io` (matches actual
  import statements) or the dot must be escaped in the binary
  gate. Comments and JSDoc that mention `boardgame.io` for
  documentation purposes will trip the unescaped pattern even
  though they are not imports. (WP-031 precedent: the
  verification grep `git grep -l "boardgame.io"` in
  `src/invariants/` matched all 6 files because each file's
  comments mentioned `boardgame.io` in the rationale text.
  The executor rephrased every comment occurrence to "the
  game framework" to satisfy the strict grep — a workaround
  that weakened the documentation. Future verification steps
  should use the import-line-specific pattern instead. WP-033
  precedent P6-22 confirms this pattern extends to ALL
  forbidden-token grep gates, not just `boardgame.io`: a
  docstring reading "no .reduce()" tripped the `\.reduce(`
  verification grep for the content/ directory. The executor
  rephrased to "array reduce" to satisfy the binary gate.)
- Author-facing validation schemas are separate from, and
  deliberately stricter than, permissive data-loader schemas:
  when a WP introduces validation for new authored content
  alongside an existing loader that validates shipped data,
  the two layers serve different purposes and must not share
  shape. Data-loader schemas are permissive to accommodate
  real-data quirks (e.g., historical cards with optional
  fields, star-modifier costs, nullable vp). Author-facing
  schemas are stricter because authors of new content must
  supply fields that historical data sometimes omits —
  otherwise authoring typos reach runtime as structurally
  valid but game-mechanically broken content. When the
  validator lives in a layer that cannot import the loader
  (e.g., engine category importing registry is forbidden by
  D-3301 precedent family), duplicate enums and required-
  field lists locally via `// why: <layer> category must not
  import from <loader>` comments, and cite the relevant
  decision IDs for the duplication. Apply when: (a) a loader
  layer must accept real data with documented quirks, AND
  (b) an authoring workflow must reject typos that slip past
  loader permissiveness, AND (c) the validator lives in a
  layer that cannot import the loader. (WP-033 precedent —
  D-3303 "Content Validation Is Author-Facing and Separate
  from Registry Zod Schemas." Required-field list for hero
  cards in `content.schemas.ts` is stricter than the
  registry's `HeroCardSchema`; `HERO_CLASSES` is re-declared
  locally rather than imported from `HeroClassSchema`;
  henchman shape mirrors `VillainCardSchema` required fields
  without importing them. The four validation stages —
  structural, enum, cross-reference, hook consistency — are
  canonical for this pattern, with cross-reference data
  caller-injected via `ContentValidationContext` to avoid
  the layer-boundary violation.)
- Validators with string-discriminated dispatch must not
  silent-pass unknown inputs: when a validator accepts a
  `type: string` (or similar discriminator) and dispatches
  via `switch (type)` or `if / else if` chains, the default
  / fall-through branch must NOT silently return "valid."
  Authoring typos — casing variants (`"Hero"`), pluralizations
  (`"heroes"`), misspells, empty strings — are exactly the
  inputs that reach such a validator in practice, and a
  silent pass lets them reach runtime as "valid" content.
  Resolution: declare an accept-list constant
  (`ACCEPTED_<TYPE>_NAMES as const`), add a Stage 0
  accept-list check that short-circuits all remaining stages
  when the discriminator is unrecognized, and return a
  single full-sentence error capturing the as-supplied value
  verbatim. The accept-list is an internal validator
  constant — NOT a canonical drift-checked array with a
  paired test (that would widen scope). When retrofitted
  during pre-flight (e.g., in response to a copilot RISK
  finding), extend an existing test to cover the unknown-
  input branch rather than adding a new test — this
  preserves any locked test-count baseline. (WP-033
  precedent P6-23 — §Pre-Flight Locked Decisions #10
  introduced `ACCEPTED_CONTENT_TYPES` and the locked error
  shape for unrecognized `contentType` values; test 9 was
  extended with Parts A/B/C instead of adding a 10th test.)

**Deviations from these patterns require explicit justification** and may
convert pre-flight to **NOT READY** if they imply scope creep.

---

### Maintainability & Upgrade Readiness (Senior Review)

These checks assess whether the WP's design can be safely maintained,
patched, debugged, and extended by future WPs. They are design-time
properties — assessable before implementation begins.

Answer **YES / NO** to each:

- [ ] **Extension seam exists:** the WP leaves an intentional extension
  point for future behavior (e.g., keyword dispatch map, handler
  registry, ordered effect list, type union + canonical array), even
  if only a subset is used now. This does not require new abstractions —
  only acknowledgement that the seam exists and future WPs can extend it
  without refactoring.
- [ ] **Patch locality:** a future bug fix in this WP's logic would be
  localized to a small, clearly-defined set of files (ideally 1-2).
  No cross-cutting change should be required to fix a single keyword,
  rule, or effect.
- [ ] **Fail-safe behavior:** incorrect or unsupported behavior fails
  safely (skip / no-op / deterministic message) rather than partially
  mutating game state. No invariant violation can cascade across turns
  or players.
- [ ] **Deterministic reconstructability:** execution path can be
  reconstructed from existing state (`G`) and static inputs
  (registry-derived hooks, descriptors) without requiring runtime
  logging or hidden side effects. This aligns with ARCHITECTURE.md
  "Debuggability & Diagnostics" invariant.
- [ ] **Backward-compatible test surface:** new runtime fields are
  accessed defensively so older test mocks remain valid inputs. Test
  mocks from pre-WP-XXX eras do not crash when the new code runs.
- [ ] **Semantic naming stability:** exported / public names are
  semantically stable and do not encode MVP-only assumptions (`Simple`,
  `Temp`, `V1`), current control flow (`Immediate`, `Inline`), or
  implementation details that may change. If something is MVP-scoped,
  document the limitation — but do not bake it into the symbol name.

Any **NO** must be justified. A single NO is acceptable with explanation.
Multiple NO answers may indicate the WP should be split or re-scoped
before execution.

**Note:** These are checks, not new requirements. They do not authorize
new abstractions, speculative extension points, or "while I'm here"
improvements. They assess whether the WP's *existing design* is
maintainable — not whether it should be redesigned.

**Precedent (WP-022):** `MVP_KEYWORDS` Set and the keyword `switch`
provide the extension seam — WP-023 can add conditional evaluation by
extending the set and adding cases without refactoring. `HeroEffectResult`
provides deterministic reconstructability without runtime logging.
Fail-safe: unsupported keywords and invalid magnitudes skip with no
mutation. `executeHeroEffects` guards against `undefined`
`heroAbilityHooks` for backward-compatible test surface.

**Precedent (WP-023):** Condition type `switch` in `evaluateCondition`
provides the extension seam — future WPs add new condition types by
adding cases without refactoring. Safe-skip placeholders (`heroClassMatch`
and `requiresTeam` return `false`) demonstrate that blocked evaluators
can coexist with functional ones. `evaluateAllConditions` accepts
`undefined` conditions for backward-compatible test surface. Deep
equality test enforces the "conditions never mutate G" invariant.

**Precedent (WP-025):** `BoardKeyword` union + `BOARD_KEYWORDS` canonical
array provide the extension seam — new keywords add to both without
refactoring. `buildCardKeywords` safe-skips Patrol/Guard (no data source)
while Ambush is functional — blocked and functional types coexist (same
pattern as WP-023). `G.cardKeywords ?? {}` defensive access in
`fightVillain` and `villainDeck.reveal.ts` preserves all 291 pre-WP-025
tests. Ambush uses inline `gainWound` (existing codebase pattern) instead
of deferred counter-only tracking — demonstrates inline precedent
resolution for effect type gaps.

**Precedent (WP-026):** `SchemeSetupType` union + `SCHEME_SETUP_TYPES`
canonical array provide the extension seam — new instruction types add
to both without refactoring. `buildSchemeSetupInstructions` returns `[]`
at MVP (no structured registry metadata) — the entire builder is a
safe-skip that preserves the call site and executor infrastructure for
future data resolution. `modifyCitySize` demonstrates type-constraint
safe-skip: the handler exists in the executor but logs warning + no-op
because `CityZone` is a fixed tuple (D-2602). `G.schemeSetupInstructions
?? []` defensive access preserves all 305 pre-WP-026 tests.
`executeSchemeSetup` is a pure function called after base state
construction — clean separation of state building from instruction
application. Pre-flight identified 5 blocking issues (false gating
assumption, registry type violation, missing decision ID, wrong file
location, unlocked CityZone approach) — all resolved as scope-narrowing
corrections without re-running pre-flight.

---

### Code Category Boundary Check

Verify that all files introduced or modified by this WP respect code
category boundaries as defined in `docs/ai/REFERENCE/02-CODE-CATEGORIES.md`.

- [ ] All new or modified files fall cleanly into one existing code
  category (`engine`, `framework`, `moves`, `setup`, `data-input`,
  `server`, `test`, `infra`, `docs`)
- [ ] Each file's category permits all imports and mutations it uses
- [ ] No file blurs category boundaries (e.g., engine code importing
  boardgame.io, registry code containing game logic, move code
  performing IO)
- [ ] If the WP creates files in a new directory not currently listed in
  `docs/ai/REFERENCE/02-CODE-CATEGORIES.md`: the directory must be
  classified under an existing category before execution. Document the
  classification in DECISIONS.md. Do not create new categories without
  a DECISIONS.md entry. Verify the new directory's rules match its
  assigned category (WP-027 precedent: `src/replay/` classified as
  engine category, D-2706)

Any boundary violation is a **blocking pre-flight finding** — the file
must be moved, split, or the WP re-scoped.

---

### Scope Lock (Critical)

#### WP-XXX Is Allowed To

List only what the WP is permitted to implement, and the expected file targets.

Examples:

- Create: `src/moves/zoneOps.ts` — pure helper (no framework imports)
- Create: `src/moves/coreMoves.impl.ts` — move implementations (framework ctx allowed via parameter)
- Modify: `src/game.ts` — wire implementations into `moves`
- Add: integration tests required by the WP
- Update required governance docs

#### WP-XXX Is Explicitly Not Allowed To

List forbidden changes.

Examples:

- No modification of prior contract files (unless explicitly authorized)
- No new gameplay systems beyond this WP
- No randomness via `Math.random()`
- No `.reduce()` if disallowed by rules
- No forbidden imports (e.g., `boardgame.io` in pure helpers)
- No touching non-allowlisted files
- No calling move functions from helper context (move functions perform
  validation/gating — extract the underlying logic instead)
- No expanding `CoreMoveName` / `CORE_MOVE_NAMES` without explicit
  authorization (domain moves use internal stage gating instead)
- No runtime registry access from moves (use setup-time resolution
  into `G` fields)
- No removing state from a source zone before confirming the destination
  is valid (mutation ordering discipline)
- No direct access to input data sources not listed in
  `docs/03.1-DATA-SOURCES.md` without updating that document first
- No depending on non-exported functions without documenting an
  alternative approach (verify importability during pre-flight)
- No assuming mock/PRNG capabilities that haven't been verified against
  actual test infrastructure (e.g., seed-driven shuffle in `makeMockCtx`)

**Rule:** Anything not explicitly allowed is out of scope.

---

### Test Expectations (Locked Before Execution)

Define the testing intent for this Work Packet before execution begins.
These expectations become part of the scope lock — deviations require
pre-flight escalation or a WP split.

Include:

- **New tests:** expected count and file location(s)
- **Existing test changes:** whether any existing tests are expected to
  require updates (e.g., structural assertion value changes under
  `01.5-runtime-wiring-allowance.md`)
- **Prior test baseline:** confirmation that all prior tests must continue
  to pass, with the current passing count recorded
- **Test boundaries:** what kinds of test changes are forbidden (e.g., no
  new logic in existing test files, no `boardgame.io/testing` imports)
- **Defensive guards:** whether new code needs to handle older test mocks
  that may not include fields from recent WPs (e.g., `G.heroAbilityHooks`
  may be `undefined` in pre-WP-021 test states)

Example (WP-008B style):

```
- 9 new integration tests in src/moves/coreMoves.integration.test.ts
- All 80 existing tests must continue to pass
- game.test.ts may require structural assertion updates (move count)
  under 01.5 runtime wiring allowance — value-only, no new logic
- No boardgame.io imports in test files
- No modifications to makeMockCtx or other shared test helpers
```

Example (WP-022 style — defensive guard):

```
- 11 new tests in src/hero/heroEffects.execute.test.ts
- All 255 existing tests must continue to pass
- Existing test mocks do not include heroAbilityHooks — new code must
  guard against undefined to avoid breaking existing playCard tests
- No modifications to makeMockCtx or other shared test helpers
```

**Infrastructure & Verification WP note:** For WPs that construct mock
move contexts or test infrastructure (e.g., replay harnesses), specify
whether new mock patterns are test-only (local to the new test file) or
shared (modifying `makeMockCtx` or creating reusable helpers). If
test-only, confirm they don't conflict with existing test helpers. If the
WP creates new mock patterns that future WPs should adopt, document the
pattern in the test expectations so it becomes a locked precedent
(WP-027 precedent: `ReplayMoveContext` is local to `replay.execute.ts`,
not shared; `makeMockCtx` was not modified).

**Rule:** Test expectations are locked for execution. If the implementation
discovers that more or fewer tests are needed, or that existing tests
require logic changes (not just value updates), the deviation must be
documented in the execution summary and justified in the pre-commit review.

---

### Mutation Boundary Confirmation

**Required when Work Packet Class = Behavior / State Mutation.**
Skip this section for Contract-Only and Runtime Wiring WPs.

Confirm all of the following:

- [ ] `G` mutations occur only inside move implementations (framework-authorized)
- [ ] No mutation occurs in helpers or validators
- [ ] Helpers return new values; move assigns results into `G`
- [ ] No mutation occurs outside the boardgame.io move context

Any violation is a **blocking pre-flight failure**.

---

### Risk & Ambiguity Review (Resolve Now, Lock for Execution)

List risks, ambiguities, or edge cases that could cause execution drift.

For each item, include:

- **Risk / ambiguity**
- **Impact** (LOW / MEDIUM / HIGH)
- **Mitigation**
- **Decision / pattern to follow**

**Locking rule:** Any ambiguity resolved here is considered **locked for
execution**. The execution session must not revisit or reinterpret these
decisions. Ambiguities resolved here must not be revisited during execution
or compensated for in the invocation prompt.

Examples of risks often relevant to Behavior WPs:

- Mock context shape for tests (do not modify shared helpers)
- Interface compatibility (ctx structurally assignable to narrower provider)
- Framework function names vs move names (`endTurn` move vs `ctx.events.endTurn()`)
- Immer mutation model and helper assignment pattern
- Helper-vs-move confusion (a function listed as a "helper" may actually
  be a move with validation/gating — verify signatures before execution)
- Contract field drift (WP text uses different names than actual code)
- Missing parameters (function needs data not in its current signature)
- Return value patterns (pure helpers return new values vs void mutators)
- Existing test compatibility (new runtime paths may crash older test mocks
  that lack fields from recent WPs)
- Stage gating gaps (new moves or hooks without `G.currentStage` checks
  allow actions in wrong turn phases — WP-015A discovered this in WP-014A)
- Mutation ordering hazards (removing a card from its source zone before
  confirming the destination accepts it causes silent data loss — WP-015A
  card-drop fix)
- Move classification ambiguity (WP says "add a move" but doesn't specify
  whether it expands `CoreMoveName` or uses domain-move internal gating —
  WP-016 v1 was blocked by this until the WP-014A precedent was recognized)
- Registry data flowing into moves at runtime instead of through setup-time
  resolution into `G` (violates registry boundary — consistently prevented
  by explicit import audits in WP-014A/014B/018)
- Partial mock crashes (test mocks satisfying a narrow interface but missing
  methods from a broader structural interface — WP-014B added runtime
  type guards like `isVillainDeckRegistryReader` to fail gracefully)
- Downstream invalidation (a completed WP's invariants may be violated by
  a later WP that modifies shared state — WP-014A/014B required Mode B
  audit re-validation after WP-015/015A changed routing behavior)
- Future upgrade surface area (will this code need refactoring to add
  the next keyword, rule, or effect? If yes, the WP may be planting a
  landmine — consider whether an extension seam should be part of the
  current design)
- Data provenance ambiguity (behavior depends on data whose source,
  version, or storage location is not clearly identified — if a bug
  appears, which file or database do you inspect?)
- Data source identity confusion (two files with similar names but
  incompatible schemas — WP-003 loaded `card-types.json` instead of
  `sets.json` with zero errors thrown)
- TypeScript type too narrow for real data (card fields contain modifier
  strings like `"2+"` that match Zod schemas but fail TypeScript
  interfaces — WP-003 star-cost defect)
- Registry schema gaps blocking WP scope (henchmen have no individual
  card entries, scheme twists have no ext_ids, deck counts depend on
  rules not config — WP-014B had to invent virtual card conventions
  because the registry couldn't be refactored mid-phase)
- WP scope contradicting ARCHITECTURE.md (WP-005B specified flat global
  piles but architecture required nested `G.piles` — D-1212 forced
  correction after the conflict was discovered)
- Framework API limitations requiring workarounds (boardgame.io `setup()`
  doesn't accept registry parameter — D-1213 invented module-level
  holder pattern as known tech debt)
- Types defined inline creating consolidation debt (WP-005B defined
  `PlayerZones` inline, WP-006A had to move it — D-1216)
- Test helper state pollution (shared module-level state in `makeMockCtx`
  can leak between tests without reset hooks — caught in WP-005B
  revision pass)
- Modifying immutable files without authorization (files locked by prior
  WPs like `schema.ts` can be changed only with WP-level decision — not
  casual "while I'm here" fixes)
- Numeric foreign keys for cross-service identifiers (couples to seeding
  order, breaks on re-seeds, makes all saved configs invalid — D-1201)
- Re-deriving settled decisions from chat history instead of reading
  authoritative documents (WP-001 discovered 15+ prior sessions had
  drifted; the coordination system was built to prevent this)
- Non-idempotent migrations (DDL without `IF NOT EXISTS` or inserts
  without `ON CONFLICT` fail on re-run — FP-02 established idempotency
  as mandatory)
- Cross-set slug collisions (same slug in multiple sets causes silent
  PostgreSQL upsert overwrites — FP-00.5 Phase 4 audits for this)
- Treating R2 validation warnings as errors (warnings document known
  data quirks and do not block; only errors block — conflating them
  halts development unnecessarily)
- Schema-engine drift (JSON/Zod schema field names, types, or constraints
  diverge from TypeScript contract — Phase 3 exit gate discovered 5 field
  mismatches in Match Setup schema that accumulated silently for weeks)
- Handler ownership ambiguity (data-only contract defined but WP doesn't
  specify where handler functions live — WP-009B had to invent the
  ImplementationMap pattern because WP-009A didn't specify)
- Persistence classification gaps (new G fields added without documenting
  which persistence class they belong to — WP-013 had to discover that
  hookRegistry is setup-only and outcome is derived, not stored)
- Phase transition ordering (observability flags set after transition
  instead of before — causes multiplayer race conditions, D-1238)
- Simultaneous condition ambiguity (both loss and victory conditions met
  in same turn — must be resolved by rulebook authority with explicit
  evaluation order, not inferred, D-1235)
- Locked value string literal drift (WP locked values table contains
  condition types, keyword names, or discriminant strings that don't
  match the actual strings produced by setup code — WP-023 used
  `requiresColor` but code produces `heroClassMatch`, D-2304.
  Evaluators built against WP names silently fail to match runtime data)
- Runtime data availability gap (WP assumes data is in G for condition
  evaluation or dispatch, but the field either doesn't exist or doesn't
  contain the needed subfields — WP-023 assumed team/class data in
  `G.cardStats` but `CardStatEntry` only has stat fields, D-2302.
  Mitigation: narrow scope with safe-skip placeholders for blocked
  evaluators, defer data resolution to a follow-up WP)
- Scope lock vs data infrastructure mismatch (WP's "Files Expected to
  Change" is too narrow to accommodate the data additions needed for
  the WP's stated behavior — WP-023 needed team/class data in G but
  its scope didn't include `types.ts`, `economy.types.ts`, or setup
  code. Resolution options: narrow the WP's behavior or expand its
  file scope with human authorization)
- WP file paths referencing non-existent files (WP scope section lists
  files to create or modify that don't exist in the codebase — WP-024
  referenced `src/setup/buildDefaultHookDefinitions.ts` and
  `src/setup/buildImplementationMap.ts` but the actual code lives in
  `src/rules/ruleRuntime.impl.ts`. The WP was written before the code
  was structured, and the path assumptions were never validated. The
  executing agent would create duplicate files in wrong locations.
  Mitigation: verify all WP file paths against the actual filesystem
  during Dependency Contract Verification and correct the WP before
  execution, D-2405)
- Stub replacement integration test cascade (replacing earlier WP's
  stub handlers with real handlers changes trigger names, message
  strings, and counter deltas that integration tests assert. If
  pre-flight does not identify which tests assert stub-specific
  behavior, the executing agent may be surprised by test failures
  outside the WP allowlist. Mitigation: during pre-flight, read
  integration tests that consume the stub handlers and authorize
  value-only assertion updates under 01.5. WP-024 precedent, D-2404)
- Effect type vocabulary gap (WP handler needs to produce a behavior
  that has no corresponding `RuleEffect` type — e.g., wound card
  movement requires `'gainWound'` but only `modifyCounter`,
  `queueMessage`, `drawCards`, `discardHand` exist. Without
  pre-flight identification, the executing agent may invent a new
  effect type or attempt inline mutations. Mitigation: identify the
  gap during pre-flight, lock the MVP to available effect types with
  counter + message tracking, defer full behavior to a future WP
  that adds the effect type. WP-024 precedent, D-2403)
- Effect type gap with existing inline precedent (WP specifies routing
  behavior through the RuleEffect pipeline, but the needed effect type
  doesn't exist AND the codebase already has inline code doing exactly
  that behavior. Example: WP-025 Ambush "each player gains 1 wound"
  specified `getAmbushEffects(): RuleEffect[]` applied via
  `applyRuleEffects`, but escape wounds in `villainDeck.reveal.ts`
  already call `gainWound` directly inline. The inline pattern is
  the correct resolution — it delivers functional behavior instead of
  counter-only tracking. Mitigation: identify the inline precedent
  during pre-flight, correct the WP to use the inline pattern, and
  document the choice in DECISIONS.md. WP-025 precedent, D-2503)
- Setup-time data source gap (WP assumes registry card data contains
  structured metadata for classification, but the data either doesn't
  exist, exists in a different form, or refers to a different mechanic
  with the same name. Example: WP-025 assumed Patrol/Guard keywords
  exist in card data — Patrol appeared only as a different mechanic
  in one set, Guard had zero occurrences. Only Ambush was extractable
  from ability text. Without pre-flight verification against actual
  card JSON files, `buildCardKeywords` would have silently returned
  wrong results for Patrol or empty results with no explanation.
  Mitigation: read actual data files during pre-flight, identify which
  classifications have data sources and which don't, apply safe-skip
  for missing sources, document in DECISIONS.md. WP-025 precedent,
  D-2504)
- WP function signatures using direct registry types instead of
  established `unknown` convention (WP text specifies
  `registry: CardRegistry` as a parameter type, but the engine
  convention is `registry: unknown` with a local structural interface
  and runtime type guard. The direct type violates the layer boundary
  by requiring an import from `@legendary-arena/registry`. Without
  pre-flight correction, the executing agent imports the registry
  package into engine code. Mitigation: verify all setup-time function
  signatures against the `buildCardStats` pattern during Dependency
  Contract Verification. WP-025 precedent)
- WP gating assumption provably false (WP Assumes section asserts a
  blocking condition — e.g., "structured metadata exists in registry" —
  that is demonstrably untrue when verified against actual data. The WP
  gates itself on a false premise, making execution logically impossible
  without correction. Mitigation: verify each gating assumption during
  pre-flight. If false, convert to safe-skip MVP constraint (builder
  returns `[]`, tests use synthetic data) or defer the WP. Do not
  execute a WP whose own blocking assumptions are false. WP-026
  precedent: "Scheme card data includes setup instruction metadata"
  was false — resolved by converting to safe-skip with `// why:`
  comments and D-2601 documentation)
- Decision ID coupling in governance artifacts (EC/WP references a
  DECISIONS.md entry by a specific numeric ID that hasn't been created
  yet. The executing agent cannot verify EC compliance for a decision
  that doesn't exist. Mitigation: use title-based references with ID
  as navigational aid, or create the decision entry during pre-flight
  before execution begins. WP-026 precedent: EC-026 referenced D-0603
  which did not exist — resolved by creating D-2601 and adopting
  title-first referencing pattern)
- New file placement violating code category boundaries (WP places a
  setup-time builder in a domain-specific directory instead of the
  `src/setup/` directory required by code category rules. Without
  pre-flight correction, the executing agent creates files in wrong
  locations, establishing a directory precedent that drifts from the
  category model. Mitigation: verify all new file paths against
  `02-CODE-CATEGORIES.md` during Dependency Contract Verification.
  WP-026 precedent: `schemeSetup.build.ts` specified in `src/scheme/`
  but setup builders belong in `src/setup/` — corrected to
  `src/setup/buildSchemeSetupInstructions.ts`, D-2603)
- Type constraint preventing functional implementation (WP defines
  an instruction type or handler that cannot be functionally
  implemented because a prior contract constrains the underlying type.
  Example: `modifyCitySize` is in the `SchemeSetupType` union but
  `CityZone` is a fixed 5-tuple from WP-015. The handler exists in
  the executor but can only warn + no-op until a future WP converts
  the type. Mitigation: identify the constraint during pre-flight,
  lock the MVP to warn + no-op with a DECISIONS.md entry, and ensure
  tests assert the warning and unchanged state. WP-026 precedent,
  D-2602)
- Non-exported helper functions (WP references a function in its spec
  that is local/non-exported in its module — the executing agent cannot
  import it. Without pre-flight verification, the agent either creates
  a duplicate implementation or fails to compile. Mitigation: verify
  exportability during Dependency Contract Verification by reading the
  actual source file. If not exported, document an alternative approach
  — call the underlying pure helper directly or reconstruct equivalent
  behavior. WP-027 precedent: `advanceStage` local to `game.ts`,
  resolved via `advanceTurnStage` from `turnLoop.ts`, D-2705)
- Infrastructure capability assumptions (WP assumes mock capabilities
  — seed-driven randomness, framework context shaping, specific test
  helper behavior — that don't exist in current test infrastructure.
  Without pre-flight identification, the executing agent discovers the
  gap mid-implementation and may invent ad-hoc workarounds. Mitigation:
  verify `makeMockCtx` and test helpers actually support the assumed
  capability. If not, apply safe-skip MVP (store the capability
  contract field for future use) or expand test infrastructure with
  explicit authorization. WP-027 precedent: seed fidelity deferred;
  `seed` field stored but not used by mock shuffle, D-2704)
- Companion type scope expansion (WP introduces a return type or helper
  type not listed in EC locked values but naturally required by a
  specified function — e.g., the return type of `verifyDeterminism` is
  a structured result, not a primitive. Without pre-flight
  authorization, the executing agent may invent types or use inline
  object literals. Mitigation: authorize companion types during
  pre-flight if they are a direct consequence of a locked function
  signature. If the type is independently useful beyond the WP's
  immediate scope, it should be added to the EC locked values before
  execution. WP-027 precedent: `DeterminismResult` was the return
  type of `verifyDeterminism` but not in EC locked values)
- Projection aliasing (WP produces a derived view by reading `G` fields
  and returning objects. Without explicit copy discipline, returned
  arrays or objects may be references to mutable `G` data, allowing
  consumers to mutate `G` through the projection. Standard tests
  including `JSON.stringify` equality checks compare values, not
  references, so they cannot detect aliasing. Mitigation: during
  pre-flight, identify all `G` fields that will be read and returned
  in projections. Lock the requirement that mutable sources must be
  spread-copied. During post-mortem, trace every returned value to its
  source. WP-028 precedent: `cardKeywords` array in city projection
  returned a direct reference to `G.cardKeywords[space]`, fixed with
  spread operator during post-mortem)
- Framework ctx subset needed without boardgame.io import (WP introduces
  a function that reads `phase`, `turn`, or `currentPlayer` from ctx
  but must not import `boardgame.io`. Without pre-flight resolution,
  the executing agent either imports boardgame.io's `Ctx` type or
  invents an ad-hoc interface. Mitigation: define a local structural
  interface with `readonly` fields and no optional members during
  pre-flight. Lock the exact shape. WP-028 precedent: `UIBuildContext`
  resolved during pre-flight, locked as exact-shape-no-widening)
- Lifecycle wiring creep (WP introduces a new function that is not part
  of the boardgame.io lifecycle, but the executing agent "helpfully"
  wires it into `game.ts`, a move, or a phase hook. Mitigation: the
  session prompt must include an explicit lifecycle prohibition listing
  where the function MUST NOT be called from and where it IS consumed.
  WP-028 precedent: `buildUIState` lifecycle prohibition added to
  session prompt, EC, and WP)
- Decision ID encoding mismatch (WP/EC references a decision by ID
  using regular hyphens, but DECISIONS.md uses em-dashes in headings.
  grep for `D-0301` misses `D‑0301`, producing a false "not found."
  Mitigation: always search by title keyword as a fallback, not just
  the ID string. WP-028 precedent: D-0301 and D-0302 incorrectly
  reported as missing during pre-flight)
- Filter input type missing required data (WP introduces a filter that
  operates on a derived type like UIState, but the type lacks fields
  the filter needs to expose. The WP/EC file lists omit the upstream
  producer. Without pre-flight detection, the executing agent discovers
  mid-implementation that the filter cannot provide the data specified
  in the visibility matrix. Mitigation: during pre-flight, read the
  actual input type and compare every "visible" entry in the
  information visibility matrix against the type's fields. If data is
  missing, add the upstream producer to scope under 01.5 wiring
  allowance. WP-029 precedent: `UIPlayerState` had no `handCards` field;
  `buildUIState` needed a one-line addition to populate it)
- `exactOptionalPropertyTypes` strictness (TypeScript project config
  enables `exactOptionalPropertyTypes`, which rejects
  `T[] | undefined` for optional properties typed as `field?: T[]`.
  Inline ternaries that return `undefined` on one branch fail type
  checking. Mitigation: use conditional assignment — build the base
  object without the optional field, then assign in an `if` block.
  WP-029 precedent: `preserveHandCards` required this pattern)
- Pre-flight refinement back-sync gap (pre-flight locks a parameter
  shape, type signature, or API refinement that differs from the WP
  text as originally written, and the session prompt correctly
  implements the refinement — but nobody updates the WP text itself
  to match. The code and tests ship correctly; pre-flight, session
  prompt, WORK_INDEX, STATUS, and post-mortem all reference the new
  shape; but the WP spec file retains the old shape. A future reader
  of the WP alone sees a signature mismatch between "spec" and
  "code." Mitigation: during pre-flight, when a refinement is locked
  that differs from the WP text, explicitly note in the Authorized
  Next Step section that the WP text must be back-synced. The pre-
  commit review catches this as a non-blocking nit; a proactive
  pre-flight note eliminates the drift before execution starts.
  WP-030 precedent: `evaluateScenarioOutcome` signature locked as
  split `victoryConditions`/`failureConditions` arrays instead of
  single `conditions: ScenarioOutcomeCondition[]`. Implementation
  followed the lock; WP-030 line 196 retained the old shape until
  the pre-commit review caught it as Nit 1. Fix was a one-line edit
  with no code or test impact)
- External reviewer findings against stale artifact snapshots
  (external reviewer produces findings against a cached or snap-
  shotted view of repo artifacts — typically via a Sharepoint,
  archive, or outdated clone — and the findings include claims that
  are false against the actual repo state. Common failure modes:
  "decision ID X is missing" when it exists under an em-dash heading
  (related to P6-2 but distinct — that was a grep encoding issue in
  a local session; this is reviewer viewing stale content), "file Y
  has orphaned markdown marker" when it's been fixed already,
  "WORK_INDEX marks WP as Needs review" when the convention is
  `[ ] ... ✅ Reviewed` meaning pending execution but reviewed.
  Mitigation: treat every external-reviewer finding as untrusted
  until verified against actual repo state via direct tool inspection.
  Reject false findings explicitly in a disposition log. Accept valid
  findings with citations. Do not apply fixes based on stale claims.
  WP-030 precedent: external review made four factually incorrect
  claims (D-0603 missing, WP-030 "Needs review", redundant grep step,
  non-house-style D-3001 stub) and two valid ones (trailing backticks
  in DECISIONS.md, `advanceCampaignState` outcome type tightening).
  Disposition log in pre-flight §External Review — Disposition Log
  recorded each verdict with evidence)
- Mystery untracked file appearance during session (an untracked
  file appears in `git status` that was not present at session start
  and was not created by the executing agent. Common causes: a
  parallel terminal created it, a background agent ran, the user
  added it while the session was working, or an IDE sync occurred.
  Mitigation: flag in execution summary and post-mortem, but do not
  touch it — it is outside WP scope. Exclude from commit staging
  explicitly. Do not attempt to investigate its provenance during
  the WP session. WP-030 precedent:
  `docs/ai/REFERENCE/01.5-copilot-check.md` appeared mid-session
  (later renamed to `01.7-copilot-check.md` to avoid a slot collision
  with `01.5-runtime-wiring-allowance.md`); flagged in execution
  summary, post-mortem, and pre-commit review; correctly excluded from
  both WP-030 commits)
- Contract-Only WP creates new directory without code-category
  classification (WP introduces files in a new
  `packages/game-engine/src/<dir>/` that has no entry in
  `02-CODE-CATEGORIES.md` and no DECISIONS.md classification. Prior
  precedents: D-2706 for `src/replay/` (WP-027), D-2801 for
  `src/ui/` (WP-028). Without pre-flight correction, the executing
  agent creates files in an unclassified directory, establishing
  a directory precedent that drifts from the category model.
  Mitigation: during pre-flight §Code Category Boundary Check,
  verify that every new directory has a classification decision.
  If absent, create as a pre-session action (PS-#) before execution
  starts, following the D-2706 / D-2801 template. WP-030 precedent:
  PS-1 resolved by creating D-3001 for `src/campaign/`, matching
  the prior two decisions in structure and status. WP-031 precedent:
  PS-1 resolved by creating D-3101 for `src/invariants/` — the
  fourth instance of the same pattern, now codified as Established)
- "Scan-G-and-throw" check spec without setup-layer concrete-output
  verification (WP introduces a check function that iterates `G`
  or any of its fields and throws on a specific predicate, but
  pre-flight reasons only at the type system level — e.g., "scan
  zones for duplicate CardExtIds" — without verifying the
  predicate against what the upstream builders actually emit.
  Type-level reasoning treats `CardExtId = string` as "any string
  is unique"; runtime reality is that fungible token strings are
  reused (8 copies of `'starting-shield-agent'` per player deck,
  30 copies of `'pile-bystander'` per supply pile). The check
  would throw on every valid G, regressing the existing test
  baseline at the moment the wiring fires for the first time.
  Mitigation: during pre-flight Dependency Contract Verification,
  for any "scan-and-throw" invariant, trace the upstream builder
  output: read `buildInitialGameState`, `buildVillainDeck`,
  `buildMastermindState`, and any other relevant builder; identify
  the shape of the values they actually emit; confirm the check
  predicate classifies them correctly. If the predicate would
  false-positive, narrow it during pre-flight via a new RS entry
  + DECISIONS.md decision before the session prompt is generated.
  WP-031 precedent: original `checkNoCardInMultipleZones` would
  have thrown on `'pile-bystander'` 30 times in a row. Discovered
  at implementation time, not pre-flight time. Resolved as
  mid-execution PS-3 + Amendment A-031-01 + D-3103. A pre-flight
  trace would have caught it before the session prompt was
  written.)
- WP draft text references field names that don't exist in the
  canonical type files (WP §C/§D/§K spec lists field names like
  `victoryPile` for a member of `PlayerZones`, but the canonical
  type at `packages/game-engine/src/state/zones.types.ts` declares
  `victory: Zone` instead. The mismatch is a drafting typo that
  type-checks at the WP-text level (because WP text isn't compiled)
  but breaks the implementation as soon as the executor reads
  `G.playerZones[id].victoryPile` and gets `undefined`. Mitigation:
  during pre-flight Dependency Contract Verification, every field
  name referenced in the WP text must be cross-checked against
  the canonical type file. Add a verification item: "for each
  field name in the WP text, confirm it exists at the cited
  line:offset in the canonical type." WP-031 precedent: §C item 2
  and §D item 1 listed `victoryPile` in three places; the
  canonical type uses `victory`. Resolved as mid-execution
  Amendment A-031-02. Pre-flight Dependency Contract Verification
  would have caught the typo if it had cross-checked every field
  name against the source.)
- Runtime-undefined values from mock contexts vs compile-time
  framework types (WP introduces a check or builder that reads a
  framework lifecycle field — e.g., `boardgame.io`'s `Ctx.phase`
  or `Ctx.turn` — and the framework declares the field as
  non-null `string` / `number`. The compile-time type is
  satisfied at the wiring site, but the engine's existing test
  helpers — `makeMockCtx` and similar — do not set the field, so
  the runtime value flows through as `undefined` despite the
  compile-time non-null declaration. The check function reads the
  undefined value and either throws on a string operation or
  produces a confusing error message. Mitigation: during pre-flight
  Runtime Readiness Check, identify any framework lifecycle field
  the WP reads and verify whether mock contexts in the test path
  set it. If they don't, the check function's parameter signature
  must be widened to `field: string | undefined` (not the
  framework type) and short-circuit on undefined with a
  `// why:` comment. Do NOT modify the mock helpers, and do NOT
  widen the framework-context interface. WP-031 precedent:
  Amendment A-031-03 widened `checkValidPhase` /
  `checkTurnCounterMonotonic` parameter types after the executor
  encountered the `makeMockCtx` cast that bypasses the
  framework's non-null declaration. Discovered during
  implementation; should have been caught in pre-flight Runtime
  Readiness.)
- Verification grep patterns matching comments and JSDoc instead
  of imports (pre-flight Verification Steps include grep gates
  like `git grep -l "boardgame.io" -- "<dir>/"` that expect "no
  output" to confirm forbidden imports are absent. The pattern
  uses `.` which is regex-special and matches "any character," so
  the gate will match comments, JSDoc, and rationale text that
  legitimately mention the forbidden module name in documentation.
  The executor must then either rephrase every comment to weaken
  the docs, or escape the dot in the pattern, or refine the
  pattern to match actual import statements only — none of which
  are obvious from the grep output. Mitigation: pre-flight
  Verification Steps must use import-line-specific patterns for
  binary gates: `from ['"]boardgame\.io` instead of bare
  `boardgame.io`. Comments mentioning the forbidden name remain
  permitted because they explain the rationale for the
  prohibition. WP-031 precedent: the executor's first grep run
  flagged 6 files because comments mentioned `boardgame.io` for
  documentation purposes. Workaround was to rephrase comments to
  "the game framework," weakening the docs. The correct fix is
  to update the verification step's pattern to escape the dot.)

- Canonical array incomplete for validation scope (WP validates
  against a broader set than the named constant provides. Example:
  `CORE_MOVE_NAMES` contains 3 of 10 registered moves. A validator
  using `CORE_MOVE_NAMES.includes(moveName)` would reject 7 valid
  moves as `INVALID_MOVE`. Without pre-flight verification of the
  canonical array's membership against the actual runtime set,
  the executor builds a validator that rejects valid input.
  Mitigation: during Dependency Contract Verification, verify the
  actual membership of any canonical array the WP uses for
  validation. If the array is incomplete for the validation scope,
  inject the valid set as a parameter instead of importing the
  incomplete constant. WP-032 precedent: `CORE_MOVE_NAMES` has
  `drawCards`, `playCard`, `endTurn` — the other 7 moves
  (`advanceStage`, `revealVillainCard`, `fightVillain`,
  `recruitHero`, `fightMastermind`, `setPlayerReady`,
  `startMatchIfReady`) are domain moves not in any canonical
  array. Resolved by adding `validMoveNames: readonly string[]`
  parameter. Pre-flight RS-2.)

If risks exist, recommend **mitigation or clarification** before execution.

---

### Pre-Flight Verdict (Binary)

Choose **exactly one**:

- READY TO EXECUTE — WP-XXX is properly sequenced, scoped, and ready
  for execution.
- DO NOT EXECUTE YET — WP-XXX has blocking issues that must be
  resolved first.

Provide 3-6 sentences of justification grounded in:

- dependency readiness
- contract fidelity (names, signatures, field paths verified)
- scope lock clarity
- risks resolved
- architectural boundary confidence
- maintainability (extension seams, patch locality, fail-safe behavior)

If the verdict is READY TO EXECUTE but mandatory WP/EC updates are
required first, list them explicitly. The verdict may be conditional on
completing those updates. Once updates are applied, they should be logged
in the "Authorized Next Step" section — no re-run of pre-flight is needed
unless the updates change scope or introduce new architectural concerns.

---

### Invocation Prompt Conformance Check (Pre-Generation)

This check applies **before** a session execution prompt is generated.
It runs only if the verdict is **READY TO EXECUTE**.

Confirm:

- [ ] All EC locked values are copied verbatim into the invocation prompt
- [ ] No new keywords, helpers, file paths, or timing rules appear only
  in the prompt
- [ ] File paths, extensions, and test locations match the WP exactly
- [ ] No forbidden imports or behaviors are introduced by wording changes
- [ ] The prompt does not resolve ambiguities that were not resolved in
  this pre-flight
- [ ] Contract names and field names in the prompt match the verified
  dependency code (not just the WP text)
- [ ] Helper call patterns in the prompt reflect actual signatures
  (parameter order, return types, pure vs mutating)

**Rule:**
The invocation prompt is strictly a transcription + ordering artifact.
If it requires interpretation, pre-flight is incomplete.

---

### Authorized Next Step

If and only if the verdict is **READY TO EXECUTE**:

> You are authorized to generate a **session execution prompt** for WP-XXX
> to be saved as:
> `docs/ai/invocations/session-wpxxx-<short-name>.md`

**Guard:** The session prompt **must conform exactly** to the scope, constraints,
and decisions locked by this pre-flight. No new scope may be introduced.

If the verdict was conditional on WP/EC updates, log their resolution here:

```
**Pre-session actions — ALL RESOLVED (YYYY-MM-DD):**

1. <description of update and what it resolved>
2. <description of update and what it resolved>

All mandatory pre-session actions are complete. No re-run of pre-flight
required — these updates resolve risks identified by this pre-flight
without changing scope.
```

If the verdict is **DO NOT EXECUTE YET**, stop. Do not generate a session
prompt.

---

### Final Instruction

Pre-flight exists to **prevent premature execution and scope drift**.

If there is uncertainty, missing context, or unresolved ambiguity:

**DO NOT PROCEED TO EXECUTION.**

Escalate, clarify, or split the WP instead.

---

## Lessons Learned Template (Step 7)

After the commit (step 6), capture lessons from the WP execution that
should inform future work. This step runs in the same session as the
commit or in a new session with access to the completed WP artifacts.

### Purpose

Lessons learned exist to:

- Propagate new patterns or anti-patterns into the pre-flight template
- Update governance documents with newly discovered enforcement needs
- Record execution friction that should be smoothed for future WPs
- Ensure institutional knowledge survives session boundaries

### Template

```markdown
## Lessons Learned — WP-XXX (<short title>)

**Date:** YYYY-MM-DD

### New Patterns Discovered

<!-- Patterns that worked well and should become locked precedents -->
<!-- Format: what the pattern is, why it matters, which future WPs benefit -->

1. ...

### Anti-Patterns Discovered

<!-- Things that went wrong or almost went wrong -->
<!-- Format: what happened, why, how it was caught, what prevents recurrence -->

1. ...

### Pre-Flight Template Updates

<!-- Specific additions to 01.4 sections triggered by this WP -->
<!-- e.g., new Dependency Contract Verification check, new Risk example -->

- [ ] Added to Established Patterns: ...
- [ ] Added to Risk examples: ...
- [ ] Added to Dependency Contract Verification: ...
- [ ] No template updates needed

### Governance Document Updates

<!-- Updates to REFERENCE docs, .claude/rules/, or EC-TEMPLATE -->

- [ ] Updated: ...
- [ ] No governance updates needed

### Post-Mortem Findings (from step 4)

<!-- Summarize any fixes applied during post-mortem -->
<!-- These become precedent entries in 01.6 -->

- ...
```

### What to Update

Based on lessons learned, update any combination of:

- `01.4` Established Patterns section — new locked precedents
- `01.4` Risk & Ambiguity examples — new risk categories
- `01.4` Dependency Contract Verification checks — new verification items
- `01.4` Precedent Log — new phase/WP-level entries
- `01.6` Precedent Log — post-mortem findings
- `.claude/rules/*.md` — new enforcement rules
- `docs/ai/REFERENCE/02-CODE-CATEGORIES.md` — new directory classifications
- `EC-TEMPLATE.md` — new checklist patterns

**Rule:** Lessons learned are not optional documentation. They are the
mechanism by which the governance system improves. A WP that discovers a
new failure mode but does not propagate it into the templates is leaving
a trap for future sessions.

---

## Session Context Template (Step 8)

After lessons learned (step 7), produce a session context file for the
**next** WP in the dependency chain. This file bridges session boundaries
so the next session starts with full context.

### Purpose

Session context files exist because:

- Each Claude Code session starts fresh with no memory of prior sessions
- Re-deriving project state from git history and docs is slow and
  error-prone
- The prior session has the most accurate understanding of what just
  changed and what matters next

### Location

Save as: `docs/ai/session-context/session-context-wpXXX.md`

Where `XXX` is the **next** WP that will consume this context (not the WP
that just completed). For example, after completing WP-028, produce
`session-context-wp029.md`. This file becomes step 0 input for the next
WP's session.

### Template

```markdown
WP-NNN is complete (commit <hash>). Key context for WP-XXX:

- <test count> engine tests passing, <suite count> suites
- <summary of moves, phases, or capabilities now available>
- <dependency chain status — what's complete, what's next, what's blocked>
- WP-XXX: <one-line description of the next WP>

<key artifacts produced by the completed WP>:
- <artifact 1> — what it does, where it lives
- <artifact 2> — what it does, where it lives

Setup-time builders / runtime patterns still in effect:
- <pattern 1>
- <pattern 2>

Key architectural patterns still in effect:
- <decision ID> — <one-line summary>
- <decision ID> — <one-line summary>

<relevant decisions for next WP>:
- D-NNNN: <summary>
- D-NNNN: <summary>

<files the next WP will need to read or depend on>:
- <file path> — <what it contains and why it matters>

Steps completed for WP-NNN:
0: <session-context file loaded, or "N/A — first WP in phase">
1: pre-flight (<date>, READY)
2: <session prompt filename> (generated)
3: execution (<date>, <test count> tests, <fail count> fail)
4: post-mortem (<summary of findings/fixes, or "clean — no issues">)
5: pre-commit review (<date>, or "pending">
6: commit (<hash>, or "pending">
7: lessons learned (<summary, or "no template updates needed">)
8: this file

Run pre-flight for WP-XXX next.
```

### Content Guidelines

Include:

- **Test baseline** — exact counts so the next session can verify no
  regressions
- **Dependency chain status** — which WPs are complete, which are next,
  which are blocked and on what
- **Key artifacts** — new files, types, functions, or exports that the
  next WP will consume or depend on
- **Architectural patterns** — conventions, setup patterns, or runtime
  patterns that remain in effect
- **Relevant decisions** — DECISIONS.md entries the next WP should read
- **Implementation anchors** — specific files the next WP will need to
  read before coding
- **Workflow audit trail** — steps 0-8 completion log for the WP that
  just finished (closes the audit loop for the prior WP and lets the
  next session verify the handoff is complete)

Do NOT include:

- Full file contents (the next session will read them)
- Speculative design for the next WP (that belongs in the WP itself)
- Debugging notes or transient issues (those go in lessons learned)

### Precedent

- `docs/ai/session-context/session-context-wp028.md` — produced after WP-027, consumed
  by WP-028 pre-flight. Captured test counts, move list, replay harness
  artifacts, setup conventions, dependency chain, and relevant decisions.

**Rule:** The session context file is a **factual snapshot**, not a
planning document. It describes what is true now, not what should happen
next. The next WP's pre-flight and session prompt handle planning.

---

## Precedent Log

This section documents recurring failure modes discovered during prior
phases. Each entry describes what went wrong, why, and what pre-flight
check now prevents it. Entries are ordered by discovery date.

### Foundation Phase — Environment & Infrastructure (FP-00.4 through FP-02)

**FP-1: Stop-on-failure gate semantics (FP ordering)**
- **What it establishes:** Foundation Prompts run once in strict order
  (00.4 -> 00.5 -> 01 -> 02). If any FP fails, ALL downstream work is
  blocked — not just the next FP, but all Work Packets. This is a
  harder gate than per-WP lint checks.
- **Why this matters for pre-flight:** Pre-flight may assume FPs are
  complete for normal WPs. But if a WP touches infrastructure that an
  FP established (database schema, R2 data, server wiring, environment
  variables), pre-flight should verify the relevant FP's assumptions
  still hold.

**FP-2: R2 validation error vs warning distinction (FP-00.5)**
- **What happened:** R2 validation produces both errors (structural
  failures like missing sets, invalid JSON) and warnings (known data
  quirks like `[object Object]` abilities, cross-set duplicate slugs,
  missing VP on some masterminds). Early in development, warnings were
  sometimes treated as blockers, unnecessarily halting work.
- **Root cause:** No explicit severity distinction in validation output.
- **Fix:** `pnpm validate` exit code 0 = no errors (warnings ok),
  exit code 1 = errors exist. Warnings are informational and documented
  in `03-DATA-PIPELINE.md` Known Data Quality Items.
- **Prevention:** Structural Readiness now distinguishes errors from
  warnings for R2 validation.

**FP-3: Idempotent migration pattern (FP-01/02)**
- **What it establishes:** All database DDL uses
  `CREATE TABLE IF NOT EXISTS`, all inserts use
  `INSERT ... ON CONFLICT DO NOTHING`. Migrations can be re-run safely.
  There is no rollback mechanism — this is intentional simplicity.
  Manual recovery via `psql` if needed.
- **Why this matters for pre-flight:** Any WP that adds migrations must
  follow the same idempotency pattern. Non-idempotent migrations will
  fail on re-deployment.

**FP-4: Environment assumptions validated once (FP-00.4)**
- **What it establishes:** 9 required environment variables, Node v22+,
  boardgame.io `^0.50.0`, rclone configured with R2 remote, database
  seeded. These are checked once by FP-00.4 and assumed valid for all
  subsequent work.
- **Why this matters for pre-flight:** WPs that depend on specific
  environment capabilities (database access, R2 connectivity, specific
  Node APIs) can assume FP-00.4 has validated them. But if a WP adds
  new environment requirements (new env vars, new external services),
  the FP-00.4 check should be updated — note this as a post-execution
  task.

---

### Phase 0 — Coordination & Game Skeleton (WP-001 through WP-004)

**P0-1: Prior sessions drifted without coordination system (WP-001)**
- **What happened:** 15+ prior Claude Code sessions had re-derived
  settled decisions from chat history, producing conflicting field names
  (`strikeCount` vs `vp`), stale table names (`expansions` vs
  `legendary.sets`), wrong migration paths, and inconsistent image URL
  patterns.
- **Root cause:** No single source of truth existed. Each session started
  fresh and guessed at conventions.
- **Fix:** WP-001 created the full `docs/ai/REFERENCE/` coordination
  system with override hierarchy, WP template, and session protocol.
  All REFERENCE documents were audited and corrected.
- **Prevention:** The override hierarchy (00.1 > ARCHITECTURE.md > WPs >
  conversation) is now the foundational governance pattern. Pre-flight
  reads all authority documents before making a verdict.

**P0-2: ext_id vs numeric FK decision cascaded everywhere (D-1201, D-1202)**
- **What happened:** The database schema initially used `bigint` foreign
  keys for card references. This would couple match records to seeding
  order — a re-seed would invalidate all saved configurations and break
  replay targeting.
- **Root cause:** Standard relational database practice (numeric FKs) is
  wrong for a system where the seed data can be regenerated.
- **Fix:** D-1201 and D-1202 locked `ext_id` text strings for all
  cross-service identifiers. `MatchSetupConfig` uses string IDs, not
  numeric ones. This cascaded into zone contents (CardExtId), virtual
  card ext_ids, and all downstream WPs.
- **Prevention:** Dependency Contract Verification now checks that
  cross-service identifiers use ext_id strings.

**P0-3: boardgame.io CJS-only server bundle (D-1206)**
- **What happened:** `boardgame.io/server` only ships CommonJS. The
  project is ESM-only (Node v22+). Direct `import` failed at runtime
  with no helpful error message.
- **Root cause:** boardgame.io's package exports don't include ESM for
  the server subpackage. This was not discoverable until WP-004 tried
  to wire the server.
- **Fix:** D-1206 introduced `createRequire(import.meta.url)` bridge.
  Documented as known tech debt with `// why:` comment.
- **Prevention:** Established Patterns documents framework workarounds
  as visible tech debt. Runtime Readiness checks framework API
  compatibility.

**P0-4: Immutable file pattern established (WP-003)**
- **What happened:** After fixing the two registry defects, WP-003
  designated `schema.ts`, `shared.ts`, and `localRegistry.ts` as
  immutable — confirmed correct and locked against casual modification.
- **Root cause:** Registry code is the data input foundation. Incorrect
  changes here cause silent failures that are extremely hard to trace
  (zero sets loaded with no error).
- **Fix:** Immutable file designations recorded in
  `.claude/skills/legendary-registry/SKILL.md`. Future changes require WP-level
  authorization and DECISIONS.md entry.
- **Prevention:** Dependency Contract Verification now checks immutable
  file designations.

**P0-5: End-to-end proof before feature work (WP-004)**
- **What happened:** WP-004 was a pure wiring packet — no game logic,
  no new features. It loaded the registry, wired `LegendaryGame` into
  `boardgame.io Server()`, handled the CJS bridge, and proved the
  architecture works end-to-end with real packages.
- **Why this mattered:** If the wiring had failed (wrong API shape,
  incompatible versions, missing exports), every Phase 1+ WP would
  have been blocked. Proving the architecture before building features
  prevented cascading failures.
- **Prevention:** Established Patterns documents that new architectural
  layers should be proven with a wiring WP before feature WPs build on
  them.

---

### Phase 1 — Foundation Contracts & Setup (WP-005A through WP-006B)

**P1-1: WP scope contradicted ARCHITECTURE.md (D-1212)**
- **What happened:** WP-005B's scope section specified global piles
  (`bystanders`, `wounds`, `officers`, `sidekicks`) as flat top-level
  keys on `G`. But ARCHITECTURE.md required a nested `G.piles` container.
  The contradiction was not caught until implementation was outlined.
- **Root cause:** The WP scope section was drafted without pre-validating
  against ARCHITECTURE.md. The authority hierarchy (ARCHITECTURE.md >
  Work Packets) was not consulted before scope was finalized.
- **Fix:** D-1212 documented that the nested structure is correct and
  established the override principle: architecture always wins.
- **Prevention:** Dependency Contract Verification now includes WP scope
  pre-validation against ARCHITECTURE.md.

**P1-2: Framework API workaround required (D-1213)**
- **What happened:** boardgame.io's `setup()` function signature only
  accepts `(context, setupData)` — it has no way to pass the registry.
  The engine needed registry data at setup time but the framework API
  didn't support it.
- **Root cause:** Framework constraints not discovered until
  implementation. The `setup()` API was not inspected during WP design.
- **Fix:** D-1213 invented `setRegistryForSetup(registry)` — a
  module-level registry holder in `game.ts`. Documented as known tech
  debt, explicitly flagged for future factory pattern replacement.
- **Prevention:** Runtime Readiness now checks framework API
  compatibility. Established Patterns documents that framework
  workarounds must be visible tech debt with DECISIONS.md entries.

**P1-3: Inline type definitions created consolidation debt (D-1216)**
- **What happened:** WP-005B defined `PlayerZones`, `GlobalPiles`, and
  `CardExtId` inline in `types.ts`. When WP-006A needed zone contracts,
  these types already existed in two places, creating duplication risk.
  WP-006A had to retroactively consolidate them into
  `src/state/zones.types.ts` with re-exports for backward compatibility.
- **Root cause:** Types were defined inline during implementation for
  convenience, without considering that future WPs would need them as
  standalone contracts.
- **Fix:** D-1216. Types moved to dedicated `*.types.ts` file. Re-exports
  preserved backward compatibility.
- **Prevention:** Dependency Contract Verification now requires new types
  consumed by multiple files to be defined in dedicated contract files.
  Established Patterns documents this as project norm.

**P1-4: Test helper state pollution risk (WP-005B revision)**
- **What happened:** `makeMockCtx` used a module-level registry holder
  that could leak state between tests if not explicitly reset.
- **Root cause:** Shared test helpers with module-level state were
  designed without considering cross-test isolation.
- **Fix:** Added `clearRegistryForSetup()` test-only reset hook during
  the same WP's revision pass.
- **Prevention:** Established Patterns now documents that test helpers
  must include state isolation mechanisms.

**P1-5: A/B packet pattern emerged as project norm**
- **What happened:** Phase 1 organically split every major feature into
  A packets (contracts/types) and B packets (implementation). This was
  not planned — it emerged because contracts needed to be stable before
  logic could safely build on them.
- **Root cause:** Without the split, implementation would lock API
  surfaces prematurely, and subsequent WPs couldn't depend on stable
  types.
- **Fix:** Adopted as project norm. All future phases follow A/B
  packet pattern for features that define new contracts.
- **Prevention:** Established Patterns documents this explicitly.

---

### Phase 2 — Core Turn Engine (WP-003 through WP-008B)

**P2-1: Silent data source confusion (WP-003 Defect 1)**
- **What happened:** `httpRegistry.ts` fetched `card-types.json` (card
  type taxonomy) instead of `sets.json` (set index). Zod validation
  silently succeeded because `card-types.json` entries are valid objects —
  but they lack `abbr` and `releaseDate` fields. Result: zero sets loaded
  with no error. Every downstream match creation call would fail.
- **Root cause:** Two registry files with similar names (`sets.json`,
  `card-types.json`) but incompatible schemas. Generic Zod parsing
  accepted both without distinguishing them.
- **Fix:** D-1203. Added explicit `// why:` comment explaining the
  distinction. Loader validates data source identity, not just shape.
- **Prevention:** Dependency Contract Verification now includes data
  source identity checks. Risk examples include data source confusion.

**P2-2: TypeScript type too narrow for real card data (WP-003 Defect 2)**
- **What happened:** `FlatCard.cost` was typed as `number | undefined`
  but real card data includes star-cost strings like `"2*"` (Wasp) and
  `"3*"`. The Zod schema (`HeroCardSchema.cost`) already accepted both
  `number` and `string`, but the TypeScript interface rejected valid data
  silently during registry load.
- **Root cause:** TypeScript interfaces were defined from the "ideal"
  data model, not the actual data. Real Legendary card data has modifier
  strings, nulls, and formatting quirks that the Zod schema accommodates
  but narrow types do not.
- **Fix:** D-1204. Widened `FlatCard.cost` to `string | number | undefined`.
- **Prevention:** Dependency Contract Verification now checks that
  TypeScript types accommodate real data quirks. Risk examples include
  type narrowing vs real data.

**P2-3: Registry schema gaps forced WP split (WP-014A/B)**
- **What happened:** WP-014 was originally one packet covering the full
  villain deck (reveal pipeline + deck composition). During pre-flight,
  4 critical schema gaps were discovered: henchmen have no individual
  card entries, scheme twists have no ext_ids, deck composition counts
  depend on rules not config, and mastermind strike identification was
  implicit. These gaps couldn't be resolved without design decisions.
- **Root cause:** Registry schema was designed for card display (registry
  viewer), not for gameplay engine consumption. Gameplay needs (individual
  card tracking, deterministic instancing) exceed the display schema.
- **Fix:** D-1410 through D-1413. Split into WP-014A (reveal pipeline,
  independent of deck construction) and WP-014B (deck composition with
  virtual card conventions). Virtual cards generated at setup time with
  locked ext_id formats.
- **Prevention:** Established Patterns now documents virtual card
  conventions and WP splitting when registry gaps block implementation.

**P2-4: Fail-closed vs silent no-op confusion (cross-WP pattern)**
- **What happened:** Early WPs inconsistently handled error cases —
  some threw, some returned silently, some logged warnings. The
  distinction between "structural invariant violation" (something is
  wrong with the game setup) and "unmet gameplay condition" (player
  doesn't have enough attack) was not formalized.
- **Root cause:** No explicit failure mode taxonomy. Each WP invented
  its own error handling strategy.
- **Fix:** Formalized across Phase 2: structural violations produce
  fail-closed behavior with `G.messages` entry. Unmet gameplay conditions
  produce silent no-op (return void). Moves never throw.
  `Game.setup()` is the only context that may throw.
- **Prevention:** Established Patterns now documents the fail-closed vs
  silent no-op distinction with specific examples.

---

### Phase 3 — MVP Multiplayer Infrastructure (WP-009A through WP-013)

**P3-1: Schema-engine drift in Match Setup (Phase 3 exit gate)**
- **What happened:** The Match Setup JSON schema used wrong field name
  `heroIds` instead of `heroDeckIds`, was missing 5 count fields, and
  had `playerCount` max of 10 vs engine's `maxPlayers = 5`. Drift
  accumulated silently from WP-005A through all of Phase 3.
- **Root cause:** The JSON schema and the TypeScript `MatchSetupConfig`
  contract were derived from the same requirements document but never
  cross-validated. No executable test compared schema constraints against
  type constraints.
- **Fix:** D-1244 through D-1248 locked composition block to 9 fields
  matching `MatchSetupConfig` exactly, enforced `additionalProperties:
  false`, and corrected `playerCount` max.
- **Prevention:** Dependency Contract Verification now includes
  schema-engine alignment. Established Patterns now documents
  fail-closed configuration with `additionalProperties: false`.

**P3-2: Handler ownership ambiguity (WP-009A/009B boundary)**
- **What happened:** WP-009A defined `HookDefinition` as data-only but
  did not specify where handler functions should live. WP-009B had to
  invent the `ImplementationMap` pattern during implementation.
- **Root cause:** The contract WP (009A) focused on the data shape but
  not the runtime architecture. Handler location is an architectural
  concern that should have been resolved before the contract was locked.
- **Fix:** D-1232 locked handlers in `ImplementationMap` outside `G`.
  D-1233 established the two-step execute/apply pipeline.
  ARCHITECTURE.md Section 4 documents both.
- **Prevention:** Dependency Contract Verification now requires explicit
  handler ownership documentation for data-only contracts. Established
  Patterns documents the two-step pipeline and data/handler separation.

**P3-3: Persistence field classification discovered during implementation (WP-013)**
- **What happened:** WP-013 had to discover at implementation time which
  `G` fields belong in snapshots. `hookRegistry` is setup data (not
  runtime state) and must be excluded. `outcome` is derived and must
  never be stored. Zone snapshots store counts only, not card arrays.
- **Root cause:** ARCHITECTURE.md did not have a field-level persistence
  classification table before WP-013. The WP had to build it during
  execution, consuming time and introducing risk.
- **Fix:** ARCHITECTURE.md Section 3 now documents explicit field tables
  for runtime, configuration, and snapshot persistence classes.
- **Prevention:** Dependency Contract Verification now checks persistence
  classification for any new `G` fields.

**P3-4: Lobby observability flag ordering (WP-011)**
- **What happened:** UI needed to detect lobby completion, but querying
  `ctx.phase` from the UI violates the engine/framework boundary.
  Solution: set `G.lobby.started = true` before calling
  `ctx.events.setPhase('setup')`. Order is non-negotiable — if reversed,
  multiplayer clients may miss the event due to race conditions.
- **Root cause:** Bridging framework state (`ctx.phase`) and game state
  (`G`) requires explicit coordination. The pattern is subtle and was
  not documented before WP-011 encountered it.
- **Fix:** D-1238 locked the "flag then transition" pattern.
- **Prevention:** Runtime Readiness now checks phase transition ordering.
  Established Patterns documents the flag-before-transition rule.

**P3-5: Endgame evaluation order ambiguity (WP-010)**
- **What happened:** Both loss and victory conditions could be met in
  the same turn. Initial implementation did not specify evaluation order,
  creating a determinism violation.
- **Root cause:** The WP described conditions independently without
  addressing the simultaneous case. Legendary tabletop rules specify
  loss overrides victory, but this was not in the WP text.
- **Fix:** D-1235 locked loss-before-victory evaluation order. Explicit
  test covers the simultaneous edge case.
- **Prevention:** Runtime Readiness now checks for simultaneous/conflicting
  condition handling. Risk examples include this pattern.

**P3-6: Unknown rule effect type handling (WP-009B)**
- **What happened:** Future WPs will add new `RuleEffect` types. The
  runtime needed a strategy for encountering unknown types — crash,
  ignore, or degrade gracefully?
- **Root cause:** Forward compatibility requires an explicit degradation
  strategy. Without one, the first new effect type added by a future WP
  could crash the game loop.
- **Fix:** D-1234 established: unknown effect types push a warning to
  `G.messages` and execution continues without throwing.
- **Prevention:** Runtime Readiness now checks for degradation strategies
  when unknown/unsupported types are possible. Established Patterns
  documents the warn-and-continue approach.

---

### Phase 4 — Core Gameplay Loop (WP-014A through WP-020)

**P4-1: Stage gating gap (WP-015A patch of WP-014A)**
- **What happened:** `revealVillainCard` had no check for
  `G.currentStage === 'start'`, allowing cards to be revealed in the
  wrong turn phase.
- **Root cause:** WP-014A focused on the reveal pipeline but did not
  specify stage gating. The omission was not caught in pre-flight.
- **Fix:** WP-015A added an early-return stage gate. A micro-patch
  packet was created rather than modifying WP-014A.
- **Prevention:** Runtime Readiness now requires identifying stage
  gating requirements for any new move or hook.

**P4-2: Mutation ordering — silent card loss (WP-015A patch)**
- **What happened:** Cards were removed from the villain deck before
  City placement was confirmed valid. A malformed `G.city` caused the
  card to vanish — removed from deck but never placed.
- **Root cause:** Synchronous mutation ordering was not considered.
  The "remove then place" sequence is unsafe when the destination
  can reject the card.
- **Fix:** Deferred deck removal until City placement was confirmed.
- **Prevention:** Runtime Readiness now checks mutation ordering for
  multi-step operations. Scope Lock now forbids removing from source
  before confirming destination validity.

**P4-3: Move classification ambiguity (WP-016 v1 blocker)**
- **What happened:** WP-016 said to add `fightVillain` and `recruitHero`
  as moves, implying expansion of `CoreMoveName`. But `CoreMoveName` is
  a locked contract. Pre-flight v1 declared this a blocking conflict.
- **Root cause:** The WP did not distinguish between core moves
  (registered in `CoreMoveName`) and domain moves (with internal stage
  gating). WP-014A had already established the domain-move pattern with
  `revealVillainCard`, but this wasn't documented as a reusable pattern.
- **Fix:** Pre-flight v2 recognized the WP-014A precedent. Domain moves
  enforce stage gating internally without expanding `CoreMoveName`.
  ARCHITECTURE.md was updated to document the pattern.
- **Prevention:** Dependency Contract Verification now includes move
  classification. Established Patterns now documents the domain-move
  pattern explicitly.

**P4-4: Registry boundary at runtime (prevented across Phase 4)**
- **What happened:** Never reached production, but was a consistent risk.
  Multiple WPs needed card data during moves (type classification,
  stat values, ability hooks).
- **Root cause:** The natural impulse is to query the registry when you
  need card data. The architecture forbids this — moves must use
  pre-resolved data in `G`.
- **Fix:** Every Phase 4 WP that needed card data added a setup-time
  resolution step: `G.villainDeckCardTypes` (WP-014B),
  `G.cardStats` (WP-018), `G.heroAbilityHooks` (WP-021). Import
  audits in verification steps catch violations.
- **Prevention:** Runtime Readiness now explicitly checks registry
  data flow. Scope Lock forbids runtime registry access from moves.

**P4-5: Narrow mock crashes (WP-014B runtime guard)**
- **What happened:** Test mocks satisfying `CardRegistryReader` (narrow
  interface with only `listCards()`) crashed when `buildCardStats`
  called `listSets()` and `getSet()` — methods only on the broader
  `CardStatsRegistryReader` interface.
- **Root cause:** Structural typing means a narrow mock compiles but
  crashes at runtime when broader methods are called.
- **Fix:** Added `isCardStatsRegistryReader` runtime type guard that
  returns an empty result for narrow mocks instead of crashing.
- **Prevention:** Dependency Contract Verification now checks for
  optional fields and interface compatibility. Test Expectations now
  includes defensive guard guidance.

**P4-6: Downstream invalidation requiring re-audit (WP-014A/014B)**
- **What happened:** After WP-015 changed villain/henchman routing from
  discard to City, WP-014A's reveal pipeline invariants needed
  re-validation. Pre-flight v2/v3 revisions were created as "Mode B
  audit" prompts to verify invariants still held.
- **Root cause:** Downstream WPs can modify shared state paths that
  earlier WPs established. Without re-validation, invariant violations
  accumulate silently.
- **Mitigation:** When a WP modifies state paths established by a prior
  WP, note the risk explicitly. If the change is significant, a Mode B
  audit session may be warranted after execution.
- **Note:** This is a post-execution concern, not a pre-flight check.
  Pre-flight can flag the risk; the audit happens after the downstream
  WP completes.

### Phase 5 — Hero Abilities (WP-021, WP-022, WP-023)

**P5-1: Contract field drift in WP text (WP-022 pre-flight)**
- **What happened:** WP-022 used `gainAttack`, `gainRecruit`, `koCard`
  as keyword literals and `amount` as the descriptor field. The actual
  WP-021 contracts defined `'attack'`, `'recruit'`, `'ko'` and
  `magnitude`.
- **Root cause:** WP-022 was written before WP-021 was implemented.
  The WP author used intuitive names rather than the exact literals
  that WP-021 later defined.
- **Fix:** Pre-flight read the actual WP-021 source files and corrected
  all names before execution.
- **Prevention:** Dependency Contract Verification now mandates reading
  actual source files, not trusting WP text alone.

**P5-2: Helper-vs-move confusion (WP-022 pre-flight)**
- **What happened:** WP-022 listed `drawCards` as a helper to call for
  the `'draw'` keyword. But `drawCards` is a full move function that
  takes `MoveContext` and performs args validation and stage gating.
  Calling it from `executeHeroEffects(ctx: unknown)` would not compile.
- **Root cause:** The WP text described the *intent* (draw cards) but
  not the *mechanism* (which function to call and whether it's callable
  from a helper context).
- **Fix:** Pre-flight identified the incompatibility and locked a
  resolution: extract the draw algorithm using zone-ops primitives
  instead of calling the move function.
- **Prevention:** Dependency Contract Verification now requires verifying
  whether functions are pure helpers vs move functions.

**P5-3: Missing parameter in function signature (WP-022 pre-flight)**
- **What happened:** `executeHeroEffects(G, ctx, cardId)` had no
  `playerID` parameter, but the `'draw'` keyword needs to know whose
  deck/hand zones to modify.
- **Root cause:** The original signature was designed around the effect
  descriptors (which reference a card) without considering that some
  effects need player context.
- **Fix:** Pre-flight added `playerID: string` to the signature after
  reading the integration point code and understanding what data the
  caller has available.
- **Prevention:** Runtime Readiness now requires reading the integration
  point code and confirming what parameters are available.

**P5-4: Locked value string literals using aspirational names (WP-023 pre-flight)**
- **What happened:** WP-023's locked condition types table used
  `requiresColor` but `heroAbility.setup.ts` produces `heroClassMatch`.
  An evaluator built against the WP's name would never match any real
  condition in `G.heroAbilityHooks` at runtime.
- **Root cause:** The WP was written with intended semantics
  (`requiresColor` = "requires a color match") rather than the actual
  string produced by WP-021's setup code (`heroClassMatch`). The WP
  author described what the condition *means*, not what the code *says*.
- **Fix:** Pre-flight read `heroAbility.setup.ts` and found the actual
  string at line 123. Locked the session prompt to use `heroClassMatch`.
- **Prevention:** Dependency Contract Verification now includes a
  "Locked value string literals match actual code" check that requires
  verifying string literals against the code that *produces* them, not
  just the code that *consumes* them.

**P5-5: Data availability gap — G field exists but lacks needed subfields (WP-023 pre-flight)**
- **What happened:** WP-023 said condition evaluators would look up
  team/class data from `G.cardStats`. The field `G.cardStats` exists,
  but `CardStatEntry` only has `attack`, `recruit`, `cost`, `fightCost`
  — no `team` or `heroClass`. The data exists in the registry schema
  (`HeroGroupSchema.team`, `HeroCardSchema.hc`) but was never resolved
  into G at setup time.
- **Root cause:** The WP assumed a richer `CardStatEntry` than WP-018
  actually built. The WP's "Files Expected to Change" didn't include
  `economy.types.ts`, `types.ts`, or setup code — so there was no
  authorized path to add the missing data.
- **Fix:** Pre-flight proposed Option A (narrow scope): implement all
  4 evaluators but `heroClassMatch` and `requiresTeam` return `false`
  (safe-skip placeholder) until a follow-up WP resolves team/class data
  into G. Human authorized this approach.
- **Prevention:** Structural Readiness and Dependency Contract
  Verification now include checks that verify subfield availability
  inside G types, not just top-level field existence. The safe-skip
  placeholder pattern is now a locked precedent for handling data gaps
  without blocking execution or expanding scope.

**P5-6: Scope lock vs data infrastructure mismatch (WP-023 pre-flight)**
- **What happened:** Two of four condition types required data that
  didn't exist in G, and the WP's scope didn't include the files needed
  to add it. This created a design gap — the WP described behavior that
  was unimplementable within its own scope boundaries.
- **Root cause:** The WP was designed top-down from game mechanics
  (what conditions should exist) rather than bottom-up from available
  data (what conditions can be evaluated with current G fields).
- **Fix:** Narrow scope with human authorization. The session prompt
  made blocked evaluators explicit and documented them as safe-skip
  placeholders with `// why:` comments.
- **Prevention:** Risk & Ambiguity Review now includes "Scope lock vs
  data infrastructure mismatch" as a named risk pattern.

---

### Phase 6 — Scheme & Mastermind Abilities (WP-024)

**P6-1: WP file paths referencing non-existent files (WP-024 pre-flight)**
- **What happened:** WP-024's scope section and "Files Expected to Change"
  listed `src/setup/buildDefaultHookDefinitions.ts` (modify) and
  `src/setup/buildImplementationMap.ts` (new or modify). Neither file
  exists. Both `buildDefaultHookDefinitions` and
  `DEFAULT_IMPLEMENTATION_MAP` live in `src/rules/ruleRuntime.impl.ts`,
  consolidated there during WP-009B implementation.
- **Root cause:** The WP was authored before WP-009B was implemented.
  The WP author assumed file paths based on intended architecture
  (`setup/` directory for setup-time functions) rather than the actual
  structure that WP-009B created (`rules/` directory, consolidated
  into one file). The paths were never validated against the actual
  filesystem after WP-009B was complete.
- **Fix:** Pre-flight read the actual codebase, found the functions in
  `ruleRuntime.impl.ts`, and corrected both WP-024 and EC-024 before
  execution. D-2405 logged as a permanent decision.
- **Prevention:** Dependency Contract Verification now includes a
  "WP file paths verified against actual filesystem" check. Risk
  examples include "WP file paths referencing non-existent files."

**P6-2: Stub replacement cascading into integration tests (WP-024 pre-flight)**
- **What happened:** WP-009B created stub handlers
  (`defaultSchemeImplementation`, `defaultMastermindImplementation`)
  with placeholder triggers (`onTurnStart`, `onTurnEnd`) and specific
  message strings (`'Scheme: turn started.'`, `'Mastermind: turn ended.'`).
  Integration tests in `ruleRuntime.integration.test.ts` asserted these
  exact values. WP-024 replaced stubs with real handlers using real
  triggers (`onSchemeTwistRevealed`, `onMastermindStrikeRevealed`),
  which broke 5 of 6 integration test assertions.
- **Root cause:** The integration tests were designed to prove the
  pipeline worked with stubs. When the stubs were replaced, the tests'
  value assertions became stale. This is a predictable consequence of
  stub-to-real replacement, but it was not anticipated in the WP's
  "Files Expected to Change" section.
- **Fix:** Pre-flight identified the integration tests as requiring
  value-only assertion updates and authorized the changes under the
  01.5 runtime wiring allowance. The session prompt explicitly listed
  each test's required update. D-2404 logged.
- **Prevention:** Established Patterns now documents "stub replacement
  cascades into integration tests" as a locked pattern. Risk examples
  include this scenario.

**P6-3: Effect type vocabulary gap limiting MVP behavior (WP-024 pre-flight)**
- **What happened:** WP-024 specified that the mastermind strike handler
  should cause "each player gains 1 wound." This requires moving a card
  from `G.piles.wounds` to a player's discard pile. But the existing
  `RuleEffect` types (`queueMessage`, `modifyCounter`, `drawCards`,
  `discardHand`) have no wound-gain effect. Adding a new `'gainWound'`
  type would require modifying `ruleHooks.types.ts` (WP-009A contract).
- **Root cause:** The WP described desired gameplay behavior without
  checking whether the existing effect type vocabulary could express it.
  The `RuleEffect` union was designed for WP-009B's MVP and was never
  expanded. The WP's "Non-Negotiable Constraints" said to use existing
  effect types, creating a tension between the stated behavior and the
  stated constraints.
- **Fix:** Pre-flight resolved the tension by extending the safe-skip
  pattern from condition evaluators (WP-023) to effect production:
  implement the handler structure using available effect types
  (`modifyCounter` + `queueMessage`) for tracking and observability,
  defer actual wound card movement to a future WP that adds the
  `'gainWound'` effect type. D-2403 logged.
- **Prevention:** Established Patterns now documents the "safe-skip for
  effect type gaps" pattern. Risk examples include "effect type
  vocabulary gap."

---

### Phase 5 continued — Board Keywords (WP-025)

**P5c-1: Effect type gap resolved by inline precedent instead of deferred
tracking (WP-025 pre-flight)**
- **What happened:** WP-025 specified `getAmbushEffects(): RuleEffect[]`
  applied via `applyRuleEffects` for the Ambush keyword's "each player
  gains 1 wound" behavior. Like WP-024, no `gainWound` RuleEffect type
  exists. However, unlike WP-024 (which deferred to counter-only
  tracking), the codebase already had functional inline wound-gain code:
  escape wounds in `villainDeck.reveal.ts` (lines 124-137) call
  `gainWound` directly.
- **Root cause:** The WP described the ideal architecture (pipeline-routed
  effects via RuleEffect) without checking whether the effect vocabulary
  could express it, AND without checking whether an inline precedent
  already existed for the same behavior.
- **Fix:** Pre-flight identified both the gap and the inline precedent.
  Resolution: use the inline `gainWound` pattern (same as escape wounds)
  rather than counter-only tracking. The WP's scope B and E were
  corrected — `getAmbushEffects(): RuleEffect[]` became
  `hasAmbush(): boolean` with inline wound logic. D-2503 logged.
- **Prevention:** Established Patterns now documents "inline behavior as
  effect type workaround." Risk examples include "effect type gap with
  existing inline precedent." Dependency Contract Verification refined
  to check for inline precedents when effect type gaps are found.
- **Key insight:** The D-2403 safe-skip pattern has two modes: (1) defer
  to counter-only tracking when no inline precedent exists (WP-024), or
  (2) use the inline precedent when one does (WP-025). Mode 2 delivers
  functional MVP behavior; mode 1 delivers tracking only.

**P5c-2: Setup-time data source missing for declared keywords (WP-025
pre-flight)**
- **What happened:** WP-025 declared three board keywords (Patrol,
  Ambush, Guard) with `buildCardKeywords` extracting keyword data from
  the registry. Pre-flight investigated the actual card JSON data across
  all 40 sets and found: Ambush appeared 304 times (identifiable by
  `"Ambush:"` prefix in ability text), Patrol appeared 4 times but as
  a completely different mechanic (`[keyword:Patrol the Bank]` in Secret
  Wars Vol 2 — named location patrols, not the generic fight-cost
  modifier WP-025 describes), and Guard had zero occurrences.
- **Root cause:** The WP's Assumes section said "Card data includes
  keyword information in ability text or structured metadata" without
  verifying this against actual data. No structured keyword metadata
  exists in the registry schema. Only Ambush is parseable from ability
  text. Patrol in the data is semantically different from the WP's
  concept. Guard simply doesn't exist.
- **Fix:** Pre-flight corrected the WP's Assumes section and scope C.
  `buildCardKeywords` extracts Ambush from ability text. Patrol and
  Guard produce empty results (safe-skip per D-2302). All three
  mechanics are fully implemented and tested with synthetic data.
  D-2504 logged.
- **Prevention:** Dependency Contract Verification now includes
  "setup-time data source verified for all extractors." Risk examples
  include "setup-time data source gap." Established Patterns now
  documents "setup-time data extraction with safe-skip for missing
  sources."

**P5c-3: WP function signature using direct registry type instead of
`unknown` convention (WP-025 pre-flight)**
- **What happened:** WP-025 scope C specified
  `buildCardKeywords(registry: CardRegistry, matchConfig: MatchSetupConfig)`.
  The `CardRegistry` type is from `@legendary-arena/registry`, which
  the game engine must not import. The established convention (used by
  `buildCardStats`, `buildVillainDeck`, `buildMastermindState`) is
  `registry: unknown` with a local structural interface and runtime
  type guard.
- **Root cause:** The WP author used the conceptual type name rather
  than the established convention. This is a variation of "locked value
  string literal drift" (D-2304) applied to function signatures.
- **Fix:** Pre-flight corrected the WP to use `registry: unknown` with
  `CardKeywordsRegistryReader` local interface per the `buildCardStats`
  pattern.
- **Prevention:** Dependency Contract Verification now includes
  "WP function signatures match established conventions." Risk examples
  include "WP function signatures using direct registry types."

---

### Phase 6 — Verification, UI & Production (WP-027, WP-028, WP-029)

**P6-1: Projection aliasing — returned references to mutable G data
(WP-028 post-mortem)**
- **What happened:** `buildUIState` city projection returned
  `gameState.cardKeywords[space] ?? []`. When `cardKeywords[space]`
  existed, this returned a **direct reference** to the mutable array in
  `G`. A UIState consumer could accidentally mutate `G` through the
  returned array, violating the purity guarantee.
- **Root cause:** The `?? []` fallback creates a new array on miss, but
  the hit path shares a reference. This is invisible to standard tests —
  `JSON.stringify` equality checks compare values, not object identity.
  The non-mutation test (`gBefore === gAfter`) also passes because no
  consumer actually mutated the reference during the test.
- **Fix:** Replaced with `cardKeywords !== undefined ? [...cardKeywords]
  : []` — always produces a new array. Discovered during post-mortem
  section 5 (Mutation, Aliasing, & Reference Safety).
- **Prevention:** Dependency Contract Verification now includes
  "projection aliasing verified for all derived outputs." Established
  Patterns now documents "projection aliasing prevention." Risk examples
  include "projection aliasing." Post-mortem `01.6` section 5 is
  designated high-risk for all projection WPs.

**P6-2: Decision ID encoding mismatch — em-dash vs hyphen (WP-028
pre-flight)**
- **What happened:** Pre-flight grep for `D-0301` in DECISIONS.md
  returned no matches, leading to an incorrect finding that D-0301 and
  D-0302 did not exist. They did exist — but DECISIONS.md headings use
  em-dashes (`D‑0301`) while the grep pattern used regular hyphens
  (`D-0301`).
- **Root cause:** Unicode em-dash (`U+2011`) is visually
  indistinguishable from ASCII hyphen (`U+002D`) but they are different
  bytes. Standard grep does not fuzzy-match between them.
- **Fix:** Verified by searching for the decision title keyword instead
  of the ID. Both D-0301 and D-0302 were confirmed present.
- **Prevention:** Dependency Contract Verification now includes "Decision
  ID search accounts for character encoding." Always search by title
  keyword as a fallback, not just the numeric ID string.

**P6-3: Framework ctx subset pattern — local structural interface for
non-framework consumers (WP-028 pre-flight)**
- **What happened:** WP-028 specified `buildUIState(G, ctx: Ctx)` but
  the UI state files must not import boardgame.io. `makeMockCtx` returns
  `SetupContext` (a completely different shape with `{ ctx: { numPlayers },
  random: { Shuffle } }`), not the `{ phase, turn, currentPlayer }` shape
  `buildUIState` needs.
- **Root cause:** WP text assumed boardgame.io's `Ctx` type would be
  available. No prior WP had needed a subset of `Ctx` fields without
  importing boardgame.io. WP-022/027's local interface pattern applied
  to registry readers and move contexts, but not to framework context
  consumers.
- **Fix:** Pre-flight defined `UIBuildContext` as a local structural
  interface with `readonly` fields and no optional members. Tests use
  a plain inline mock, not `makeMockCtx`. Locked as exact shape during
  pre-flight.
- **Prevention:** Established Patterns now documents "local structural
  interface for framework ctx subset." Risk examples include "framework
  ctx subset needed without boardgame.io import."

**P6-4: Lifecycle wiring creep — non-framework code wired into game.ts
(WP-028 pre-flight)**
- **What happened:** Pre-flight identified that without explicit
  prohibition, the executing agent could wire `buildUIState` into
  `game.ts`, a phase hook, or a move — none of which are appropriate
  for a projection function. This would couple the UI projection to the
  gameplay lifecycle.
- **Root cause:** No prior WP had introduced a function that was
  explicitly NOT part of the boardgame.io lifecycle. All prior engine
  functions were either moves, phase hooks, setup builders, or pure
  helpers called from moves. A projection consumed by external layers
  (UI, server, replay) was a new pattern with no lifecycle precedent.
- **Fix:** Pre-flight added an explicit lifecycle prohibition to the
  session prompt, EC, and WP: `buildUIState` MUST NOT be called from
  `game.ts`, any move, or any phase hook.
- **Prevention:** Established Patterns now documents "lifecycle isolation
  for non-framework code." Risk examples include "lifecycle wiring
  creep." Session prompt AI Agent Warning section includes a lifecycle
  prohibition block.

**P6-5: Filter input type missing required data — upstream producer scope
gap (WP-029 pre-flight)**
- **What happened:** WP-029 introduced `filterUIStateForAudience` which
  operates on `UIState` (not `G`). The WP's information visibility matrix
  specified that the active player sees hand card ext_ids. But
  `UIPlayerState` only had `handCount: number` — no `handCards` field.
  The filter cannot add data that doesn't exist in its input type. Both
  WP-029's "Files Expected to Change" and EC-029's "Files to Produce"
  omitted `uiState.build.ts`.
- **Root cause:** The WP was designed from the filter's output perspective
  (what each audience sees) without verifying that the filter's input
  type contained all necessary data. The upstream producer (`buildUIState`)
  was assumed to already include hand card ext_ids, but WP-028 deliberately
  projected hands as counts only (D-2802).
- **Fix:** Pre-flight identified the scope gap as a blocking finding. Added
  `uiState.build.ts` to both WP-029 and EC-029 file lists. The
  modification was minimal: `handCards: [...zones.hand]` (one line) in the
  player projection loop, authorized under 01.5 wiring allowance.
- **Prevention:** Dependency Contract Verification now includes "filter/
  consumer input type contains all needed data." Established Patterns now
  documents "upstream producer modification for downstream filter." Risk
  examples include "filter input type missing required data."

**P6-6: `exactOptionalPropertyTypes` TypeScript strictness (WP-029
execution)**
- **What happened:** `preserveHandCards` initially used an inline ternary:
  `handCards: player.handCards !== undefined ? [...player.handCards] : undefined`.
  TypeScript rejected this because the result type `string[] | undefined` is
  not assignable to the optional property `handCards?: string[]` under
  `exactOptionalPropertyTypes: true`.
- **Root cause:** The project's `tsconfig.json` enables
  `exactOptionalPropertyTypes`, which distinguishes between "property is
  absent" and "property is present with value `undefined`." An inline
  ternary produces the latter, which is rejected.
- **Fix:** Used conditional assignment pattern: build the base object
  without `handCards`, then assign in an `if` block. This produces
  "property absent" on the else path, which satisfies the strict check.
- **Prevention:** Established Patterns now documents
  "`exactOptionalPropertyTypes` TypeScript strictness." Risk examples
  include this pattern. Future WPs with optional fields should use
  conditional assignment, not inline ternaries.

### Phase 6 continued — Campaign & Meta-Orchestration (WP-030)

**P6-7: Code-category classification as pre-flight PS-# for new
engine subdirectories (WP-030 pre-flight)**
- **What happened:** WP-030 introduced `packages/game-engine/src/campaign/`
  as a new directory for the campaign framework. The directory was not
  listed in `02-CODE-CATEGORIES.md` and had no DECISIONS.md classification.
  Pre-flight §Code Category Boundary Check flagged this as a blocking
  finding.
- **Root cause:** Contract-Only WPs that introduce new subdirectories
  create the same governance gap as the replay (WP-027, D-2706) and
  UI (WP-028, D-2801) directories before them. Without explicit
  classification, the category boundaries drift silently. This is
  the third instance of the same pattern, and each time the WP
  executed correctly, but only because pre-flight caught the gap and
  required a DECISIONS.md entry as a pre-session action.
- **Fix:** PS-1 resolved during pre-flight by creating D-3001 —
  "Campaign Directory Classified as Engine Code Category" — following
  the D-2706 / D-2801 template. Status: Immutable. Pre-flight then
  flipped from conditional to unconditional READY TO EXECUTE.
- **Prevention:** §Code Category Boundary Check already contains the
  check since the WP-028 update. Established Patterns now documents
  the **workflow** explicitly: catch during pre-flight, resolve as
  PS-# by creating the DECISIONS.md entry before execution, log the
  resolution in Authorized Next Step. Risk examples include "Contract-
  Only WP creates new directory without code-category classification"
  with the D-2706 / D-2801 / D-3001 precedent chain. Three confirmed
  precedents now exist — the next new engine subdirectory should
  trigger PS-# automatically.

**P6-8: Pre-flight refinement back-sync gap — WP text retains
pre-refinement signature (WP-030 pre-commit review)**
- **What happened:** Pre-flight risk review #5 locked a parameter-
  shape refinement for `evaluateScenarioOutcome`: split the single
  `conditions: ScenarioOutcomeCondition[]` parameter into separate
  `victoryConditions` and `failureConditions` arrays, so the locked
  loss-before-victory evaluation order is expressible at the call
  site. The session prompt implemented the refinement correctly; the
  code shipped with the split signature; tests used the split form;
  STATUS, WORK_INDEX, and post-mortem all referenced the new shape.
  But WP-030 line 196 retained the original single-array signature
  text. A future reader of WP-030 alone would see a spec/code drift.
- **Root cause:** When the pre-flight tightened the `ScenarioOutcome`
  return type, the author (this agent) updated the return type in
  WP-030 text but did not also back-sync the parameter list for the
  split refinement. The split refinement was introduced later when
  writing the session prompt, which explicitly noted the split as a
  "minor parameter shaping" but did not back-edit WP-030 text.
- **Fix:** Pre-commit review §Optional Pre-Commit Nits flagged this
  as Nit 1. Applied as a one-line edit to WP-030 line 196 replacing
  the old signature with the split form, plus four bullets of
  rationale citing D-1235 precedent and pre-flight risk #5 lock. No
  code, test, or other governance-doc changes required.
- **Prevention:** Risk examples now include "pre-flight refinement
  back-sync gap" with a proactive mitigation: when pre-flight locks
  a refinement that differs from WP text, the Authorized Next Step
  section should explicitly list the back-sync as a required pre-
  session action. This catches the drift before execution starts
  instead of relying on the pre-commit review to catch it at the
  gatekeeper stage.

**P6-9: Named union for related function parameters (WP-030 external
review tightening)**
- **What happened:** WP-030 initially specified
  `evaluateScenarioOutcome` returning `'victory' | 'defeat' |
  'incomplete'` as an inline literal union, and
  `advanceCampaignState` accepting `outcome: string` as a plain
  string. This would have allowed a caller to pass `'Victory'`,
  `'win'`, or any other string to `advanceCampaignState` without
  compile error — silent outcome-string drift between the two
  functions.
- **Root cause:** The two functions were specified independently,
  and the shared literal union was expressed once as a literal and
  once as `string`. Without a named type forcing both signatures to
  agree, TypeScript could not catch the drift.
- **Fix:** External review flagged the risk during the pre-flight
  review phase. Pre-flight disposition log accepted the finding and
  applied the tightening: introduce
  `type ScenarioOutcome = 'victory' | 'defeat' | 'incomplete'` in
  `campaign.types.ts` and use it in both signatures. EC-030 Locked
  Values were updated to include the named union. The tightening
  was scope-neutral — no new behavior, just better types.
- **Prevention:** Established Patterns now documents "named union
  for related function parameters" with the general principle: when
  two or more functions exchange a literal value that can drift,
  define a named `type` union in the shared contract file and use
  the named type in every signature. Apply whenever the same literal
  union appears in more than one public function signature.

**P6-10: Explicit "01.5 NOT INVOKED" declaration for purely additive
Contract-Only WPs (WP-030 session prompt)**
- **What happened:** WP-030 was a pure Contract-Only WP that did not
  trigger any of the 01.5 runtime-wiring allowance conditions: no
  new `LegendaryGameState` field, no `buildInitialGameState` shape
  change, no new `LegendaryGame.moves` entry, no new phase hook. The
  session prompt included an explicit `### Runtime Wiring Allowance —
  NOT INVOKED` section that enumerated the four criteria, marked each
  as absent, cited 01.5 §Escalation, and stated that the allowance
  could not be cited retroactively.
- **Root cause:** Prior WPs had implicitly not-invoked 01.5 when it
  didn't apply, relying on the absence of an invocation to mean
  not-invoked. This leaves room for an executing agent to
  retroactively claim 01.5 as a loophole if an unanticipated
  structural break appears mid-execution. The absence of an explicit
  declaration also makes the audit trail ambiguous — a reviewer
  cannot tell whether 01.5 was considered and rejected vs. simply
  not considered.
- **Fix:** WP-030 established the pattern: for any Contract-Only WP,
  the session prompt must include an explicit 01.5 NOT INVOKED
  section with (a) the four trigger criteria enumerated and marked
  absent, (b) a reference to 01.5 §Escalation, and (c) a stop-and-
  escalate instruction if a structural break appears. Post-mortem
  §5 and pre-commit review §5 both verified the declaration held
  throughout execution.
- **Prevention:** Established Patterns now documents "explicit 01.5
  NOT INVOKED declaration for purely additive WPs" as the canonical
  pattern for Contract-Only WPs. The declaration is also referenced
  in the pre-commit review template's Runtime Boundary Check.

**P6-11: Reserved parameter pattern — `void parameter;` for MVP-
unused parameters with locked API shape (WP-030 execution)**
- **What happened:** `advanceCampaignState(state, scenarioId,
  outcome, rewards)` accepts `outcome: ScenarioOutcome` as a locked
  API parameter. At MVP, `outcome` is not used to alter the returned
  state — it is reserved for future branching logic (e.g., advancing
  to different scenarios depending on victory vs defeat). Deleting
  the parameter would require changing call sites when the parameter
  becomes meaningful; prefixing with `_` would require renaming at
  the same time. Both approaches churn the API surface.
- **Root cause:** TypeScript's `noUnusedParameters` (or strict linter
  equivalents) flag unused parameters as errors. The obvious fixes
  (delete, rename with `_` prefix) both compromise the locked API.
- **Fix:** Reference the parameter in a no-op `void outcome;`
  statement at the top of the function body with a `// why:` comment
  explaining that the parameter is reserved for future branching
  logic. This satisfies the unused-parameter check, preserves the
  locked API shape, and documents the intent for future readers.
- **Prevention:** Established Patterns now documents "reserved
  parameter pattern for signature stability" with the full
  `void parameter;` + `// why:` convention. Apply when a parameter
  is part of a locked API shape but not yet meaningful in the MVP
  implementation.

**P6-12: External reviewer findings verified against actual repo
state (WP-030 external review disposition)**
- **What happened:** An external reviewer (not the pre-flight
  author) proposed improvements against a stale or cached view of
  the repo artifacts. Four of the findings were factually incorrect:
  (1) "D-0603 missing from DECISIONS.md" — it exists at line 188 with
  the em-dash heading `D‑0603`; (2) "WP-030 marked Needs review in
  WORK_INDEX" — the convention `[ ] ... ✅ Reviewed` means pending
  execution but document approved, not needs review; (3) "Add grep
  verification step for forbidden imports" — already present in WP-030
  Verification Steps §3 and §6; (4) "D-3001 stub format using `####`
  and `Status: Active Policy`" — doesn't match the house-style
  `### D-NNNN` + `Status: Immutable` used by D-2706 and D-2801. Two
  other findings were valid and applied: trailing orphaned backticks
  at the end of DECISIONS.md, and the `advanceCampaignState(outcome:
  string)` type-tightening to the named `ScenarioOutcome` union.
- **Root cause:** External reviewers may operate against cached,
  snapshotted, or outdated views of repo artifacts. Their findings
  are useful as prompts but not authoritative. Accepting findings
  without verification leads to incorrect actions (e.g., creating a
  duplicate D-0603 entry when it already exists).
- **Fix:** Pre-flight §External Review — Disposition Log recorded
  each finding's verdict with evidence: citations to actual line
  numbers, explicit rejection of false claims, and explicit
  acceptance of valid ones. No fix was applied based on a rejected
  finding.
- **Prevention:** Risk examples now include "external reviewer
  findings against stale artifact snapshots" with the general
  mitigation: treat every external-reviewer finding as untrusted
  until verified against actual repo state via direct tool
  inspection. Reject false findings explicitly. Accept valid findings
  with citations. A disposition log is the audit artifact.

**P6-13: Contract-Only WP with new directory + full 01.6 post-mortem
+ 01.3 pre-commit review + two-commit split (WP-030 workflow
precedent)**
- **What happened:** WP-030 was the first Contract-Only WP to
  exercise the full 9-step workflow end-to-end with every governance
  artifact in place: session context loaded (step 0), pre-flight
  with PS-1 resolution (step 1), session prompt with explicit 01.5
  NOT INVOKED declaration (step 2), execution in the same session
  as pre-flight via user authorization (step 3, one-session
  deviation), full 10-section 01.6 post-mortem with verdict WP
  COMPLETE (step 4), 01.3 pre-commit review with verdict Safe to
  commit as-is and one non-blocking nit (step 5), two-commit split
  (`SPEC:` for governance + artifacts, `EC-030:` for code) following
  the WP-029 precedent (step 6). Steps 7 and 8 followed after commit.
- **Root cause:** Prior Contract-Only WPs had used ad-hoc workflows.
  WP-030 was the first to exercise every step as a demonstration of
  the full governance pipeline for a purely additive WP. The session
  even deviated from the step-3-fresh-session convention (execution
  ran in the same session that generated the pre-flight and session
  prompt), which is worth noting for future audits — the deviation
  was explicit, user-authorized, and logged in the execution summary.
- **Fix:** None required — the workflow completed successfully. The
  entry in this Precedent Log serves as a reference for future
  Contract-Only WPs: follow this exact pattern unless there is a
  reason to deviate.
- **Prevention:** Future Contract-Only WPs with new directory
  introductions should use WP-030 as the template. The four new
  artifacts (pre-flight, session prompt, post-mortem, pre-commit
  review) establish a reusable pattern for the `docs/ai/invocations/`
  directory. Lessons from WP-030 are propagated into Established
  Patterns, Risk examples, and this Precedent Log.

**P6-14: Pre-flight blind spot for "scan-G-and-throw" checks
against fungible / non-unique CardExtIds (WP-031 mid-execution
discovery, PS-3)**
- **What happened:** WP-031 §D specified `checkNoCardInMultipleZones`
  as a literal Set-based dedup of every CardExtId across every
  zone. Pre-flight reasoned at the type-system level
  (`CardExtId = string`, "any string is unique") and locked the
  implementation. The executor halted at §Implementation Task D
  before writing any code, traced the predicate against the
  concrete output of `buildInitialGameState` /
  `buildStartingDeckCards` / `pilesInit.ts` `createPileCards`,
  and discovered that the engine's MVP design reuses six fungible
  token CardExtIds (`'starting-shield-agent'`,
  `'starting-shield-trooper'`, `'pile-bystander'`, `'pile-wound'`,
  `'pile-shield-officer'`, `'pile-sidekick'`) inside piles and
  starting decks. A literal cardId-uniqueness check would throw
  `InvariantViolationError` on the first pile entry of every
  freshly-built valid `G`, regressing the 348-test baseline.
- **Root cause:** Pre-flight Dependency Contract Verification did
  not include a "trace upstream builder concrete output" step for
  scan-and-throw invariants. The pre-flight author (this agent)
  read the type system but not the concrete builder. The 30-issue
  copilot scan also missed it — Finding #4 (drift-detection)
  treated `checkNoCardInMultipleZones` as a black-box specification
  without cross-referencing the actual setup-layer output.
- **Fix:** PS-3 mid-execution — user authorized Option 1
  (fungible-exclusion cross-zone semantics). Three amendments
  applied in place: WP §Amendments A-031-01 (fungible-exclusion
  semantics), A-031-02 (`victoryPile` → `victory` field name
  drift), A-031-03 (lifecycle parameter `| undefined` widening).
  D-3103 written for the card uniqueness invariant scope. Pre-
  flight RS-9 / RS-10 / RS-11 + PS-3 added. Copilot check Re-Run
  2026-04-15 with Findings #31 / #32 / #33 marked PASS.
- **Prevention:** Established Patterns now documents "pre-flight
  must verify scan-G-and-throw invariants against concrete
  builder output, not just against the abstract type system."
  Risk examples now include "Scan-G-and-throw check spec without
  setup-layer concrete-output verification." Future "scan-G" WPs
  must trace upstream builder output during pre-flight before
  the session prompt is generated. The pre-flight Dependency
  Contract Verification step gains a new check item: "for any
  invariant predicate that scans `G` and throws, verify the
  predicate against the concrete output of every builder that
  produces valid `G`."

**P6-15: First mid-execution PS-# with user-authorized
scope-neutral amendment (WP-031 PS-3 workflow precedent)**
- **What happened:** WP-031 was the first WP to introduce a
  pre-session action (PS-#) **mid-execution** rather than
  pre-execution. PS-1 (D-3101) and PS-2 (D-3102) were created
  before the session prompt was generated, per the standard
  pre-flight workflow. PS-3 (D-3103 + A-031-01/02/03) was created
  AFTER the session prompt was generated and AFTER the executor
  was actively writing code, when implementation hit a
  scope-neutral incompatibility with reality. The workflow
  handled it gracefully: the executor halted at the §Implementation
  Task D boundary (per the session prompt's "STOP and escalate
  when the check cannot be confidently classified" rule),
  reported the conflict to the user with concrete file/line
  evidence, requested authorization for Option 1 (fungible
  exclusion), and after the user authorized, applied the
  amendments to: WP §Amendments section, DECISIONS.md (D-3103),
  pre-flight (new RS-9/10/11 + PS-3), and copilot check (Re-Run
  2026-04-15 with new Findings #31/32/33). Each artifact was
  re-confirmed before execution resumed.
- **Root cause:** The original 9-step workflow assumed all PS-#
  resolutions happen pre-session. The WP-031 fungibility
  discovery showed that some incompatibilities cannot be caught
  pre-flight no matter how thorough the review — they only
  surface when the executor actually starts writing the affected
  function. The workflow had no documented procedure for
  handling mid-execution discoveries.
- **Fix:** PS-3 was treated as a normal pre-session action that
  happened to land mid-session. The user-authorized amendment
  was applied to all four governance artifacts (WP, DECISIONS,
  pre-flight, copilot check) before any check-implementation
  code was committed, so the audit trail remains complete. The
  scope-neutral amendment qualified because it (a) did not add
  or remove files, (b) did not change the locked test count,
  (c) did not change wiring decisions, (d) did not change the
  category taxonomy, and (e) was captured in every relevant
  artifact.
- **Prevention:** Established Patterns now documents
  "scope-neutral mid-execution amendment as a permitted
  operation under EC mode" with the five qualifying conditions.
  The workflow is: executor halts, escalates with concrete
  evidence, user authorizes, executor applies amendments to all
  four artifacts (WP §Amendments, DECISIONS, pre-flight,
  copilot check), executor resumes execution. Future WPs that
  encounter mid-execution incompatibilities should follow this
  exact pattern instead of attempting to force-fit the original
  spec or aborting the session entirely.

**P6-16: Verification grep patterns matching comments and JSDoc
instead of actual imports (WP-031 implementation friction)**
- **What happened:** The WP-031 session prompt included
  Verification Step 3: `git grep -l "boardgame.io" -- "<dir>/"`
  expecting "no output" to confirm forbidden imports were absent.
  The pattern uses `.` which is regex-special and matches "any
  character." The executor's check files contained comments and
  JSDoc that legitimately mentioned `boardgame.io` for
  documentation purposes (e.g., "this file does not import
  boardgame.io"), and the grep matched all 6 files. The executor
  could not satisfy the binary "no output" gate without either
  (a) escaping the pattern, (b) refining the pattern to match
  import lines only, or (c) rephrasing every comment to remove
  the literal string.
- **Root cause:** The verification grep was written as a
  convenience proxy for "no boardgame.io imports" but did not
  account for the regex-special `.` or for comments mentioning
  the forbidden module name in rationale text. Binary "no
  output" gates are unforgiving — a single comment match fails
  the gate.
- **Fix:** The executor rephrased every comment occurrence in
  the invariants/ files from `boardgame.io` to "the game
  framework," weakening the documentation. The substitution was
  necessary to satisfy the strict grep, but it removed the
  precise reference to the forbidden module name from the
  rationale.
- **Prevention:** Established Patterns now documents "audit grep
  patterns must escape regex specials when used as binary 'no
  output' gates." Risk examples now include "Verification grep
  patterns matching comments and JSDoc instead of imports."
  Future pre-flight Verification Steps must use import-line-
  specific patterns: `from ['"]boardgame\.io` (matches actual
  import statements) instead of bare `boardgame.io` (matches
  any text containing those characters in any order). Comments
  mentioning the forbidden module name remain permitted.

**P6-17: Hidden-coupling fix during post-mortem as in-allowlist
refinement (WP-031 post-mortem §6 precedent)**
- **What happened:** The WP-031 post-mortem Section 6
  (Hidden-Coupling Detection) reviewed `gameRules.checks.ts`
  and discovered that the implementation used inline string
  literals for the six fungible token CardExtIds instead of
  importing them from the setup layer. The literals duplicated
  values defined in `buildInitialGameState.ts` and `pilesInit.ts`,
  creating a drift coupling: if a future setup-layer refactor
  renamed any of the six constants, the inline literals would
  not follow automatically. The post-mortem applied a fix in the
  same session: replaced the inline literals with named imports
  from `../setup/buildInitialGameState.js` (which already
  re-exports the pile constants from `pilesInit.ts`). The fix
  was strictly behavior-preserving (the imported values are
  byte-identical to the inline literals at this moment in time)
  and re-verified at 358/94/0.
- **Root cause:** The original implementation chose inline
  literals for speed-of-typing, plus a misreading of D-3103's
  "no runtime dependency on the setup layer's exports" clause
  as "do not import from setup/." The actual D-3103 clause says
  "the values are stable" — it does not forbid imports. WP §
  Amendments A-031-01 item 1 explicitly preferred the import
  form ("use the existing src/setup/buildInitialGameState.js
  and src/setup/pilesInit.js re-exports"), but the executor's
  initial implementation deviated from the preferred form.
- **Fix:** Post-mortem Section 6 finding triggered an
  in-allowlist refinement. The fix stayed inside the WP
  allowlist (`gameRules.checks.ts` was already in the WP-031
  scope), so 01.5 runtime-wiring allowance was NOT invoked —
  01.5 is reserved for off-allowlist edits required by
  structural integration. The post-mortem document recorded
  the fix under §10 "Fixes applied during post-mortem" with
  the rationale and the re-verification result.
- **Prevention:** Established Patterns now documents "post-
  mortem fix as in-allowlist refinement" to clarify the
  boundary between 01.5 (off-allowlist) and 01.6 §10 (in-
  allowlist). Future post-mortems that discover hidden-coupling
  risks, aliasing bugs, or comment gaps inside already-
  allowlisted files may apply the fix immediately and record
  it under §10 without invoking 01.5. The post-mortem
  document itself is the audit artifact.

**P6-18: Setup-only wiring scope as the default for runtime-wiring
WPs whose checks could fire at multiple lifecycle points (WP-031
D-3102 precedent)**
- **What happened:** WP-031 introduced `runAllInvariantChecks`,
  an orchestrator that could plausibly fire at four different
  lifecycle points: setup return (after `buildInitialGameState`),
  after each move (wrapping every entry in `LegendaryGame.moves`),
  on phase transitions (`onBegin`/`onEnd`), or on turn boundaries
  (`turnLoop.ts`). The original WP §H said "wire invariant checks
  into setup and optionally each move (configurable — may be
  dev/test only for performance). Document the approach in
  DECISIONS.md." Pre-flight RS-3 surfaced the ambiguity as PS-2
  and evaluated three options: Option A (per-move wiring,
  production-on, highest correctness coverage but highest
  ripple risk on the 348-test baseline), Option B (setup-only
  wiring, lowest ripple risk, narrower coverage), Option C
  (dev/test-gated per-move wiring, requires environment coupling
  in `game.ts` which conflicts with engine purity). PS-2
  resolved as **Option B (setup-only)** via D-3102, deferring
  per-move wiring to a follow-up WP.
- **Root cause:** Multi-lifecycle wiring decisions are inherently
  underspecified — the WP can describe the pipeline mechanics
  without committing to a single firing point, leaving the
  executor to guess. Without a locked decision, the executor
  might wire 10 moves AND invalidate existing tests, OR omit
  wiring entirely and ship an incomplete DoD. Pre-flight is the
  only stage where the trade-off can be made before
  implementation friction forces the choice.
- **Fix:** D-3102 locks Option B with explicit rationale: (a)
  preserves the 348-test baseline because no existing test that
  flows through a move runs the new pipeline; (b) keeps the
  engine environment-independent (no `process.env` reads); (c)
  the setup pathway is the highest-value single observation
  point for MVP invariants because every WP-001 through WP-026
  construction step converges there; (d) per-move wiring is
  deferred to a follow-up WP that can re-evaluate the
  performance-vs-coverage trade-off against real move
  throughput.
- **Prevention:** Established Patterns now documents "setup-only
  wiring scope as the default for runtime-wiring WPs whose
  checks could plausibly run at multiple lifecycle points."
  Future runtime-wiring WPs must lock a single firing point
  via a DECISIONS.md entry during pre-flight; per-move and
  per-transition wiring should be deferred unless the executor
  can prove the broader wiring will not regress any existing
  test. The decision is captured as PS-# during pre-flight,
  before the session prompt is generated.

**P6-19: First Runtime Wiring WP with full 9-step workflow +
mid-execution amendment + post-mortem fix (WP-031 workflow
precedent)**
- **What happened:** WP-031 was the first **Runtime Wiring**
  class WP (per 01.4 §Work Packet Class) to exercise the full
  9-step workflow end-to-end with every governance artifact in
  place AND the first to require a mid-execution amendment AND
  the first to have a post-mortem-applied in-allowlist fix.
  Workflow steps 0-8 all completed: session context loaded
  (step 0 — implicit, prior WP-030 step 8 produced
  session-context-wp031.md), pre-flight with PS-1 + PS-2
  resolved (step 1), copilot check (step 1b) HOLD → CONFIRM
  with two scope-neutral FIXes, session prompt generated with
  explicit 01.5 IS INVOKED declaration (step 2), execution in
  the same session as pre-flight via user authorization (step
  3 — same one-session deviation as WP-030), mid-execution
  PS-3 + Amendment A-031-01/02/03 + D-3103 with re-confirmed
  pre-flight + copilot check (step 3 mid-flight), full
  10-section 01.6 post-mortem with verdict WP COMPLETE and
  one in-allowlist coupling fix applied (step 4), 01.3 pre-
  commit review with verdict Safe to commit as-is and one
  non-blocking nit (step 5), single-commit form (instead of
  the WP-030 two-commit split) because WP-031 is Runtime
  Wiring not Contract-Only and the audit-trail invocation
  docs were committed atomically with the code (step 6).
  Step 7 (this lessons doc) and step 8 (session context for
  WP-032) follow.
- **Root cause:** Prior runtime-wiring WPs (e.g., WP-014, WP-015)
  were executed before the 9-step workflow + 01.5 + 01.6 +
  01.7 governance was in place. WP-031 was the first Runtime
  Wiring WP under the full governance pipeline, so it
  exercised every step for the first time. The mid-execution
  amendment + post-mortem fix demonstrated that the workflow
  is robust enough to handle discoveries at any step without
  aborting the session.
- **Fix:** None required — the workflow completed successfully.
  This entry serves as a reference for future Runtime Wiring
  WPs: follow this exact pattern. The mid-execution amendment
  procedure (P6-15) and the post-mortem in-allowlist fix
  procedure (P6-17) are now first-class operations in the
  workflow.
- **Prevention:** Future Runtime Wiring WPs should use WP-031
  as the template for a successful execution that included
  both a mid-execution amendment and a post-mortem fix. The
  governance artifacts (pre-flight, copilot check, session
  prompt, post-mortem, pre-commit review, lessons-learned)
  scale to handle these procedures without redesigning the
  workflow.

**P6-20: Incomplete canonical array as validation scope gap
(WP-032 pre-flight RS-2 discovery)**
- **What happened:** WP-032 specified `validateIntent` step 3 as
  "Check `intent.move.name` is a registered move." Pre-flight
  Dependency Contract Verification discovered that
  `CORE_MOVE_NAMES` — the only canonical move name array in the
  codebase — contains only 3 of 10 registered moves
  (`drawCards`, `playCard`, `endTurn`). The other 7 moves
  (`advanceStage`, `revealVillainCard`, `fightVillain`,
  `recruitHero`, `fightMastermind`, `setPlayerReady`,
  `startMatchIfReady`) are domain moves not in any canonical
  array. Using `CORE_MOVE_NAMES` for validation would have
  rejected 7 valid moves as `INVALID_MOVE`.
- **Root cause:** `CORE_MOVE_NAMES` was designed in WP-008A as a
  gating contract for the 3 core moves, not as an exhaustive
  registry of all moves. Domain moves (WP-014A, WP-016, etc.)
  were deliberately excluded from `CoreMoveName` to avoid
  expanding the core contract. The WP text said "registered
  move" without specifying which registry — pre-flight caught
  the ambiguity before it reached the executor.
- **Fix:** Pre-flight RS-2 locked the resolution: add
  `validMoveNames: readonly string[]` parameter to
  `validateIntent`. The caller injects the full set of valid
  move names for the current context. This is transport-agnostic
  (the validator has no knowledge of which moves exist) and
  avoids importing `game.ts` or boardgame.io. Tests supply the
  array explicitly. No new canonical constant was created.
- **Prevention:** Established Patterns now includes
  "caller-injected dependency arrays for transport-agnostic
  validation." Risk examples now include "canonical array
  incomplete for validation scope." Future pre-flights must
  verify that any canonical array used for validation actually
  contains the full set of values the validator needs to accept.

**P6-21: Clean Contract-Only execution with zero post-mortem
fixes (WP-032 workflow precedent)**
- **What happened:** WP-032 was the second Contract-Only WP
  (after WP-030) to exercise the full 9-step workflow. Unlike
  WP-030 (which had a two-commit split and a non-blocking nit)
  and WP-031 (which had a mid-execution amendment + post-mortem
  fix), WP-032 completed with zero deviations: no mid-execution
  amendments, no post-mortem fixes, no pre-commit nits, single
  commit. Pre-flight identified 3 significant risks (RS-1
  through RS-3) and resolved all before the session prompt was
  generated. The copilot check found 2 scope-neutral governance
  fixes (#4 EC signature, #13 code-category directory list)
  which were applied immediately.
- **Root cause:** WP-032's scope was narrow, well-defined, and
  free of data dependencies or G-mutation. The pre-flight
  template — enriched by WP-030 and WP-031 lessons — caught the
  one substantive issue (incomplete `CORE_MOVE_NAMES`) during
  Dependency Contract Verification, which is exactly the phase
  designed to catch it.
- **Fix:** None required. The workflow completed as designed.
- **Prevention:** This entry confirms that the pre-flight +
  copilot check pipeline is sufficient to produce zero-deviation
  executions for well-scoped Contract-Only WPs. The pipeline's
  value is front-loaded: RS-2 discovery alone prevented what
  would have been a mid-execution halt and spec amendment.

**P6-22: Docstring literal `.reduce()` matching strict grep gate —
second instance of P6-16 beyond boardgame.io (WP-033 execution
friction)**
- **What happened:** The WP-033 session prompt included
  Verification Step 5: `grep -rn "\.reduce(" packages/game-engine/src/content/`
  expecting "no output" to confirm no array `reduce` usage. The
  executor's `content.validate.ts` file header docstring read
  "Pure functions — never throw, never mutate inputs, no
  boardgame.io import, no registry import, no .reduce(). Returns
  a structured ContentValidationResult..." The literal `.reduce()`
  inside the docstring matched the binary grep gate, failing the
  verification even though no actual `Array.prototype.reduce` call
  existed in the file. The executor rephrased the docstring to
  "no use of array reduce" to satisfy the grep.
- **Root cause:** P6-16 documented this pattern for the
  `boardgame.io` grep gate in WP-031. WP-033 is the second
  instance of the same pattern against a different forbidden
  token (`.reduce(`). The "Audit grep patterns must escape regex
  specials when used as binary 'no output' gates" Established
  Pattern covers the regex-special part, but the WP-033 case
  shows the issue is broader: any docstring or comment that
  mentions the forbidden pattern in rationale text trips the
  strict gate regardless of whether the pattern is regex-special.
  The underlying root cause is that the binary "no output" gate
  cannot distinguish "code contains the forbidden call" from
  "documentation mentions the forbidden token."
- **Fix:** Rephrase the docstring so the literal token does not
  appear as substring. `.reduce()` → "array reduce".
  `boardgame.io` → "the game framework" (WP-031 precedent).
  Behavior preserved; documentation slightly weakened.
- **Prevention:** The existing Established Pattern "Audit grep
  patterns must escape regex specials when used as binary 'no
  output' gates" is now confirmed to apply to ALL forbidden-token
  grep gates, not just `boardgame.io`. Future WPs authoring
  content under a strict grep gate must avoid writing the literal
  forbidden token anywhere in the file — including in docstrings
  and rationale comments. The Established Pattern entry is
  reinforced by this second precedent; no new pattern added.
  Future pre-flights should consider proactively adding a
  "docstring hygiene check: forbidden tokens must not appear as
  literal substrings anywhere in the new files" note to the
  session prompt's §Hard Stops when the WP declares a strict
  binary grep gate.

**P6-23: Copilot-surfaced accept-list gate retrofitted to WP
scope during pre-flight, test-count-neutral (WP-033 Pre-Flight
Locked Decisions #10 / copilot RISK #10/#21)**
- **What happened:** WP-033 §B originally specified
  `validateContent(content: unknown, contentType: string,
  context?: ContentValidationContext): ContentValidationResult`
  with no explicit behavior for unrecognized `contentType`
  values. A free-form `contentType: string` without an accept-list
  would have let authoring typos (e.g., `"Hero"` with
  capitalization, `"heroes"` pluralized, `""` empty, any misspell)
  silently pass as "valid" content when the validator's internal
  `switch (contentType)` dispatch fell through to the default
  branch. The copilot check surfaced this as RISK #10 and RISK
  #21 during pre-flight review. Pre-flight locked the resolution
  in §Pre-Flight Locked Decisions #10: introduce
  `ACCEPTED_CONTENT_TYPES` as an internal 6-member constant, add
  a Stage 0 accept-list check that short-circuits the remaining
  stages when the type is unrecognized, and return a single
  full-sentence error with `field: 'contentType'`,
  `contentId: ''`, and the as-supplied value captured verbatim.
  The original 9-test count was preserved by extending test 9 to
  cover Part A (full-sentence pattern), Part B (unknown-
  contentType shape), and Part C (empty + pluralized accept-list
  strictness) — three assertion groups inside a single test
  rather than a 10th test.
- **Root cause:** The original WP §B signature treated
  `contentType` as a free-form string because the WP author
  assumed callers would only pass the six valid strings. But
  silent-pass-on-unrecognized-type is a well-known failure mode
  for validators with string-discriminated dispatch (equivalent
  to "default case falls through"), and authoring tools are
  exactly the callers most likely to produce typos. The copilot
  scan identified the gap; without the scan, the validator
  would have shipped with a silent-pass default branch.
- **Fix:** Pre-flight §Pre-Flight Locked Decisions #10 locked
  the full error shape (contentType, contentId, field, message
  template), the accept-list constant, the Stage 0 ordering, the
  batch behavior (record error for unknown item, continue other
  items), the required `// why:` comment citing the copilot RISK
  resolution, and the test coverage extension (one extended test
  instead of a new test). The WP §E test list was held at 9 to
  keep the locked 376/96/0 baseline intact. Execution applied
  the locks exactly; test 9 has Parts A/B/C covering all three
  concerns.
- **Prevention:** Established Patterns now documents
  "copilot-surfaced behavioral lock retrofitted to WP scope
  during pre-flight" as a canonical operation: when a copilot
  check identifies a silent-pass failure mode, the pre-flight
  may lock a corrective behavior (accept-list, reject-default,
  explicit error shape) in the §Pre-Flight Locked Decisions
  section AND extend existing tests rather than adding new ones,
  preserving the locked test count baseline. Risk examples now
  include "validator with string-discriminated dispatch falls
  through on typo / casing variant / pluralization / empty
  string." Future validator WPs must explicitly specify
  unknown-input behavior — silent pass is forbidden. The
  accept-list pattern is the default resolution.

**P6-24: Author-facing schemas stricter than permissive
data-loader schemas, without importing the data-loader layer
(WP-033 RS-1 / RS-7 / RS-9 / D-3303 architectural pattern)**
- **What happened:** WP-033 introduced author-facing content
  validation schemas
  (`packages/game-engine/src/content/content.schemas.ts`) that
  deliberately require fields (e.g., hero `name`, `team`, `hc`)
  which the registry's Zod schemas
  (`packages/registry/src/schema.ts`) treat as optional to
  accommodate real-data quirks (D-1204 / D-1227). Pre-flight
  RS-1 locked the stricter required-field lists and forbade
  any relaxation to match registry permissiveness. RS-7 / D-3302
  mirrored `VillainCardSchema`'s required-field shape locally
  for the henchman schema (registry declares `henchmen:
  z.array(z.unknown())` with no typed shape). RS-9 re-declared
  `HERO_CLASSES` locally rather than importing
  `HeroClassSchema` from the registry, because engine-category
  code (D-3301) must not import the registry package.
- **Root cause:** The registry Zod schemas and author-facing
  content schemas answer different questions and cannot share
  shape without losing information. Registry schemas validate
  *existing loaded data* (40 sets of historically-shipped cards
  with documented quirks: `anni` cards with only slug+imageUrl,
  `amwp` Wasp with `cost: "2*"`, nullable `vp` / `vAttack`
  fields). Content validation validates *new content an author
  is writing*. Merging the two layers would either force the
  loader to reject shipped data, or let author typos through
  silently. Importing the registry from the engine category is
  forbidden by layer boundaries (D-3301) — so the engine must
  re-declare the needed enums and shape constants locally,
  duplicating them rather than sharing.
- **Fix:** D-3303 captures the rationale as an architectural
  decision: "Content Validation Is Author-Facing and Separate
  from Registry Zod Schemas." The four validation stages
  (structural / enum / cross-reference / hook consistency) are
  enumerated; the caller-injected
  `ContentValidationContext` pattern is cited as the solution
  for cross-reference checks without crossing the layer
  boundary. `HERO_CLASSES` is re-declared locally with a
  `// why:` comment citing RS-9 / D-3301. Henchman shape is
  re-declared locally with a `// why:` citing D-3302 /
  D-1410..D-1413.
- **Prevention:** Established Patterns now documents
  "author-facing validation schemas are separate from, and
  deliberately stricter than, permissive data-loader schemas"
  as a canonical pattern for validation-layer WPs. Apply when:
  (a) a loader layer must accept real data with documented
  quirks, AND (b) an authoring workflow must reject typos that
  slip past the loader's permissiveness, AND (c) the validator
  lives in a layer that cannot import the loader. Duplicate
  enums and required-field lists locally via
  `// why: <layer> category must not import from <loader>`
  comments; cite the relevant decision IDs for the duplication.
  Future content-authoring WPs (new hero / villain / mastermind
  authoring specs) should tighten per-type schemas without
  importing the registry and without modifying registry schemas
  in place.

**P6-25: Third clean Contract-Only execution + fourth
consecutive code-category PS-# precedent (WP-033 workflow
precedent)**
- **What happened:** WP-033 was the third Contract-Only WP
  (after WP-030 and WP-032) to exercise the full 9-step workflow
  with zero mid-execution amendments. Pre-flight identified 9
  significant risks (RS-1 through RS-9) plus one copilot-sourced
  accept-list lock, resolved all before the session prompt was
  generated. The copilot check surfaced two scope-neutral
  governance fixes (PS-1 code-category classification via
  D-3301; PS-2 signature back-sync to the three-parameter
  `validateContent`) which were applied pre-execution. Execution
  produced the locked 376/96/0 baseline on the first try; the
  only mid-execution fixes were two scope-neutral refinements
  (the P6-22 docstring rephrasing and a
  `strictNoUncheckedIndexedAccess` guard in the enum stage) —
  both confined to files already in the allowlist. Post-mortem
  verdict: WP COMPLETE with no applied fixes. Pre-commit review
  verdict: Safe to commit as-is with no nits blocking. Single
  commit (`EC-033:` prefix) containing all WP-033 artifacts —
  no SPEC/EC split required because the governance docs were
  authored alongside the code.
- **Root cause:** WP-033's scope was narrow (three new files
  plus two additive re-export edits), well-defined (9 locked
  RS resolutions, no structural G changes, no lifecycle
  wiring), and free of data dependencies (no registry access at
  all — the pre-engine-gate design made layer-boundary
  compliance trivial). The pre-flight template — enriched by
  WP-030, WP-031, and WP-032 lessons — caught the two
  substantive issues (content category classification, accept-
  list gate) during normal review phases. The author-facing-vs-
  registry split (P6-24) was resolved architecturally by
  D-3303 before any code was written.
- **Fix:** None required. The workflow completed as designed.
  This is also the fourth confirmed instance of the
  code-category PS-# pattern (D-3001 for campaign/, D-3101 for
  invariants/, D-3201 for network/, D-3301 for content/) —
  establishing the pattern as canonical for any Contract-Only
  WP that introduces a new engine subdirectory.
- **Prevention:** This entry confirms that the pre-flight +
  copilot check pipeline has reached steady-state for
  well-scoped Contract-Only WPs: three consecutive zero-
  deviation executions (WP-030 had a two-commit split and one
  pre-commit nit; WP-032 and WP-033 had neither). The
  code-category PS-# pattern is now locked — future WPs
  introducing new `packages/game-engine/src/<dir>/`
  subdirectories must produce a `D-XX01` "Directory Classified
  as Engine Code Category" entry during pre-flight without
  exception. The pre-flight §Code Category Boundary Check
  should treat the absence of such an entry as an automatic
  PS-# blocking finding.

### Phase 6 continued — Shared Tooling Introduction (WP-065)

**P6-26: Code-category PS-# pattern extends to `packages/*`
(not just `packages/game-engine/src/*`) — first Shared Tooling
package (WP-065 pre-flight)**
- **What happened:** WP-065 introduced `packages/vue-sfc-loader/`
  as a new top-level workspace package — not a
  `packages/game-engine/src/*` subdirectory. Pre-flight §Code
  Category Boundary Check flagged the absence of a category row
  in `02-CODE-CATEGORIES.md` and a corresponding DECISIONS.md
  entry. PS-1 was resolved pre-session by creating D-6501
  "Shared Tooling classification for packages/vue-sfc-loader/"
  and adding a `shared-tooling` row to
  `02-CODE-CATEGORIES.md §Category Summary` (line 46) and
  §Category Definitions (lines 221–231). The pre-flight
  verdict then flipped to unconditional READY TO EXECUTE.
- **Root cause:** The code-category PS-# pattern was originally
  defined against `packages/game-engine/src/<dir>/` (replay,
  ui, campaign, invariants, network, content — D-2706, D-2801,
  D-3001, D-3101, D-3201, D-3301). A new top-level `packages/*`
  workspace entry had not been seen before, and it was unclear
  whether the pattern applied to directories outside the engine
  `src/` tree.
- **Fix:** PS-1 resolved the category gap with D-6501 and the
  matching `02-CODE-CATEGORIES.md` row. The pattern is now
  confirmed to apply at **workspace-package granularity** as
  well as engine-subdirectory granularity — six engine-
  subdirectory precedents plus one workspace-package precedent
  (D-6501) establish the rule: any new code-bearing directory
  that is not covered by an existing category row requires a
  PS-# DECISIONS.md entry and a matching `02-CODE-CATEGORIES.md`
  update before execution.
- **Prevention:** Established Patterns now documents
  "code-category PS-# pattern at package granularity" with the
  expanded scope. Apply whenever **any** new code-bearing
  directory appears in the diff — `packages/<name>/`,
  `packages/game-engine/src/<dir>/`, `apps/<name>/`, etc. The
  pre-flight §Code Category Boundary Check should walk the
  full proposed file list, identify directories not matching
  an existing category row, and flag each as a blocking PS-#.

**P6-27: Post-mortem reclassifies "minor additive deviation"
as scope violation (WP-065 post-mortem)**
- **What happened:** During execution, WP-065 added a 10th
  file — `packages/vue-sfc-loader/src/index.ts` — plus two
  extra exports map subpaths (`.` and `./loader`) and a
  "Programmatic API" section in `README.md`. The execution
  summary acknowledged these as a "minor additive deviation"
  that the gatekeeper could consider out of scope if desired,
  but did not remove them. The post-mortem (01.6 §2 Scope &
  Allowlist Audit) correctly re-classified the extras as a
  scope violation — the WP's "No other files may be modified"
  clause is absolute, not a suggestion — and applied the fix
  in-session: deleted `src/index.ts`, stripped the two extra
  subpaths from the exports map, removed the Programmatic API
  README section, corrected STATUS.md to match the 9-file
  allowlist, and re-ran build + typecheck + test to confirm
  no regressions.
- **Root cause:** The executing agent confused "ergonomically
  convenient" with "in scope". The WP explicitly listed 9
  files; the 10th (and its two downstream exports-map entries)
  were convenience-driven. The execution summary then
  minimized the gap as "minor" rather than flagging it as a
  blocker — relying on the gatekeeper to decide. This places
  the scope-enforcement burden on the reviewer instead of the
  implementer, inverting the governance contract.
- **Fix:** Post-mortem §10 records the fix under "Fixes
  applied during post-mortem." Delivered file set now matches
  the 9-file WP-065 allowlist exactly. No tests were affected
  by the fix (the removed code was not consumed by any test).
- **Prevention:** Established Patterns now documents "any file
  beyond the explicit `## Files Expected to Change` allowlist
  is a scope violation, not a minor deviation." The execution
  summary's §Deviations section must not be used to
  ergonomically launder out-of-scope files into the diff —
  deviations are for noting when reality diverged from the
  *Implementation Order*, not for retroactively authorizing
  extra files. If a 10th file genuinely seems required, stop
  and escalate to a pre-flight amendment (WP-031 A-031-01
  precedent) rather than shipping it and asking forgiveness.

**P6-28: 01.6 mandatoriness rules override session-prompt
"recommended" softening language (WP-065 post-mortem)**
- **What happened:** WP-065's session prompt said step 4
  (post-mortem) was "recommended rather than strictly
  mandatory" for this Infrastructure & Verification WP. The
  executing agent initially took that at face value and
  skipped to the commit workflow. The user's direct prompt
  ("did you run 01.6 on WP-065") re-surfaced the step. On
  reading 01.6 §When Post-Mortem Is Required fresh, three
  mandatoriness criteria clearly applied — new contract
  consumed by future WPs (the `compileVue` signature, the
  `NODE_OPTIONS` composition, the DEBUG line format), new
  long-lived abstraction (the Shared Tooling extension seam),
  and new code category directory (`packages/vue-sfc-loader/`
  as the first Shared Tooling entry). The session prompt's
  softer language was in tension with 01.6's own rules.
- **Root cause:** Session prompts can tailor per-WP tone but
  cannot override higher-authority REFERENCE documents. 01.6
  is subordinate only to `.claude/CLAUDE.md`, ARCHITECTURE,
  and EC-TEMPLATE — session prompts are downstream of all
  three and therefore cannot soften 01.6's mandatoriness
  rules. The tension went unflagged because 01.6 itself does
  not state its authority relative to session prompts
  explicitly.
- **Fix:** Post-mortem ran after the user prompt; findings
  were recorded; the scope-violation fix from P6-27 was
  applied during that run. The lesson is twofold: (a) when
  session-prompt language softens a REFERENCE document's
  rule, treat the REFERENCE document as authoritative and run
  the gate; (b) the prompt author who wrote "recommended
  rather than strictly mandatory" for WP-065 should have
  instead added a short justification of why the criteria in
  01.6 §When Post-Mortem Is Required were not triggered, or
  omitted the softening language entirely.
- **Prevention:** Pre-flight should treat any session-prompt
  phrasing that softens a REFERENCE document's gate as a
  blocking finding unless the pre-flight explicitly
  enumerates why the gate is not triggered. Established
  Patterns now documents "REFERENCE mandatoriness wins over
  session-prompt softening language" — if 01.6's criteria
  apply, the post-mortem runs, regardless of how the session
  prompt frames it. A companion addition in 01.6 would state
  its subordination chain relative to session prompts
  explicitly; proposed as a follow-up (non-blocking for
  WP-065).

**P6-29: Execution-time dependency-behavior smoke test for
version-sensitive external packages (WP-065 Locked Decision 7)**
- **What happened:** WP-065's `<script lang="ts">` smoke test
  could not be run during pre-flight because it required the
  new package's `@vue/compiler-sfc` dependency to be installed
  first — and that install happens as the first execution
  step, not before. Pre-flight therefore deferred the smoke
  test to execution and locked **both branches** (Outcome A =
  plain JS already emitted / Outcome B = TS remains and
  internal `transpileModule` pass required) as valid, with
  the DECISIONS.md entry to be written based on the actual
  observed outcome. Execution found Outcome B at
  `@vue/compiler-sfc` 3.5.30 (satisfying the `^3.4.27` pin)
  and recorded it as D-6506. The `typescript` dependency that
  the WP spec tentatively listed as "added only if Outcome B"
  was therefore retained.
- **Root cause:** Some dependency-behavior questions cannot
  be answered by reading release notes or API docs — they
  require empirically running the dependency in the target
  shape. When the dependency is not yet installed (because
  the package being built is the one installing it), pre-
  flight cannot answer the question without simulating the
  install, which is slower and more error-prone than
  deferring to execution.
- **Fix:** No code fix. Locked Decision 7 worked as designed.
  The branching spec (Outcome A vs Outcome B) plus the
  DECISIONS.md entry captures the actual outcome verifiably.
- **Prevention:** Established Patterns now documents
  "execution-time empirical smoke test for version-sensitive
  external dependencies" as a canonical pattern. Apply when:
  (a) the WP introduces a new external dependency whose
  behavior at the pinned version cannot be confirmed from
  documentation alone, AND (b) the dependency is not already
  installed in the current workspace. Lock both branches in
  the session prompt with explicit Outcome A / Outcome B
  specifications, defer the smoke test to the first execution
  step, and require the resolved outcome to be recorded as a
  DECISIONS.md entry during execution. Never run the smoke
  test as a throwaway-and-forget script — the DECISIONS.md
  entry is the audit trail that future maintainers need when
  the dependency version drifts.

### Phase 6 continued — Gameplay Client Bootstrap (WP-061)

**P6-30: `<script setup>` + vue-sfc-loader separate-compile
incompatibility for template-binding of setup-scope refs (WP-061
execution)**
- **What happened:** `apps/arena-client/src/components/BootstrapProbe.vue`
  was initially authored with `<script setup lang="ts">` per WP-061
  §Scope (In) D. Typecheck and build passed; component tests failed
  at mount with `TypeError: Cannot read properties of undefined
  (reading 'game')`, accompanied by Vue's `Property "snapshot" was
  accessed during render but is not defined on instance` warning.
  Root cause: WP-065's `vue-sfc-loader` calls
  `@vue/compiler-sfc.compileScript` with `inlineTemplate: false`,
  then `compileTemplate` as a separate pass. In that split pipeline,
  the template's render function receives `_ctx` (the instance
  proxy), and `<script setup>` top-level bindings — refs, reactive
  objects, `storeToRefs` outputs — are NOT auto-exposed on `_ctx`.
  The auto-exposure only happens when `inlineTemplate: true` is
  used (Vite's `@vitejs/plugin-vue` does this). Props ARE exposed
  via the instance's `$props`, which is why WP-065's `hello.vue`
  fixture works — it uses `defineProps`. Resolved by rewriting the
  component in explicit `defineComponent({ setup() { return {...} } })`
  form, which places bindings on the returned object so they reach
  `_ctx`. Recorded as D-6512.
- **Root cause:** vue-sfc-loader's separate-compile pipeline is a
  deliberate WP-065 design choice (locked per D-6506), and
  bindings metadata is not threaded from `compileScript` to
  `compileTemplate`. This is not a vue-sfc-loader bug — it is a
  documented limitation of the split pipeline. The WP-061 session
  prompt did not anticipate it because WP-065 shipped only a
  props-using fixture.
- **Fix:** No vue-sfc-loader modification (explicitly forbidden by
  EC-067 scope lock). Component authoring switched to
  `defineComponent` + explicit `setup()` return. App.vue keeps
  `<script setup>` because it is only ever processed by Vite's
  `@vitejs/plugin-vue`, never by the test loader.
- **Prevention:** Established Patterns now states: **for any `.vue`
  component that ships with a `node:test` test AND uses setup-scope
  bindings (refs, reactive, `storeToRefs`, etc.) in its template,
  use `defineComponent({ setup() { return {...} } })` rather than
  `<script setup>`. Components that use only `defineProps` (no
  setup-scope bindings in the template) may continue to use
  `<script setup>`. Non-tested components (e.g., root `App.vue`
  that is only mounted in the browser) may continue to use
  `<script setup>`. WP-062, WP-064, and every future UI packet
  consuming `vue-sfc-loader/register` must apply this pattern.
  A pre-flight check for the next UI WP should verify no
  `<script setup>` component with setup-scope template bindings
  is being tested under `node:test`.

**P6-31: Sourcemap carve-out for DCE-marker verification when
`build.sourcemap: true` is enabled (WP-061 execution)**
- **What happened:** WP-061's Step 12 verification required
  `Select-String -Path apps/arena-client/dist -Pattern
  __WP061_DEV_FIXTURE_HARNESS__ -Recurse` to return no matches,
  proving the dev `?fixture=` branch is dead-code eliminated from
  production. The executing bundle (`dist/assets/index-*.js`)
  contained zero matches — DCE worked. But `dist/assets/index-*.js.map`
  contained one match, because Vite's sourcemap emits the original
  source content (including string literals that were inside
  DCE'd branches) into `sourcesContent`. The sourcemap preserves
  the original source by construction; that is its purpose. DCE
  operates on the AST / generated code, not on the sourcemap's
  source-content array. Resolved as D-6513: the Step 12
  verification is scoped to executing JS (`dist/assets/*.js`),
  not the whole `dist/` tree.
- **Root cause:** Simultaneous requirements in WP-061 — (a)
  `build.sourcemap: true` for first-bootstrap diagnosability and
  (b) zero marker occurrences anywhere under `dist/` — are
  literally incompatible. Either the sourcemap is stripped from
  production (losing diagnosability) or the marker survives in
  the sourcemap (which is what the verification grep picks up).
  The behavioral invariant ("dev branch does not execute in
  production") is satisfied by the first condition alone; the
  grep recursing into `.js.map` is an over-strict encoding of
  that invariant.
- **Fix:** No code fix. DECISIONS.md entry D-6513 clarifies the
  verification scope. Future WPs that use DCE markers should
  scope the grep target to executing JS (`dist/assets/*.js`,
  or equivalent per build tool) from the outset rather than
  recursing into `dist/**`.
- **Prevention:** Established Patterns now states: **DCE-marker
  absence checks in WPs that enable sourcemaps must target the
  executing bundle extension only (`*.js` for Vite/Rollup/esbuild
  output). Do NOT recurse into `*.map` — sourcemaps preserve
  original source by design.** Apply when a WP ships a DCE marker
  inside a `build.sourcemap: true` config. Encode the scope in
  the session prompt's verification step command from the start
  rather than discovering it during execution.

**P6-32: DECISIONS.md ↔ 02-CODE-CATEGORIES.md asymmetry must be
checked on BOTH sides during pre-flight (WP-061 pre-session
Gate #2)**
- **What happened:** The pre-flight for WP-061 correctly
  identified the P6-26 code-category gate and produced D-6511
  ("Client App classification for apps/arena-client/") as the
  PS-1 resolution, recording it in DECISIONS.md. However, the
  matching row in `02-CODE-CATEGORIES.md` was NOT added at the
  same time. Execution-side Gate #2 caught this: D-6511
  asserted "belongs to the Client App category in
  `docs/ai/REFERENCE/02-CODE-CATEGORIES.md`", but that category
  did not yet exist as a row in the Category Summary table nor
  as a definition section. The two governance documents were
  asymmetric — a referential integrity violation. Execution-time
  resolution added the missing row and definition during
  pre-session Gate #2, before scaffolding any file under the
  new package.
- **Root cause:** P6-26 (new workspace-package category
  classification) was documented, but the pre-flight template
  did not mandate that BOTH artifacts (DECISIONS.md entry AND
  02-CODE-CATEGORIES.md row/definition) exist before execution
  begins. A one-sided PS-1 resolution is possible and was the
  failure mode here.
- **Fix:** No code fix. Row and definition section added to
  02-CODE-CATEGORIES.md as part of WP-061 scope (explicitly
  allowlisted in EC-067 for exactly this case).
- **Prevention:** Established Patterns now states: **when a
  pre-flight resolves a new workspace-package category as a
  PS-#, BOTH artifacts must be produced in the same pre-flight
  cycle — the `D-NNNN` entry in DECISIONS.md AND the matching
  row + definition in 02-CODE-CATEGORIES.md. A pre-flight that
  produces only the DECISIONS.md side leaves a governance
  referential-integrity hole that the session prompt's Gate
  must catch (as WP-061 did). The safer default is to produce
  both in pre-flight so the session prompt's Gate becomes a
  PASS on arrival rather than a mid-session edit.** Extends
  P6-26 with a two-sided completeness requirement. Applies to
  any future `apps/*` or `packages/*` addition requiring a new
  category.

### Phase 6 continued — UIState PAR Projection (WP-067)

**P6-33: Required-field promotion on a public projection type
forces a literal-constructor audit during pre-flight (WP-067
execution)**
- **What happened:** The WP-067 allowlist correctly included
  `packages/game-engine/src/ui/uiState.types.ts` (where
  `progress` was added as a required field on `UIState`),
  `packages/game-engine/src/ui/uiState.build.ts` (the
  projection producer), and the three WP-061 fixture JSONs
  (which needed the new key to satisfy `satisfies UIState`).
  It missed `packages/game-engine/src/ui/uiState.filter.ts:137`,
  which constructs a `UIState` literal via
  `const result: UIState = { game: {...}, ..., log: [...] }`.
  The TypeScript compile broke during implementation step 6
  with "Property 'progress' is missing in type". Resolved
  during execution via user-authorized in-session escalation:
  a single `progress: { ...uiState.progress }` passthrough
  line plus a `// why:` comment was added to the filter.
  Recorded as a forced cascade in STATUS.md.
- **Root cause:** When a field is promoted to required (or
  added as required) on a public projection / view-model / DTO
  type, every `: <ProjectionType> = { ... }` literal
  constructor in the codebase becomes a compile blocker. The
  pre-flight enumerated the producer site (`uiState.build.ts`)
  and the consumer sites (fixture JSONs) but missed
  intermediate sites that construct the projection type as a
  literal for transformation purposes. The filter's
  literal-constructor pattern is structurally identical to the
  build's but was authored under WP-029 and not co-located
  with the projection type.
- **Fix:** No code-shape change beyond a single passthrough
  line; user-authorized via AskUserQuestion. The cascade fix
  is behaviorally trivial (shallow spread copy, no logic) and
  was approved under the runtime wiring allowance criteria
  (structurally required by new contract; no new gameplay or
  branching logic; limited to type wiring).
- **Prevention:** Established Patterns now states: **for any
  pre-flight that promotes a field to required (or adds a new
  required field) on a public projection / view-model / DTO
  type, run a literal-constructor sweep BEFORE locking the
  allowlist:**
  ```pwsh
  Select-String -Path packages, apps -Pattern ": <ProjectionType>\s*=\s*\{" -Recurse
  ```
  **Every match is a literal constructor that the compiler
  will reject; each must be added to the allowlist explicitly
  with a single-line passthrough as the authorized scope, with
  a `// why:` comment naming the forced cascade. This is a
  type-system wiring concern distinct from 01.5 runtime
  wiring; the pre-commit review approves it under "structurally
  required by new types or contracts" rather than under 01.5.
  The pre-flight Mutation Boundary section should add a
  "literal-constructor sweep" check whenever a required field
  is added to a public projection type.** Applies to any
  future WP that adds required fields to `UIState`,
  `UIPlayerState`, `UIGameOverState`, or any other top-level
  projection / DTO type.

**P6-34: Pre-flight READY verdict must verify pre-flight edits
are committed, not just applied to the working tree (WP-067
execution)**
- **What happened:** WP-067's invocation prompt asserted
  "Base commit is `ac7cb26` ... The pre-session governance
  batch from pre-flight PS-1 through PS-4 + recs #5–#6 has
  already landed separately during the pre-flight session
  (D-6701 / D-6702 / D-6703 added, EC-068 + WP-067 edits
  applied, 03.1-DATA-SOURCES.md row added)." The Step 16
  verification expected `git diff ac7cb26 --name-only` to NOT
  include DECISIONS.md or 03.1-DATA-SOURCES.md (with the
  explicit text "they were committed during pre-flight. If
  they appear, you modified them accidentally — revert those
  edits before committing"). But at session start, those four
  files (DECISIONS.md, EC-068, WP-067, 03.1-DATA-SOURCES.md)
  were unstaged modifications in the working tree — the
  pre-flight had applied the edits but had not committed them.
  This contradiction blocked the canonical "diff matches
  14-file allowlist" gate; resolved by user-authorized
  commit-split: a separate `SPEC: WP-067 pre-flight +
  D-6701/D-6702/D-6703 + EC-068 + 03.1 row` commit (`9a9c3cf`)
  before the `EC-068:` execution commit (`1d709e5`).
- **Root cause:** The pre-flight READY verdict criteria do
  not require a `git log -1 --stat` confirmation that
  pre-flight edits are committed. The pre-flight document and
  invocation prompt both used the verb "landed" loosely,
  conflating "applied to the working tree" with "committed
  to git history". The author of the WP-067 invocation
  correctly listed the four pre-flight files as "out of
  allowlist" assuming they were already committed; in reality
  they were sitting in the working tree at session start.
- **Fix:** Two-commit sequence per user authorization. SPEC
  commit carries the four pre-flight governance files
  separately; EC-068 commit carries the 14 execution files.
  STATUS.md notes the split.
- **Prevention:** Established Patterns now states: **a
  pre-flight READY verdict is conditional on
  `git status --short` showing zero staged or unstaged
  modifications to pre-flight governance files (DECISIONS.md,
  EC-NNN-*.checklist.md, WP-NNN-*.md, 03.1-DATA-SOURCES.md,
  any REFERENCE doc updated during pre-flight) at the moment
  READY is printed. If any pre-flight edits are uncommitted,
  the pre-flight is NOT READY — it has been APPLIED but NOT
  LANDED. The pre-flight closing step must add a "git status
  verification" check immediately before printing the READY
  verdict, and the invocation prompt must distinguish
  "applied to working tree" from "landed in git history" and
  never use "landed" as a synonym for "applied".** The
  cleanest pre-flight workflow is: (a) apply governance edits,
  (b) commit them under a `SPEC:` prefix, (c) print READY
  with the SPEC commit hash as the new base. The invocation
  prompt then references the SPEC commit hash, not the
  pre-pre-flight base. Applies to every pre-flight that
  produces governance edits (D-NNNN entries, EC updates, WP
  updates, REFERENCE doc updates, code-categories updates).

**P6-35: 01.6 post-mortem must run before commit (step 4 before
step 6), not after (WP-067 execution)**
- **What happened:** The WP-067 implementation completed,
  the in-line execution summary mentioned an informal
  post-mortem audit (aliasing trace, off-allowlist audit,
  D-6701 safe-skip verification, `// why:` comment check),
  then both governance and execution commits landed — only
  AFTER the user asked "Did you run 01.6?" was the formal
  01.6 checklist executed retroactively. The 01.4 §Workflow
  Position table places step 4 (post-mortem) explicitly
  between EC-acceptance (step 3) and pre-commit review
  (step 5) / commit (step 6). Running 01.6 retroactively
  means any issues the checklist surfaces cannot be fixed
  before the commit lands; in WP-067's case the formal
  checklist confirmed WP COMPLETE with zero fixes needed, so
  the procedural miss was harmless this time, but it would
  have been a real problem if the checklist had surfaced an
  aliasing bug or a boundary violation.
- **Root cause:** The session prompt did call out 01.6 as
  MANDATORY (the WP-067 invocation said "Run post-mortem
  (01.6) in-session" and placed it as step 12 between Step 11
  governance updates and Step 13 commit). The executing
  session jumped from step 11 to step 13 without doing step
  12. The informal in-line summary was treated as
  satisfaction of the 01.6 requirement rather than as the
  preliminary observation it actually was. The full 01.6
  checklist (10 sections including binary health, scope &
  allowlist audit, boundary integrity, representation &
  determinism, mutation/aliasing, hidden coupling, test
  adequacy, documentation, forward-safety, final verdict) was
  not opened or worked through.
- **Fix:** 01.6 was executed retroactively at user request.
  All 10 sections passed; no code fixes were applied during
  post-mortem. Procedural miss noted in the post-mortem Notes
  section. Both commits stand.
- **Prevention:** Established Patterns now states: **whenever
  a session prompt's implementation order includes 01.6
  post-mortem as a numbered step before commit, that step
  must be encoded as a literal STOP gate
  ("DO NOT PROCEED TO COMMIT WITHOUT OPENING AND COMPLETING
  01.6"), and the executing session must produce the formal
  10-section 01.6 output before the commit step is invoked.
  An informal in-line summary in the chat transcript is NOT
  a substitute for the formal checklist — the formal
  checklist's Section 5 (Mutation, Aliasing, & Reference
  Safety, marked HIGH-RISK) requires line-by-line return
  trace inspection that the informal summary will skip.** The
  session prompt should also restate the workflow ordering
  explicitly: "01.6 (step 4) BEFORE pre-commit review
  (step 5) BEFORE commit (step 6) — no exceptions." Applies
  to every WP whose 01.6 is mandatory per 01.6 §When
  Post-Mortem Is Required.

### Phase 6 continued — Preferences Foundation (WP-068)

**P6-36: Session-context must never offer `WP-###:` as a valid
commit prefix; only `EC-###:` / `SPEC:` / `INFRA:` survive the
commit-msg hook (WP-068 execution)**
- **What happened:** `session-context-wp068.md` Step 7 presented
  two commit-prefix options: Option A (draft a new EC file, use
  `EC-070:`) or Option B (execute without a dedicated EC, use
  `WP-068:`). Option B was framed as the "lightweight pattern for
  registry-viewer-only work". The executing session chose Option
  B and attempted `git commit -m "WP-068: preferences foundation …"`.
  The `.githooks/commit-msg` hook rejected it immediately:
  "Subject does not match an allowed prefix. Allowed formats:
  EC-###:, SPEC:, INFRA:." Resolution required pivoting mid-session
  to Option A: drafting a minimum-ceremony
  `EC-070-preferences-foundation.checklist.md` alongside the execution
  commit and re-committing under `EC-070:`.
- **Root cause:** The session-context author referred to 01.3 by
  name ("Follow `docs/ai/REFERENCE/01.3-commit-hygiene-under-ec-mode.md`
  either way") but did not read its enforced prefix list. 01.3 is
  unambiguous: code changes under `packages/` or `apps/` MUST use
  `EC-###:`. There is no lightweight "WP-###:" escape hatch; the
  hook will reject it and the CI mirror will reject it on PR. The
  session-context was drafted from a general understanding of
  "commit prefix precedent" rather than from 01.3's actual rules.
- **Fix:** Drafted EC-070 checklist alongside execution; committed
  under `EC-070:` prefix; EC file exists at the commit hash.
- **Prevention:** Established Patterns now states: **every
  session-context file, pre-flight document, or invocation prompt
  that discusses commit prefixes MUST either (a) quote 01.3's
  three-prefix list verbatim (`EC-###:` / `SPEC:` / `INFRA:`) with
  no fourth option, or (b) cite 01.3 without enumerating options.
  `WP-###:` is never a valid code-commit prefix — the hook will
  reject it. If a WP's scope is small enough that a dedicated EC
  feels like overhead, the author must still draft a minimum-ceremony
  EC (Before Starting / Locked Values / Guardrails / Files to
  Produce / Verification / After Completing — seven sections;
  "Required Comments" and "Common Failure Smells" may be omitted
  when the WP body covers them), not invent a prefix. The EC's
  overhead is the price of the commit-hygiene contract, not an
  optional ceremony.** Applies to every session-context and
  invocation prompt authored for future WPs.

**P6-37: Test-infrastructure additions are implicit in scope when
AC requires a test-verification command to exit 0 (WP-068
execution)**
- **What happened:** WP-068 Scope §A said
  "`apps/registry-viewer/package.json` — modified: Add `pinia ^2.1.7`
  to dependencies. No other changes to package.json." Acceptance
  Criteria §G required `pnpm --filter registry-viewer test` to
  exit 0 and cited "viewer already uses `.test.ts` per WP-065
  groundwork". But `apps/registry-viewer/package.json` had no
  `test` script and no `tsx` devDependency — the viewer had never
  run tests under `node:test`. `pnpm --filter registry-viewer test`
  would exit non-zero (missing script) on a strict literal reading
  of §A. The executing session resolved by interpreting AC §A's
  wording "`dependencies` block contains `pinia` and nothing else
  new" as scoped to the `dependencies` block alone, leaving `scripts`
  and `devDependencies` untouched by that restriction, and added a
  `test` script plus `tsx` devDep. The addition was called out as
  a documented deviation in STATUS.md and in the commit body.
- **Root cause:** The WP author transcribed §A's restriction from
  a prior packet template that assumed test infrastructure was
  already in place. For the registry-viewer, that assumption was
  incorrect — session-context-wp068 §"Repo Test Baseline" explicitly
  noted "registry-viewer: 0 — WP-068 is the first packet adding
  viewer tests". The first-test-in-an-app case was not anticipated
  by the packet's scope lock wording, producing a contradiction
  between §A (literal) and §G (verification).
- **Fix:** Added `"test": "node --import tsx --test \"src/**/*.test.ts\""`
  to `scripts` and `"tsx": "^4.15.7"` to `devDependencies`.
  Documented as a scope deviation in STATUS.md, the commit body,
  and EC-070's Files to Produce. Interpretation: AC §A's "nothing
  else new" constraint applies to the `dependencies` block only.
- **Prevention:** Pre-flight §Structural Readiness Check should
  add: **when a WP is the first to add tests to an app or package,
  enumerate the test-infrastructure additions required (test
  runner devDependency, test script in the app's package.json,
  any `tsconfig.json` adjustments) as EXPLICIT allowlist entries,
  not implicit scope. If the WP's scope lock says "no other changes
  to package.json", the lock must either (a) explicitly list the
  test-script + test-runner-devDep additions as exceptions, or
  (b) scope the lock to the `dependencies` block only with an
  inline note.** Applies to every first-tests-in-an-app WP and to
  every WP that introduces a new test-verification command in its
  acceptance criteria.

**P6-38: Pre-flight must verify governance index files are
commit-clean, or explicitly carry the deferred-update as a
known handoff (WP-068 execution)**
- **What happened:** EC-070 was drafted during execution (per P6-36
  pivot). The post-commit hook warned: "The EC file exists but is
  not registered in the index. Consider adding it to EC_INDEX.md."
  EC_INDEX.md could not be cleanly updated in the WP-068 commit
  because `docs/ai/execution-checklists/EC_INDEX.md` was in the
  pre-existing dirty tree at session start with unrelated WP-062
  edits. Adding an EC-070 row would have co-mingled WP-062 state
  into the WP-068 commit. Resolved by deferring: the handoff was
  documented in the execution summary, the post-mortem §8, and
  the pre-commit review §6 (Governance & EC-Mode Alignment). A
  future small `SPEC:` or `INFRA:` commit must add the EC-070 row
  once EC_INDEX.md is clean.
- **Root cause:** Pre-flight READY verdict did not verify that
  cross-WP governance files (EC_INDEX.md, WORK_INDEX.md,
  DECISIONS_INDEX.md, 03.1-DATA-SOURCES.md) were commit-clean at
  the moment READY was printed. When the executing session
  unexpectedly needed to update one of those files (because of the
  P6-36 commit-prefix pivot forcing EC-070 to be drafted
  mid-session), the file was already dirty with unrelated work and
  the update had to be deferred.
- **Fix:** EC-070 registration deferred; tracked in three places
  (execution summary, post-mortem §8, pre-commit review §6).
  Commit lands without the EC_INDEX.md update; non-blocking
  governance debt accrued.
- **Prevention:** Pre-flight Dependency & Sequencing Check should
  add: **before printing READY, run `git status --short` and
  enumerate any modifications to cross-WP governance index files
  (EC_INDEX.md, WORK_INDEX.md, DECISIONS.md, DECISIONS_INDEX.md,
  00-INDEX.md, 03.1-DATA-SOURCES.md, and any REFERENCE doc). For
  each dirty file, the pre-flight must either (a) list it in an
  "Expected Governance Updates" allowlist so the executing session
  knows to stage its own edits selectively (git stash / patch-add /
  narrow `git add <path>` patterns), or (b) require the dirty state
  to be resolved (committed or reverted) before READY is printed.
  The pre-flight READY verdict must answer the question "can the
  executing session update governance index files without
  co-mingling other in-flight work?" — if the answer is no, the
  workflow should be paused for the dirty files to be resolved
  first.** This is the index-file analog of P6-34's "pre-flight
  READY is conditional on pre-flight governance files being
  committed" rule.

**P6-39: Corruption-safe storage loaders must distinguish
"missing" from "present-but-invalid" at the API boundary
(WP-068 execution)**
- **What happened:** The packet's `persistence.ts` spec defined
  `readEnvelope(key: string): unknown | null` with the semantic
  "returns null on any failure (private mode, missing, invalid
  JSON)". The initial `createPreferencesStore.loadFromStorage()`
  consumed this API: if `readEnvelope` returned null, `loadFromStorage`
  returned `buildDefaults()`. But the packet's §D also specified
  `backupCorrupt(key, backupKey): void` and Acceptance Criteria §D
  required that "a localStorage pre-populated with `"not json"` at
  `STORAGE_KEY` loads to defaults AND writes the corrupt blob to
  `BACKUP_KEY`." The unified-null API collapsed the two states
  ("no value stored" and "stored value is corrupt JSON") into one
  return code, so `loadFromStorage` had no way to decide whether
  to call `backupCorrupt`. The test assertion for the corrupt-blob
  backup failed on the first run. The fix was to add a secondary
  `localStorage.getItem(STORAGE_KEY)` probe inside `loadFromStorage`:
  if `readEnvelope` returned null AND the raw key was present, the
  blob is corrupt (back it up); if the raw key was absent, it was
  merely missing (skip backup).
- **Root cause:** The `readEnvelope` API was designed around
  "caller behavior is uniform for all failure modes" (return null;
  caller returns defaults). This is correct for the HAPPY case
  but wrong for any flow that needs to distinguish the failure
  modes downstream — in this case, the backup path. The unified-null
  return type is a lossy compression of a two-state input (present
  + parseable vs. present + unparseable vs. absent). A loader that
  needs to react differently to those cases must either (a) probe
  storage a second time to recover the lost bit, or (b) receive a
  discriminated return type from the outset.
- **Fix:** Secondary `localStorage.getItem(STORAGE_KEY)` probe
  inside `loadFromStorage`, wrapped in try/catch with a `// why:`
  comment naming the "missing vs. corrupt" distinction. The test
  suite passes with the corrupt-blob backup path exercised.
- **Prevention:** Established Patterns now states: **any
  persistence / loader API that supports a "back up on corruption"
  degradation path MUST return a discriminated result (e.g.,
  `{ kind: "empty" } | { kind: "corrupt", raw: string } | { kind: "value", value: unknown }`),
  not a unified `T | null`. If the API is stuck with a unified
  null-on-failure shape (e.g., inherited from a prior WP or the
  plan), the caller must compensate with a second probe and a
  `// why:` comment naming the lost bit being recovered. Pre-flight
  should flag any persistence API that combines a "return null on
  failure" surface with an Acceptance Criterion requiring
  differential failure-mode behavior downstream; that combination
  is a contract gap and the packet's scope should either widen the
  API's return type or explicitly authorize the compensating probe.**
  Applies to every WP introducing corruption-safe I/O where a
  backup / reporting / diagnostic path depends on distinguishing
  failure modes.

### Phase 6 continued — Arena HUD & Scoreboard (WP-062)

**P6-40: D-6512 / P6-30 generalizes from containers to any .vue
whose template references bindings beyond `$props` (WP-062
execution)**
- **What happened:** WP-062 was drafted with the understanding from
  D-6512 / P6-30 that only the store-consuming container
  (`ArenaHud.vue`) required the `defineComponent({ setup() { return
  {…} } })` authoring form under `@legendary-arena/vue-sfc-loader`'s
  separate-compile pipeline (`inlineTemplate: false`). Every "props-
  only subcomponent" was expected to work under `<script setup>`.
  During Step 14 test verification, four of the six subcomponents
  failed at mount time with two distinct runtime errors originating
  inside Vue 3.5.x's own runtime (not the HUD code):
  (1) `[Vue warn]: Property "colorBundle" was accessed during render
  but is not defined on instance` — for `PlayerPanel.vue` (computed
  binding), `ParDeltaReadout.vue` (three computeds:
  `hasParBreakdown`, `arrowIcon`, `toneClass`), and
  `EndgameSummary.vue` (two functions: `hasPar`, `hasScores`).
  (2) `[Vue warn]: Failed to resolve component: PlayerPanel` — for
  `PlayerPanelList.vue`, whose `<script setup>` imports `PlayerPanel`
  but the loader does NOT hoist top-level imports onto the render
  function's component registry the way `@vitejs/plugin-vue` does.
  Only `TurnPhaseBanner.vue` and `SharedScoreboard.vue` (leaf props-
  only templates referencing ONLY `$props`) worked under `<script
  setup>` as expected. Fix was to convert the four failing files
  plus the container to `defineComponent({ setup() { return {…} },
  components: {…} })` form, adding explicit `name:` fields to
  support `findAllComponents({ name: 'PlayerPanel' })` lookup in
  tests.
- **Root cause:** D-6512 was scoped to the ORIGINAL symptom
  (ArenaHud's setup-scope `snapshot` binding not reaching `_ctx`),
  but the underlying mechanism — "the separate-compile pipeline
  drops setup-scope bindings from `_ctx`" — applies uniformly to
  ANY setup-scope binding a template references, not just the
  container's store binding. It also applies to a SECOND class of
  bindings (imported child components) that the original D-6512
  diagnosis did not cover, because the container itself did not
  import child components from `<script setup>`. WP-062 was the
  first WP in the HUD chain to have both computed subcomponents AND
  a list component that imports a sibling, so it surfaced the
  generalization.
- **Fix:** Converted `ArenaHud.vue`, `PlayerPanel.vue`,
  `PlayerPanelList.vue`, `ParDeltaReadout.vue`, `EndgameSummary.vue`
  to `defineComponent` form. Kept `TurnPhaseBanner.vue` and
  `SharedScoreboard.vue` in `<script setup>` (props-only templates).
  Added `name: 'PlayerPanel'` to `PlayerPanel.vue` to support the
  ordering-assertion test in `PlayerPanelList.test.ts`. Added a
  `// why:` comment on every converted file citing D-6512 / P6-30
  and distinguishing the two bind-class cases (setup-scope bindings
  vs. imported child components).
- **Prevention:** Established Patterns now states: **any HUD `.vue`
  file whose template references bindings other than `$props` —
  including computeds, functions, or imported child components
  declared in `<script setup>` — MUST use the `defineComponent({
  setup() { return {…} }, components: {…} })` authoring form under
  the vue-sfc-loader separate-compile pipeline. `<script setup>`
  remains correct ONLY for leaf components whose template
  references exclusively props. Pre-flight for any UI WP that
  introduces SFCs under `apps/*/src/components/**` must enumerate
  which components are container/computed/list-parent (require
  `defineComponent`) vs. props-only leaf (may use `<script
  setup>`). The two diagnostic fingerprints are (a) `[Vue warn]:
  Property "X" was accessed during render but is not defined on
  instance` (setup-scope binding case — fix by returning `X` from
  `setup()`) and (b) `[Vue warn]: Failed to resolve component: X`
  (imported child component case — fix by adding `components: {
  X }` to the defineComponent options).** Applies to every HUD WP,
  every viewer-side WP that uses vue-sfc-loader tests, and every
  future UI WP that adds multi-component `.vue` trees. Supersedes
  the narrower P6-30 framing in setting the DEFAULT authoring form.

**P6-41: Path-scoped stash + re-apply + leave-stash pattern for
resolving mixed governance-file edits at commit time (WP-062
execution)**
- **What happened:** At WP-062 session start, `git status --short`
  showed `docs/ai/DECISIONS.md`, `docs/ai/DECISIONS_INDEX.md`,
  `docs/ai/work-packets/WORK_INDEX.md`, and
  `docs/ai/execution-checklists/EC_INDEX.md` modified with pre-
  existing uncommitted edits from prior WP-068 and MOVE_LOG_FORMAT
  work (D-1414, D-0203, D-0204, D-0205, WP-068 WORK_INDEX row,
  EC-070 EC_INDEX row). The WP-062 session prompt's Gate #2
  narrowly named four governance files (WP-062 MD, EC-069 MD,
  EC_INDEX.md, copilot-wp062 MD) and those four were clean — but
  the broader governance tree was NOT clean. Per EC-069's DoD, the
  session still had to modify three of those same pre-existing-
  dirty files (DECISIONS.md, WORK_INDEX.md, EC_INDEX.md). A naive
  `git add docs/ai/DECISIONS.md` would stage BOTH the pre-existing
  D-1414/D-0203-5 AND my WP-062 D-6201-6206, violating 01.3 scope
  discipline under an `EC-069:` prefix. A shortcut suggestion to
  `git stash push -- <file>` "to isolate pre-existing hunks from
  new hunks" was considered and rejected: path-scoped stash
  captures the WHOLE unstaged diff for each matched file (git
  operates on whole-file diffs against HEAD), not a subset — the
  stash would take the WP-062 edits with it. The verified working
  pattern required one additional step:
  (1) Path-scoped stash of ALL four governance-file diffs (captures
      everything — both pre-existing and in-session).
  (2) Re-apply the WP's governance edits via the Edit tool, using
      the executing session's authoritative memory of what it added
      (exact D-#### text, exact row-flip content, exact status
      change). The execution session is the authoritative source
      for its own governance additions, so reconstruction is
      deterministic.
  (3) Verify READY STATE by `git diff --stat` (expected line counts
      match WP-only adds) and by grep for both the NEW markers
      (must be present) and the stashed markers (must be absent).
  (4) Stage by name (never `git add .` or `git add -A`), commit.
  (5) LEAVE the stash. Do NOT `git stash pop` in the executing
      session — the stashed hunks will now conflict with the
      freshly-committed WP additions in the same file regions.
- **Root cause:** The project's WP cadence produces overlapping
  governance-file edits because multiple WPs update the same small
  set of documents (DECISIONS.md, WORK_INDEX.md, EC_INDEX.md).
  When an upstream WP's governance additions are drafted ahead of
  its commit (or after its code commit but before its doc commit),
  the downstream WP executing session inherits a dirty tree on
  exactly the files it also needs to modify. P6-34 (pre-flight
  governance commits) and P6-38 (cross-WP index-file cleanliness)
  are the prevention-side rules; P6-41 is the RECOVERY rule for
  when the ideal clean-tree state is not achievable in time.
- **Fix:** Executed the five-step pattern above; WP-062 committed
  as `7eab3dc` with a pure WP-062 diff. Stash `stash@{0}` retained
  for the WP-068 / MOVE_LOG governance owner to resolve in a
  subsequent session.
- **Prevention:** Established Patterns now states: **when an
  executing session's Gate #2 pre-flight governance check passes
  but the broader tree is dirty on files the session must also
  modify, the executing session MUST use the stash + re-apply +
  leave-stash pattern (five steps documented in P6-41) rather than
  (a) bundling the pre-existing edits into its own commit (scope
  violation), (b) using `git add -p` to selectively stage hunks
  (fragile, audit-hostile), or (c) attempting a path-scoped stash
  expected to isolate hunks within a file (the shortcut does not
  work — git stashes whole-file diffs). The session-context file
  for the downstream WP should pre-document the expected pattern
  in a "Mixed Governance Edits" section so the executing session
  knows in advance which files are dirty and how to proceed. The
  stash owner (upstream WP) is always the resolver — do not pop in
  the downstream session.** Applies to every WP whose DoD requires
  governance-file updates on files likely to carry pre-existing
  uncommitted work from parallel WP timelines.

**P6-42: Same-session 01.6 post-mortem + same-session pre-commit
review is a user-approved deviation from P6-35 and must be
disclosed in the commit body (WP-062 execution)**
- **What happened:** P6-35 establishes that the 01.6 post-mortem
  runs in-session (step 4, before commit) and the pre-commit review
  runs as a **separate gatekeeper session** (step 5, before commit).
  WP-062 execution asked to run BOTH in the same session under an
  explicit user-approved deviation (via `AskUserQuestion` after the
  pre-commit-review prompt was loaded). The user selected "Proceed
  in this session (accept deviation)" with full awareness of the
  WP-067 precedent that labeled this a "procedural deviation — do
  not repeat." The executing session surfaced the deviation to the
  user before honoring it, then disclosed the deviation in the
  commit body under `EC-069:` as a dedicated paragraph: "Pre-commit
  review performed in same session under user-acknowledged P6-35
  deviation; future WPs should restore separate-session gatekeeper
  discipline." This preserves the rule (separate-session is still
  the standard) while documenting the specific exception.
- **Root cause:** The WP-062 execution chain collapsed steps 4, 5,
  and 6 into a single interactive session because the session was
  operating end-to-end on behalf of the user. The separate-session
  boundary exists for adversarial-review reasons (a gatekeeper
  session starts fresh without the author's cognitive framing), but
  in practice the user sometimes wants to proceed without opening
  a second session. Rather than refuse the request or silently
  violate P6-35, the pattern is (a) stop and confirm via
  `AskUserQuestion`, (b) if the user approves, honor the request
  while surfacing the deviation in-channel, (c) disclose the
  deviation in the commit body so the audit record is complete.
- **Fix:** Disclosure paragraph added to the `EC-069:` commit body
  (commit `7eab3dc`). No governance-document change required — the
  P6-35 rule is unchanged, only this specific exercise of it is
  annotated.
- **Prevention:** Established Patterns now states: **when a user
  asks to collapse steps 4 (post-mortem) and 5 (pre-commit review)
  into the same session as step 3 (execution), the executing
  session MUST (a) surface the P6-35 "separate-session gatekeeper"
  expectation and the "WP-067 do-not-repeat" precedent via
  `AskUserQuestion` before proceeding, (b) only proceed if the user
  selects an "accept deviation" option explicitly, and (c) include
  a one-paragraph disclosure in the `EC-###:` commit body naming
  the deviation and reaffirming that future WPs should restore the
  standard. `--no-verify` equivalents (silently skipping the
  gatekeeper boundary without asking) are prohibited. The
  deviation is a CONSCIOUS choice that the user owns; the
  executing session's job is to make the choice visible and to
  preserve the audit record.** Applies to every WP whose 01.6 is
  mandatory AND whose user chain is interactive enough to request
  step-collapse. Does NOT apply to automated / batch execution
  contexts where the gatekeeper boundary is structurally enforced
  by the workflow orchestrator.

---

### Phase 6 continued — Replay Snapshot Producer (WP-063)

**P6-43: Verification-step greps match JSDoc text — describe
forbidden APIs in prose, not by literal name, when the file is
under a grep-based purity check (WP-063 execution)**
- **What happened:** `packages/game-engine/src/replay/buildSnapshotSequence.ts`
  carried a JSDoc line reading "no I/O, no console, no wall clock,
  no Math.random, no performance.now, and no boardgame.io import"
  describing what the helper does NOT do. EC-071 Verification Step
  3 greps `buildSnapshotSequence.ts` with the pattern
  `console\.|Date\.now|Math\.random|performance\.now|from 'node:fs`
  and REQUIRES no matches as proof of helper purity. The JSDoc's
  "Math.random" substring matched the `Math\.random` sub-pattern,
  producing a false-positive verification failure even though the
  runtime code was pristine. The fix was to reword the JSDoc to
  "no logging, no wall-clock reads, no non-engine RNG, and no
  boardgame.io import" — functional content identical, but the
  grep now returns zero matches unambiguously.
- **Root cause:** Verification-step greps are substring matches,
  not semantic checks. Any occurrence of a forbidden API name —
  including in comments, docstrings, module headers, or string
  literals — triggers a match. The more carefully a file
  documents WHAT IT DOES NOT DO (a good practice for readers),
  the more likely a grep-based purity check will false-positive.
  The engine-wide `// why:` / JSDoc convention actively encourages
  this kind of "not X, not Y" negative description, which is at
  odds with substring greps.
- **Fix:** WP-063 post-mortem §10 rewrote the JSDoc to use
  paraphrases ("no wall-clock reads" not "no Date.now"; "no
  non-engine RNG" not "no Math.random"; "no logging" not "no
  console"). The substantive claim is unchanged — the helper is
  still explicitly documented as avoiding these APIs — but the
  grep-visible tokens no longer appear in the file.
- **Prevention:** Established Patterns now states: **when a WP's
  EC includes a Verification Step that greps a specific file for
  forbidden API names, any JSDoc, module header, or comment inside
  that file MUST describe forbidden APIs in prose rather than by
  literal name. Substring-match verification is blind to semantic
  context — "no Math.random" in a doc comment and `Math.random()`
  in a live call produce identical grep output. The EC author
  should flag this in the session prompt's §Non-Negotiable
  Constraints by listing both forbidden APIs AND their
  documentation paraphrases (e.g., "no `Math.random()` — document
  as 'no non-engine RNG'"). The executing session's first pass
  over the file should proactively use the paraphrase form; the
  post-mortem §10 fixes list captures any reword required after
  a grep false-positive surfaces at Verification Step time.**
  Applies to every WP with a purity-style grep in its EC's
  Verification Steps. Does NOT apply to WPs where the
  verification is semantic (e.g., compiler output, type-check
  failures, test assertions) — those distinguish comments from
  code natively.

**P6-44: `pnpm-lock.yaml` is an implicit allowlist entry when a
WP adds a new workspace package (WP-063 execution)**
- **What happened:** WP-063 added `apps/replay-producer/` as a
  new workspace package (first `cli-producer-app` per D-6301).
  `pnpm install` after the `package.json` write naturally updated
  `pnpm-lock.yaml` to resolve the new `workspace:*` dependency
  on `@legendary-arena/game-engine` and the `tsx` /
  `@types/node` / `typescript` devDeps. The WP-063 §Files
  Expected to Change list did not mention `pnpm-lock.yaml`. The
  executing session staged it anyway, on the reasoning that
  omitting it would leave the lockfile drifted and break
  `pnpm install --frozen-lockfile` in CI. The gatekeeper
  session accepted this interpretation without requesting an
  amendment.
- **Root cause:** §Files Expected to Change lists what the
  *author* modifies; tooling-driven side-effects (lockfile
  updates, generated type files, compiled artifacts) are not
  explicitly enumerated even when they are structurally
  required. WP-063 is the first WP in the repo to add a new
  workspace package since the governance tightening around
  P6-27 (scope lock) and P6-37 (test-infra explicit
  allowlist), so the lockfile-side-effect case had not been
  previously surfaced.
- **Fix:** WP-063 Commit A (`97560b1`) included
  `pnpm-lock.yaml` in the staged set; the post-mortem §2
  Scope & Allowlist Audit documents the rationale and flags
  it as an implicit-but-required consequence of adding the
  new workspace package.
- **Prevention:** Established Patterns now states: **when a
  WP's §Files Expected to Change adds a new workspace package
  (anything under `packages/**/package.json` or
  `apps/**/package.json` that did not previously exist),
  `pnpm-lock.yaml` is an implicit allowlist entry and MUST be
  staged in the EC-### code commit. No pre-flight amendment is
  required unless the lockfile change extends beyond the new
  package (e.g., transitive-dep bumps in other packages). The
  executing session should include `pnpm-lock.yaml` in its
  `git diff --name-only` audit with a one-line note "expected
  lockfile side-effect of adding <package-name>". Conversely,
  if `pnpm-lock.yaml` is dirty but no new package is added,
  THAT is a scope-lock violation — investigate before staging.**
  Applies to every WP that adds a new workspace package. Does
  NOT apply to WPs that only modify existing `package.json`
  files; those follow P6-37's explicit allowlist rule for
  test-infra additions.

**P6-45: WP-literal signature vs upstream canonical type —
reconcile via an execution-surfaced D-entry, not a
pre-flight amendment (WP-063 execution)**
- **What happened:** WP-063's spec literally defined
  `ReplayInputsFile.inputs: readonly ReplayInput[]` and the
  helper signature `buildSnapshotSequence({ setupConfig, seed,
  inputs })` — three fields. But `ReplayInput` in WP-027's
  canonical `replay.types.ts` is the *top-level match record*
  (`{ seed, setupConfig, playerOrder, moves }`), not a
  per-step move record. Per-step records are `ReplayMove`.
  The WP's own acceptance criterion
  `snapshots.length === inputs.length + 1` only makes sense if
  each "input" is a per-step record, confirming that "inputs"
  was meant as `ReplayMove[]` — the WP author used "inputs"
  colloquially. Additionally, `buildInitialGameState` requires
  a `CardRegistryReader` and `applyReplayStep` requires
  `numPlayers` (derived from `playerOrder.length`), neither of
  which fits the 3-field literal signature. The executing
  session reconciled during implementation rather than
  stopping for a pre-flight amendment: renamed the file field
  to `moves: readonly ReplayMove[]`, added `playerOrder` and
  `registry` to `BuildSnapshotSequenceParams`, and logged the
  reconciliation as D-6305 in DECISIONS.md with a full
  rejected-alternatives list. The 01.6 post-mortem §D-6305
  section documented the choice before Commit A; the Commit B
  governance commit landed the D-6305 entry.
- **Root cause:** WP spec authoring predates the full engine
  surface. WP-063 was drafted before WP-027's canonical
  `ReplayInput` naming was widely referenced in spec language,
  and before WP-080 introduced `applyReplayStep`'s explicit
  `numPlayers` parameter. When an upstream canonical name
  exists, casual WP-spec use of the same word for a different
  concept creates a reconciliation debt that only surfaces at
  execution time.
- **Fix:** D-6305 logs the reconciliation in DECISIONS.md with
  four rejected alternatives (intersection type, module-scoped
  registry singleton, numPlayers inference from moves,
  rename-field-preserve-type). The executing session did NOT
  escalate to a pre-flight amendment because (a) the WP's
  acceptance criteria were internally consistent under the
  reconciled interpretation, (b) the reconciliation preserved
  the WP's intent (the CLI does wrap per-step records, not
  top-level match records), and (c) the upstream canonical
  `ReplayMove` type was already the correct shape — the WP
  spec just used a different label for the same concept. A
  pre-flight amendment would have added a full round-trip of
  governance churn for what was, in substance, a terminology
  reconciliation. The D-entry + post-mortem audit is a cleaner
  audit trail for the same outcome.
- **Prevention:** Established Patterns now states: **when the
  executing session discovers that a WP's literal
  type/signature wording conflicts with an upstream canonical
  type, the session MUST (a) confirm the reconciled
  interpretation is internally consistent with the WP's own
  acceptance criteria (if the AC assumes one reading and the
  literal spec assumes another, the AC wins — the literal
  text is an authoring mistake), (b) log the reconciliation as
  an execution-surfaced D-entry (e.g., D-6305) with a full
  rejected-alternatives list in DECISIONS.md + an index row in
  DECISIONS_INDEX.md, (c) surface the reconciliation in the
  01.6 post-mortem §Decisions Surfaced During Execution
  (§10 Fixes list) BEFORE Commit A, and (d) reference the
  D-entry in the governance Commit B body. Do NOT (e) silently
  implement the reconciled interpretation without a D-entry, or
  (f) escalate to a pre-flight amendment when the AC is
  internally consistent under the reconciliation — the
  D-entry + post-mortem audit is the faster and cleaner
  record. Pre-flight amendment remains the right path ONLY
  when the reconciled interpretation materially changes AC
  semantics (e.g., adds new required fields to shipped types,
  removes a documented behavior).** Applies to every WP whose
  spec references type names that exist in upstream canonical
  modules. Does NOT apply to NET-new type names the WP is
  introducing — those cannot conflict with upstream.

**P6-46: vue-sfc-loader `<script setup>` -vs- `defineComponent`
decision must be phrased as "any template binding beyond
`$props` / `$emits`" — not "leaf vs non-leaf" or "uses emits
or not" (WP-064 execution)**
- **What happened:** EC-074 Locked Values described the SFC
  authoring decision per component as "leaf (props + emits only)
  → `<script setup>` OK unless template references non-props
  bindings". The executing session initially authored
  `<ReplayFileLoader />` in `<script setup>` form because it
  reads as a leaf — only one prop-equivalent surface (an
  `emits` declaration) plus a file `<input>`. The first test
  run failed: the `role="alert"` region rendered but its
  `{{ errorMessage }}` text was empty, and the alert region did
  not clear after a successful re-load. Diagnosis: under
  vue-sfc-loader's separate-compile pipeline (D-6512), only
  `defineProps` and `defineEmits` declared bindings reach the
  template's `_ctx`. Top-level `ref`s and helper functions
  declared inside `<script setup>` are NOT exposed to `_ctx`,
  so `errorMessage` rendered as `undefined` (empty string in
  Vue's `{{ }}` interpolation) and the `v-if` evaluated against
  an undefined value (truthy after the first set, but never
  cleared because the proxy never saw the assignment). The
  same failure mode WP-061's `<BootstrapProbe />` and WP-062's
  six HUD containers (`<ArenaHud />`, `<PlayerPanel />`,
  `<PlayerPanelList />`, `<ParDeltaReadout />`,
  `<EndgameSummary />`, etc.) had hit and documented as
  D-6512 / P6-30. The executing session refactored
  `<ReplayFileLoader />` to `defineComponent({ setup() { return
  {...} } })` form mid-execution; tests passed immediately. No
  scope expansion (still inside the WP-064 allowlist; same file).
- **Root cause:** EC-074's "leaf (props + emits only)" wording
  is technically correct but easy to mis-parse. The executing
  agent reads "props + emits only" as "if the template only
  binds props or emits, leaf form is OK" — which is true — and
  then confuses "the template binds an emit" with "the
  component declares an emit." A component can declare an emit
  via `defineEmits` and have a template that never references
  the emit binding directly (it references a `ref` or a helper
  that calls the emit), and the template-binding test still
  fires because the `ref` / helper is the binding, not the
  emit. The mismatch between "what the script declares" and
  "what the template binds" is the actual axis the rule
  cares about.
- **Fix:** ECs and session prompts that lock SFC authoring form
  per component MUST phrase the rule as: *"any template
  binding that is neither a `defineProps`-declared prop nor a
  `defineEmits`-declared emit forces `defineComponent({ setup()
  { return {...} } })` form."* Concretely: a template that
  references a `ref`, a `computed`, a helper function, a
  `storeToRefs` binding, or any imported value MUST use
  `defineComponent` form, regardless of whether the component
  is "leaf" by other criteria (single root, no child
  components, etc.). The existing WP-061 / WP-062 precedent
  (D-6512 / P6-30) IS this rule; the WP-064 reinforcement is
  on the wording an EC uses to express it. The "leaf vs
  non-leaf" distinction is a useful intuition for spec
  authors but a misleading binary for executing agents — the
  template-binding test is the load-bearing predicate.
- **Prevention:** Established Patterns now states: **for any
  client-app or other vue-sfc-loader-loaded SFC WP, the EC's
  Locked Values § "Component authoring form" entry MUST use
  the verbatim phrasing "any template binding that is neither
  a `defineProps`-declared prop nor a `defineEmits`-declared
  emit forces `defineComponent({ setup() { return {...} } })`
  form." Per-component classification (leaf, non-leaf,
  container, presenter) may be included as a sanity-check
  hint, but the binding test is the rule that decides. The
  pre-flight Component Authoring Form Audit (a new optional
  audit step for any WP that authors SFCs under vue-sfc-loader)
  walks each new SFC's draft template and confirms the binding
  test against the proposed authoring form.** Applies to every
  WP that authors SFCs under `@legendary-arena/vue-sfc-loader`
  (currently `apps/arena-client/`; future client apps if the
  loader is reused). Does NOT apply to SFCs loaded only by
  Vite's `@vitejs/plugin-vue` (which uses the inlined-template
  path that handles `<script setup>` bindings correctly per the
  D-6512 root cause).

**P6-47: Pinia reactive proxy breaks reference equality in store
assertions — assert by content, not by reference (WP-064
execution)**
- **What happened:** WP-064's `<ReplayInspector />` tests were
  initially authored using
  `assert.equal(store.snapshot, sequence.snapshots[i])` —
  strict reference equality between the value stored in the
  Pinia store and the original snapshot reference handed to
  `setSnapshot`. All 13 inspector navigation tests failed with
  `actual: [Object], expected: [Object], operator: 'strictEqual'`.
  Diagnosis: Pinia wraps stored values in reactive proxies on
  insertion. The proxy is a different object identity than the
  raw snapshot, even though it transparently forwards reads
  and writes. `assert.equal(proxy, raw)` is therefore always
  `false`, regardless of what's "in" the store. The executing
  session rewrote the tests to use a `loadedIndex(store,
  sequence)` helper that compares
  `JSON.stringify(store.snapshot)` against
  `JSON.stringify(sequence.snapshots[i])` and returns the
  matching index — the same pattern WP-062's `<ArenaHud />`
  deep-immutability suite uses. All tests passed; the
  behavioral assertion is strictly stronger ("the snapshot at
  index N is loaded by content" is a more realistic check than
  "the same object reference was stored"). No assertions were
  weakened.
- **Root cause:** Pinia (Options API or Setup API) wraps state
  values in `reactive()` proxies on assignment. `reactive()`
  preserves shape and forwards property access, but the proxy
  is a wrapping object — the raw value is reachable via
  `toRaw()` but not by direct identity comparison. This is
  unrelated to WP-061's store contract (which is correct);
  it's a Pinia + Vue 3 reactivity invariant. The WP-061 store
  tests already use content assertions
  (`assert.equal(store.snapshot?.game.phase, 'play')`), but
  no precedent flagged the more general rule for any future
  client WP that asserts identity on stored objects.
- **Fix:** Tests that need to assert "the right snapshot is
  loaded" use one of three patterns:
  (1) **Content equality:**
  `assert.deepStrictEqual(store.snapshot, expectedSnapshot)`
  or `JSON.stringify` round-trip when the snapshot is large
  and a custom helper avoids printing the whole diff on
  failure;
  (2) **Display-state side effect:** assert on the rendered
  text content or attributes that the snapshot drives
  (`wrapper.find('[data-testid="replay-position"]').text()`
  for "currently at index N"; `wrapper.find('[aria-label="...
  "]').text()` for the field value);
  (3) **`toRaw` unwrap:** `import { toRaw } from 'vue'; assert.
  equal(toRaw(store.snapshot), expectedSnapshot)` — works but
  couples the test to Vue internals; prefer (1) or (2).
- **Prevention:** Established Patterns now states: **for any
  client-app WP that asserts on Pinia store state in tests,
  use content equality (deep-equal or `JSON.stringify` round-
  trip) or display-state side effects, NEVER strict reference
  equality (`assert.equal(store.snapshot, originalReference)`).
  This is a Pinia + Vue 3 reactivity invariant, not a
  per-store contract issue. Pre-flight Test Expectations for
  client-app WPs MUST flag any planned assertion that compares
  store-held values by reference — the executing agent will
  hit this on the first test run regardless. Pre-flight should
  also note the EC-064 / WP-064 `loadedIndex(store, sequence)`
  helper as the canonical pattern for "which fixture index is
  currently loaded" assertions.** Applies to every client-app
  WP that exercises Pinia store interaction in tests. Does NOT
  apply to engine tests (no Pinia) or to component tests that
  drive components by props rather than by store (props are
  passed by reference and equality holds).

**P6-48: Static JSON import widens literal types — `satisfies
<ContractType>` against a JSON literal fails when the contract
uses literal types like `version: 1` (WP-064 execution)**
- **What happened:** WP-064's `apps/arena-client/src/fixtures/
  replay/index.ts` was first authored using the WP-061 pattern
  (`apps/arena-client/src/fixtures/uiState/typed.ts`):
  `import threeTurnSampleJson from './three-turn-sample.json';
  const threeTurnSample = threeTurnSampleJson satisfies
  ReplaySnapshotSequence;`. Typecheck failed with
  `TS1360: Type '{...; version: number; ...}' does not satisfy
  the expected type 'ReplaySnapshotSequence'. Types of property
  'version' are incompatible. Type 'number' is not assignable
  to type '1'.` Diagnosis: TypeScript's `resolveJsonModule`
  imports JSON values with primitive types **widened** —
  `1` becomes `number`, `"play"` may stay `'play'` for short
  strings but generally widens to `string` for non-trivial
  JSON, etc. The contract `ReplaySnapshotSequence` types
  `version: 1` as a literal (the D-6303 compile-time version
  seam), so the widened import never satisfies it.
  WP-061's `UIState` works under `satisfies` only because none
  of its fields use literal types narrower than `string` /
  `number` — `phase: 'lobby' | 'play' | 'end'` happens to
  narrow correctly because the JSON contains exactly one of
  the literal strings, but `version: 1` narrows to `number`
  because TypeScript's JSON-import inference treats numeric
  literals less aggressively than string literals.
  The executing session resolved by re-routing the import
  through the runtime parser:
  `const threeTurnSample = parseReplayJson(JSON.stringify(
  threeTurnSampleJson), 'three-turn-sample.json');` — the
  parser's runtime D-6303 assertion (`version === 1`) gates
  the conversion and returns a typed `ReplaySnapshotSequence`.
  The trade-off: an unnecessary encode/decode round-trip at
  module load time, in exchange for keeping the contract type
  intact. The post-mortem §6 logs this as a non-blocking
  hidden-coupling note.
- **Root cause:** TypeScript's `resolveJsonModule` performs
  type inference per JSON literal but applies different
  widening rules to numeric vs string literals. There is no
  known compiler flag that narrows numeric JSON literals to
  their literal types. The contract owner (here WP-063 /
  D-6303) deliberately uses the literal `version: 1` as a
  compile-time seam for version detection, so widening the
  contract to `version: number` would defeat its purpose. The
  intersection (literal contract + widened import) is the
  unavoidable mismatch.
- **Fix:** Three options for any future fixture loader that
  imports JSON against a literal-typed contract:
  (1) **Re-route through the runtime parser** (the WP-064
  choice). Pros: single canonical validation path, runtime
  assertion gates the conversion, fixture-as-data is exercised
  by the same code path as user-loaded files. Cons: redundant
  encode/decode round-trip at module load.
  (2) **Cast through `unknown`:** `const fixture =
  threeTurnSampleJson as unknown as ReplaySnapshotSequence`.
  Pros: no runtime overhead. Cons: loses the structural check
  entirely — silently masks future drift in non-version
  fields.
  (3) **Hand-author a typed fixture in TypeScript instead of
  JSON:** `export const fixture: ReplaySnapshotSequence = { ...
  };`. Pros: full compile-time check including literal types.
  Cons: cannot be regenerated by an external tool; defeats
  the purpose of CLI-produced fixtures (WP-063 byte-identical
  round-trip).
  WP-064 chose (1) because the fixture must be CLI-produced
  (per WP-063 byte-identical determinism) and the runtime
  D-6303 site already exists.
- **Prevention:** Established Patterns now states: **for any
  client WP that loads CLI-produced JSON fixtures via static
  import, expect TypeScript to widen numeric literal fields
  (`version: 1` → `number`, `count: 5` → `number`) and reject
  `satisfies <Contract>` against contracts that use literal
  types. Pre-flight Structural Readiness Check for fixture
  loaders MUST verify whether the contract type uses any
  literal fields (numeric or string) and lock the loader
  pattern in advance. The three options are (a) re-route
  through the runtime parser (preferred when a runtime parser
  exists for the contract — the parser's validation is the
  authoritative gate), (b) cast through `unknown` (only when
  no runtime parser exists AND the loss of compile-time
  checking is explicitly accepted in DECISIONS.md), or (c)
  hand-author the fixture in TypeScript (only when the fixture
  is small enough that hand-authoring is maintainable AND the
  fixture does not need to round-trip through an external CLI
  for byte-identical regeneration). The choice is locked
  during pre-flight, not discovered at typecheck time.**
  Applies to every fixture-loader WP that imports JSON
  against a contract type with at least one literal field.
  Does NOT apply when the contract uses only widened types
  (`string`, `number`, `boolean`) at every level — those
  satisfy widened JSON imports natively.

**P6-49: `pnpm --filter <pkg>` CLI scripts resolve relative
paths from the package's CWD, not the repo root — committed
`.cmd.txt` reproducibility commands must use paths relative
to the executing package (WP-064 execution)**
- **What happened:** WP-064 needed to regenerate
  `apps/arena-client/src/fixtures/replay/three-turn-sample.json`
  via the WP-063 CLI from a sibling
  `three-turn-sample.inputs.json` file. The first invocation
  used repo-root-relative paths:
  `pnpm --filter @legendary-arena/replay-producer produce-replay
  --in apps/arena-client/src/fixtures/replay/three-turn-sample.
  inputs.json --out apps/arena-client/src/fixtures/replay/
  three-turn-sample.json --produced-at 2026-04-19T00:00:00Z`.
  The CLI failed with `ENOENT: no such file or directory,
  open 'C:\pcloud\BB\DEV\legendary-arena\apps\replay-producer\
  apps\arena-client\src\fixtures\replay\three-turn-sample.
  inputs.json'` — pnpm filtered down into the producer's
  directory and the relative path was joined onto that CWD,
  producing the doubled `apps/replay-producer/apps/arena-client`
  segment. The executing session corrected to
  `--in ../arena-client/src/fixtures/replay/three-turn-sample.
  inputs.json --out ../arena-client/src/fixtures/replay/
  three-turn-sample.json` (paths relative to `apps/replay-
  producer/`), which resolved correctly. The committed
  `three-turn-sample.cmd.txt` documents this with an explicit
  note: *"Paths are resolved by the producer relative to apps/
  replay-producer/ (the package the script runs under), so the
  --in / --out arguments use ../arena-client/... rather than
  apps/arena-client/..."*. Byte-identical regeneration was
  verified twice after the path fix.
- **Root cause:** `pnpm --filter <pkg> <script>` changes the
  process's CWD to the matched package's root before running
  the script. Any relative path argument the script receives
  is then resolved against that CWD by the consuming Node API
  (`fs.readFile`, `fs.writeFile`, etc.) — not against the
  caller's CWD. This is standard pnpm behavior, documented but
  easy to miss when reading a `.cmd.txt` invocation that visually
  starts at "the repo root" because the user types it from the
  repo root. The disconnect between "where I type the command"
  and "where the script's relative paths are resolved" is the
  load-bearing detail.
- **Fix:** Two patterns work; pick one consistently per WP:
  (1) **Package-relative paths** (the WP-064 choice): paths
  use `../<sibling-package>/...` from the executing package's
  perspective. Pros: matches the actual CWD pnpm sets up; the
  command reads exactly as the script will see it. Cons: the
  caller (typing from the repo root) sees a `..` path that
  doesn't visually match the repo layout.
  (2) **Absolute paths**: the `.cmd.txt` uses absolute paths
  resolved at command-author time. Pros: unambiguous. Cons:
  not portable across machines (different repo roots) — the
  `.cmd.txt` becomes a per-machine artifact, defeating its
  reproducibility purpose.
  Option (1) is preferred and matches the WP-063
  `apps/replay-producer/samples/three-turn-sample.cmd.txt`
  precedent (which uses `samples/...` because the inputs are
  inside the producer's own directory). For cross-package
  invocations, (1) extends naturally to `../<package>/...`.
- **Prevention:** Established Patterns now states: **any WP
  that commits a `.cmd.txt` reproducibility file for a
  `pnpm --filter <pkg> <script>` invocation MUST use paths
  relative to the filtered package's directory, NOT the repo
  root. The `.cmd.txt` MUST include a brief comment
  explaining this disconnect (one or two sentences:
  "Paths are resolved relative to apps/<filtered-package>/,
  not the repo root, because pnpm --filter changes CWD into
  the package before running the script. Run the command
  from the repo root.") so a future reader regenerating the
  artifact does not rediscover this lesson. Pre-flight Fixture
  Generation Path lock for any WP that produces a fixture via
  cross-package CLI MUST specify the package-relative paths
  as part of the locked invocation.** Applies to every WP that
  consumes a CLI from a different workspace package (e.g.,
  arena-client consuming the replay-producer CLI; future
  spectator-client consuming the same; any future WP using a
  CLI from `apps/server/` or a packaged tool). Does NOT apply
  when the WP's own package owns the CLI (paths can be
  package-internal and the disconnect doesn't surface).

**P6-50: Pre-flight Locked Values that pre-load grep-discipline
rules prevent mid-execution fixes — P6-43's prevention side
(WP-035 execution)**
- **What happened:** WP-034 hit six P6-43 collisions at its very
  first verification gate run — JSDoc strings in
  `versioning.types.ts`, `versioning.check.ts`, and
  `versioning.migrate.ts` contained literal tokens
  (`boardgame.io`, `Math.random`, `Date.now`) that the EC's
  purity greps treated as matches. The WP-034 post-mortem
  documented the fix (reword to paraphrase) and flagged this as
  the first empirical demonstration that P6-43 is load-bearing
  across sessions. WP-035 took the lesson further: its session
  prompt's §Locked Values included an explicit paraphrase-
  discipline clause calling out the forbidden tokens
  (`boardgame.io`, `Math.random`, `Date.now`, `performance.now`,
  `@legendary-arena/registry`, `apps/server`) and their approved
  paraphrases ("the game framework", "non-engine RNG",
  "wall-clock helper", "high-resolution timing reads", "the
  registry package", "the server layer") before a single file
  was authored. Result: **zero mid-execution fixes**. The
  executing session's first pass over `ops.types.ts` used the
  paraphrase form proactively; the first verification gate run
  returned zero matches on the first attempt. WP-035's post-
  mortem §Lesson Recorded called this out explicitly as "the
  pre-flight paraphrase-discipline Locked Value prevented the
  P6-43 collision class that surfaced in WP-034."
- **Root cause:** P6-43 as originally drafted was a **reactive**
  rule — "when the grep false-positives, reword the JSDoc." The
  post-mortem §10 fix list is where the reword happens, after
  the gate has already failed and been re-run. This is correct
  but costs at least one verification-gate cycle per affected
  file, plus the mental context-switch to understand why the
  gate failed on what looks like clean code. Pre-loading the
  paraphrase-discipline rule into the session prompt's §Locked
  Values eliminates the cycle by making paraphrase the
  first-pass default rather than the post-gate correction.
- **Fix:** None required in WP-035 — the proactive Locked Value
  prevented the defect. The fix is to the pre-flight template,
  not to any individual WP.
- **Prevention:** Established Patterns now states: **any WP
  whose EC includes a Verification Step with purity-style greps
  over new source files MUST have a §Locked Values subsection
  in its session prompt that enumerates the forbidden grep
  tokens AND their approved paraphrases BEFORE the execution
  session begins. Do not rely on the executing session to
  rediscover the paraphrase requirements during the first
  verification gate run — that is P6-43's reactive path and
  costs a full gate cycle per affected file. The paraphrase
  table belongs in Locked Values alongside the contract
  literals, not in Lessons Learned alongside post-hoc
  observations. Specifically, for engine-category WPs
  classified under a new `src/<subdir>/` D-entry, the session
  prompt's §Locked Values or §Non-Negotiable Constraints should
  include a two-column table: "Forbidden token" / "Approved
  paraphrase," populated from the EC's Verification Step grep
  patterns.** Applies to every Contract-Only or Type-Only WP
  that ships new files under grep-scoped purity checks (all
  `packages/game-engine/src/<subdir>/` WPs from D-2706 forward;
  all `apps/<pkg>/` WPs with analogous purity gates). Does NOT
  apply to WPs where Verification Steps are semantic (compiler
  output, test assertions, type-check failures) — those
  distinguish documentation strings from runtime code natively.
  Relationship to P6-43: P6-43 remains the recovery rule when
  a collision surfaces despite the proactive loading; P6-50 is
  the prevention rule that should make P6-43's reactive path a
  rare fallback, not the default.

**P6-51: DoD wording "update DECISIONS.md" is ambiguous —
pre-flight must lock placement (D-entry vs prose-in-produced-
doc) before execution (WP-035 execution + pre-commit review)**
- **What happened:** WP-035's §Definition of Done listed:
  *"`docs/ai/DECISIONS.md` updated — at minimum: why four
  environments (dev/test/staging/prod); why no hot-patching in
  prod; relationship between release validation gates and
  engine invariant checks (WP-031)."* The execution session
  read this as "the rationale must be captured somewhere in
  DECISIONS.md's authority" and placed the three rationales
  as prose inside the produced ops docs
  (`docs/ops/RELEASE_CHECKLIST.md` §Why these gates,
  `docs/ops/DEPLOYMENT_FLOW.md` §Why No Hot-Patching,
  `docs/ops/RELEASE_CHECKLIST.md` §Relationship to runtime
  invariant checks). Only D-3501 (the ops-subdirectory
  classification) landed as a discrete DECISIONS.md entry.
  The WP-035 pre-commit review flagged this as a non-blocking
  "DoD wording vs. where the rationale landed" nit — the
  rationale exists and is reachable, but the DoD's literal
  reading ("`DECISIONS.md` updated") expects discrete D-entries,
  not cross-document prose. A follow-up SPEC commit (`a9f6c1a`)
  added D-3502 (four environments), D-3503 (no hot-patching),
  and D-3504 (release gates ↔ runtime invariants) as back-
  pointer entries citing the authoritative prose in the ops
  docs.
- **Root cause:** The phrase "update DECISIONS.md with rationale
  for X" collapses two different artifacts into one sentence:
  (a) a discrete numbered D-entry under DECISIONS.md's
  "Decision — Rationale — Status" structure, versus (b) any
  rationale text placed somewhere reachable by citation (which
  could satisfy "DECISIONS.md updated" only if the produced doc
  is itself considered part of DECISIONS.md's authority, which
  it is not). The ambiguity lives in the template language, not
  in any specific WP. Executing sessions will default to the
  interpretation that minimizes file churn (prose in the
  produced doc, because the produced doc was going to exist
  anyway); pre-commit reviewers will default to the literal
  interpretation (discrete D-entries, because that matches
  DECISIONS.md's actual structure). The resolution is to
  force the decision at pre-flight time, before either default
  is reached.
- **Fix:** WP-035's Nit 1 was resolved in-session by a follow-
  up `SPEC:` commit adding D-3502 / D-3503 / D-3504 with
  explicit back-pointer language ("prose restatement is
  retained in `docs/ops/<doc>.md` §<section>"). The ops-doc
  prose remains authoritative; the D-entries exist for
  citation resolution and grep-based audits. The fix is to the
  pre-flight template, not to the WP execution.
- **Prevention:** Established Patterns now states: **any WP
  whose Definition of Done references "update DECISIONS.md with
  X" MUST have its session prompt explicitly lock the
  placement of X during pre-flight. Two valid forms:
  (1) "Discrete D-entry — a new D-NNNN entry in DECISIONS.md +
  a matching row in DECISIONS_INDEX.md, with the rationale
  structured under the canonical Decision / Rationale /
  Implications / Alternatives rejected / Status format"; or
  (2) "Prose-in-produced-doc with D-entry back-pointer — the
  authoritative rationale lives in a produced doc (e.g.,
  `docs/ops/<name>.md`), with a short back-pointer D-entry
  in DECISIONS.md citing the produced doc's relevant section."
  Pick one per rationale at pre-flight; do NOT leave the
  choice to the executing session. Session prompts should
  state the chosen form in §Locked Values as "DECISIONS.md
  placement: form (1)" or "form (2) with back-pointer citing
  `docs/<doc>.md §<section>`." When form (2) is chosen, the
  back-pointer D-entry MUST be a first-class entry (with its
  own DECISIONS_INDEX row) even if the authoritative content
  is outside DECISIONS.md — this preserves the citation chain
  for future audits. Form (1) is the default for rationales
  that do not have an obvious produced-doc home; form (2) is
  the default for WPs that produce a long-lived operational
  document where the rationale naturally belongs as reader-
  facing prose.** Applies to every WP whose DoD references
  DECISIONS.md updates alongside any other produced
  documentation artifact (WP-035's `docs/ops/` pattern;
  future WPs that produce `docs/content/`, `docs/network/`,
  or similar long-lived non-engine docs). Does NOT apply to
  WPs whose DoD only references DECISIONS.md (no produced-doc
  ambiguity to resolve) or WPs whose DoD only references a
  produced doc without mentioning DECISIONS.md (no D-entry
  expected). Relationship to P6-43 / P6-50: those govern the
  content discipline inside new engine files; P6-51 governs
  the **placement discipline** for rationale across
  DECISIONS.md and produced docs. The three precedents apply
  independently — a single WP may need all three (engine file
  with grep gates + produced doc with rationale + DoD-referenced
  DECISIONS.md updates).

**P6-52: Meta-prose containing a grep-guarded identifier trips the
guard even when the prose is arguing against the identifier (WP-039
execution + post-mortem §10.1)**
- **What happened:** WP-039's session prompt locked a Verification
  Step 5 grep guarding against parallel severity / counter types:
  `grep -rE "MetricPriority|MetricSeverity|MetricCategory|MetricEntry"
  packages/ docs/ops/LIVE_OPS_FRAMEWORK.md` with expected empty
  output. The executing session authored §5 Metric Label Conventions
  with an opening paragraph that read: *"The four labels below are
  organizational groupings … Any future proposal to typify them
  (e.g., introduce a `MetricCategory` union) is a separate pre-flight
  and requires its own DECISIONS.md entry."* The prose was arguing
  **against** introducing `MetricCategory` — explicitly telling
  future authors it is out of scope and requires its own DECISIONS.md
  entry. The grep does not read semantics; the literal identifier
  `MetricCategory` appeared in the file, and Step 5 returned a
  non-empty match. The fix was a paraphrase: *"Any future proposal
  to typify them into a code-level union is a separate pre-flight
  and requires its own DECISIONS.md entry."* — same semantic meaning,
  zero grep match. Step 5 then returned empty output and the
  framework doc committed cleanly. Recorded in the WP-039 post-mortem
  as the one reality-reconciliation item (§10.1). No scope expansion;
  no governance content lost.
- **Root cause:** Verification-step greps written as literal-identifier
  guards (e.g., `grep -E "ForbiddenName1|ForbiddenName2|…"`) operate
  on raw text, not on semantics. When authoring prose that discusses
  the forbidden identifier — to tell readers it is out of scope, to
  warn against it, to flag it as a future concern, or to record that
  a prior pre-flight rejected it — the natural authorial impulse is
  to quote the identifier by name so the reader can search for it.
  This impulse is correct for human reading but wrong for the grep.
  The prose-review pass reads the paragraph as clearly advocating
  against the identifier and passes; the verification-step grep
  reads the same paragraph as a match and fails. Both reviewers are
  reading correctly; the artifact is inconsistent. The asymmetry is
  intrinsic to literal-identifier greps and cannot be resolved by
  tweaking the grep pattern — adding `(?!.*"arg|e\.g\.|advocate|
  reject)` lookaheads would produce a rule that future authors
  cannot predict and that re-breaks on every new phrasing. The
  resolution has to live in the authoring discipline, not in the
  grep.
- **Fix:** In-session paraphrase — replace the literal identifier
  with a descriptive noun phrase that carries the same semantic
  content. Canonical transformations:
  - `MetricCategory union` → `code-level union`
  - `MetricPriority` → `parallel severity type`
  - `MetricEntry` → `parallel counter container`
  - `BetaFeedback` (if guarded) → `separate beta-feedback type`
  The paraphrase preserves the reader's understanding (the forbidden
  concept is still clearly identified) while eliminating the literal
  grep match. The post-mortem records the paraphrase under
  "reality-reconciliation findings" so the next executor can
  recognize the class.
- **Prevention:** Established Patterns now states: **any pre-flight
  that introduces a verification-step grep guarding a literal-
  identifier list MUST include a `§Locked Values` subsection titled
  "Authoring discipline for grep-guarded identifiers" stating
  verbatim: *"The identifiers listed in Verification Step N
  (`NAME1`, `NAME2`, …) MUST NOT appear in the produced document
  under any circumstance — including meta-prose arguing against
  them, parenthetical examples showing what not to introduce, or
  footnotes recording prior pre-flight rejections. Paraphrase each
  identifier to a descriptive noun phrase (e.g., `ForbiddenName` →
  `parallel <concept> type`). The grep cannot distinguish
  advocacy from opposition; the authoring discipline must."*
  The Locked Values entry MUST cite this precedent (P6-52) by
  number. Session prompts for WPs that forbid parallel types, forbid
  parallel containers, forbid specific hardcoded identifiers, or
  forbid specific constant names — all of which are verification-
  step grep patterns — inherit the same discipline. A paraphrase
  table of the form "forbidden identifier → paraphrased noun phrase"
  SHOULD be included in §Locked Values for WPs that need more than
  three identifiers guarded — the table preempts authorial impulse
  and prevents the executing session from re-deriving the
  paraphrase under verification-step pressure.** Applies to every
  WP whose verification steps include a literal-identifier grep with
  expected empty output. Does NOT apply to semantic greps (e.g.,
  "does this phrase appear anywhere") or to greps that target
  import statements or function-call patterns (where the grep is
  intrinsically about syntax, not about mentioning a word).
  Relationship to P6-43 / P6-50: P6-43 is the reactive rule when
  forbidden-token enumeration collides with `// why:` comments
  inside engine files; P6-50 is the proactive loading rule that
  makes P6-43's reactive path a rare fallback for engine files;
  P6-52 extends the same discipline to produced documents — the
  document author faces the same "I need to mention this to warn
  readers" impulse that engine-file authors face, and the same
  paraphrase-or-remove resolution applies. The three precedents
  govern three distinct surface classes (comments inside engine
  files / prose inside produced docs / verification-step grep
  authoring) but share a single principle: literal-identifier greps
  cannot distinguish mention from advocacy, so the authoring
  discipline must.

**P6-53: Context (Read First) must name every pre-existing authority
surface with a caps-tagged AUTHORITATIVE callout when the WP touches
that surface (WP-039 v1→v2 pre-flight Path A)**
- **What happened:** WP-039's original draft (pre-Path-A) proposed
  three engine-facing artifacts: `MetricPriority = 'P0' | 'P1' | 'P2'
  | 'P3'` as a new union in a new `packages/game-engine/src/metrics/`
  subdirectory; `MetricEntry { name, priority, category, notes? }` as
  a new container for the four operational counters; and a severity
  classification that split replay desync into "same-version = P0"
  vs. "cross-version = P1." The v1 pre-flight
  (`docs/ai/invocations/preflight-wp039-post-launch-metrics-live-
  ops.md`) returned DO NOT EXECUTE YET with three blocking findings.
  All three traced to the same root cause: the pre-Path-A WP-039 did
  not name `docs/ops/INCIDENT_RESPONSE.md` or `packages/game-engine/
  src/ops/ops.types.ts` in §Context (Read First), so the authoring
  pass did not re-check the landed surfaces before proposing
  parallel ones. `INCIDENT_RESPONSE.md` already defined the P0 / P1
  / P2 / P3 severity ladder with replay desync unconditionally P1;
  `ops.types.ts` already exported `IncidentSeverity` (the same
  union) and `OpsCounters` (the four-field container). The Path A
  rewrite resolved all three blockers by construction: dropped
  `MetricPriority` (cross-link to `IncidentSeverity`), dropped
  `MetricEntry` (cross-link to `OpsCounters`), dropped the
  same-version / cross-version split (cross-link to
  `INCIDENT_RESPONSE.md:33`). The rewrite added both authority
  sources to §Context (Read First) with an explicit caps-tagged
  **AUTHORITATIVE** callout: *"`docs/ops/INCIDENT_RESPONSE.md` —
  AUTHORITATIVE for severity semantics"* and *"`packages/game-engine/
  src/ops/ops.types.ts` — AUTHORITATIVE for ops types."* The v2
  pre-flight returned READY TO EXECUTE — all three v1 blockers
  resolved before a single line of code or new doc was authored.
- **Root cause:** §Context (Read First) is where a WP author
  enumerates the surfaces that must be re-read before the authoring
  pass begins. An incomplete §Context list does not fail any lint
  check and does not block any commit — the WP can be authored,
  approved, and handed to an executor without any signal that
  landed authority surfaces have been skipped. The authoring pass
  then proceeds from a partial authority chain: the author sees
  that no existing severity type is named, concludes that none
  exists, and proposes one. The drift is invisible until a
  pre-flight or review catches the duplicate — often after
  significant authoring effort has landed. The cost of the skip
  is asymmetric: adding a surface to §Context costs a line; leaving
  it out and catching the drift in pre-flight costs a Path A
  rewrite of the entire WP and EC. The resolution has to live at
  the WP-authoring stage, not at the pre-flight stage — pre-flight
  is the last defense, not the first.
- **Fix:** WP-039's Path A added `INCIDENT_RESPONSE.md` and
  `ops.types.ts` to §Context (Read First) with caps-tagged
  **AUTHORITATIVE** markers. The caps-tagging is deliberate: it
  signals to the authoring pass (and to every subsequent reviewer)
  that these surfaces are not merely relevant background — they
  are load-bearing authority that must be re-read and honored. The
  executing session then re-read both before writing a single line
  of `LIVE_OPS_FRAMEWORK.md`, and the framework doc's §3 and §4
  cross-link to both surfaces rather than re-deriving them. The
  post-mortem §5 records "Gate 6 (Authority-chain re-read
  confirmation) — both documents were read in full before a single
  line of LIVE_OPS_FRAMEWORK.md was written."
- **Prevention:** Established Patterns now states: **any WP whose
  scope touches an operational surface that already exists in the
  repo — a landed type, a landed document, a landed DECISIONS.md
  entry, a landed script, or a landed ops playbook — MUST enumerate
  each of those surfaces in §Context (Read First) with a caps-tagged
  **AUTHORITATIVE** marker. The minimum form is: `` - `<path>` —
  **AUTHORITATIVE for <what>.** <one-sentence description of what
  the surface owns and what the WP must not re-derive.>``
  The caps-tagging is not decorative — it is a contract between the
  WP author and every downstream reviewer that the named surface
  has been re-read, honored, and will not be duplicated. At least
  one AUTHORITATIVE callout is required for any WP that touches
  severity classification, counter observability, release gates,
  deployment flow, incident response, network authority, replay
  determinism, versioning, scoring, or any other operational
  surface with a landed authority. WPs that touch multiple such
  surfaces need one AUTHORITATIVE callout per surface. Pre-flight
  reviewers MUST verify that every surface referenced in the WP's
  Scope (In) or §Non-Negotiable Constraints appears in §Context
  (Read First) with the caps-tagged marker — a surface mentioned
  in scope but absent from AUTHORITATIVE Context is a pre-flight
  blocker. The pre-flight template §Context (Read First) section
  should include a checklist row: *"All pre-existing authority
  surfaces touched by this WP are listed with caps-tagged
  AUTHORITATIVE markers."*** Applies to every WP beyond pure
  greenfield packets (new code category with no existing authority
  surface). Does NOT apply to WPs whose Scope (In) introduces
  exclusively new surfaces with zero cross-reference to landed
  artifacts (rare — typically only the first WP in a new
  subdirectory chain). Relationship to P6-51: P6-51 governs
  placement discipline for rationale across DECISIONS.md and
  produced docs; P6-53 governs enumeration discipline for
  authority surfaces in §Context (Read First). P6-51 applies at
  session-prompt-authoring time; P6-53 applies at WP-authoring
  time. The two precedents are upstream-downstream — a WP that
  correctly names its authority surfaces via P6-53 will have a
  clearer path to correct DECISIONS.md placement via P6-51,
  because the authority-surface list informs whether a new
  rationale is a discrete D-entry (novel decision) or a
  back-pointer entry (cross-link to landed prose).

**P6-54: Contract changes on `MatchSetupConfig` field types or
formats require a GLOBAL grep for consumers, not a directory-scoped
enumeration (WP-113 mid-execution `economy.logic.ts` amendment +
post-execution `isSchemeRegistryReader` realignment)**
- **What happened:** WP-113 introduced two coupled changes — server
  registry wiring + a set-qualified `<setAbbr>/<slug>` ID format
  for all five `MatchSetupConfig` entity-ID fields. The EC §Locked
  Values "Builder internal-iterator updates (PS-7 LOCKED)" block
  enumerated four affected files by walking the `/setup/` and
  adjacent `/villainDeck/` + `/mastermind/` directories. Two
  consumers escaped the enumeration:
  - (a) **Mid-execution gap:** `packages/game-engine/src/economy/
    economy.logic.ts` `buildCardStats()` consumes
    `matchConfig.heroDeckIds`, `villainGroupIds`, and
    `henchmanGroupIds` as bare slugs at three internal iterators
    (lines 179, 195, 224). After the four enumerated builders
    landed and the EC §6 sequencing-rule steps 1-4 completed,
    `buildCardStats()` would silently produce empty `G.cardStats`
    for any qualified-ID config — breaking the loadout integration
    test's locked acceptance criterion ("G.cardStats populated for
    the chosen cards") and breaking runtime move costs
    (`fightVillain`, `recruitHero`, `fightMastermind`). The gap
    was discovered via runtime trace (`grep matchConfig\.heroDeckIds`
    returning five hits where the EC enumeration named four), the
    user authorized Option A inline, the WP §Mid-Execution
    Amendment block + EC PS-7 list extension + cap-raise 16 → 17
    landed, and the fix was applied per the existing PS-7 transform
    pattern. D-3103 precedent (WP-031) authorizes inline scope-
    neutral mid-execution amendments of this shape.
  - (b) **Post-execution gap:** `setup/buildSchemeSetupInstructions.ts`
    `isSchemeRegistryReader` checked for `getScheme(schemeId)`, a
    method that never existed on the real `CardRegistry` (the
    registry exposes `listSets` + `getSet` for set-level access;
    scheme data lives inside `getSet(abbr).schemes[]`). The
    pre-WP-113 builder body always fell into its early-return
    path because the guard always failed; the MVP "returns []"
    semantic per D-2601 was preserved by accident, not by design.
    WP-113's promotion of the misnamed guard to an export and its
    use at the orchestration site loudly fired a
    `"buildSchemeSetupInstructions skipped"` diagnostic on every
    match-create with a real registry — a noisy false positive
    surfaced by manual smoke testing. Fix Option A (post-execution
    alignment): realign the interface and guard to the real
    registry shape (`listSets` + `getSet`), matching the four other
    builder readers; body still returns `[]` per D-2601.
- **Root cause:** Both gaps share a directory-scope-vs-global-grep
  coverage problem at pre-flight time. The "builder" mental model
  treated setup-builder files as a structural set; (a)'s
  `economy.logic.ts` is functionally a setup builder (called from
  `buildInitialGameState`) but lives outside the `/setup/`-named
  directory. (b)'s misnamed guard checked a method name that was
  never verified against the real `CardRegistry` interface
  declaration in `packages/registry/src/types/index.ts`. Both gaps
  would have been caught by global-scope verification — (a) by a
  grep for `matchConfig\.{fieldName}` + `config\.{fieldName}`
  across all source directories, (b) by smoke-testing the guard's
  `typeof candidate.METHOD === 'function'` call against the actual
  exported interface shape rather than the structural-typing
  assumption.
- **Fix:** None required as a separate WP — both gaps were closed
  during the WP-113 execution arc (mid-execution amendment for (a),
  post-execution alignment fix for (b); both documented in
  DECISIONS.md D-10014). The lasting fix is to the pre-flight
  template, not to any individual WP.
- **Prevention:** Established Patterns now states the following
  pre-flight verifications for any WP that introduces a
  contract-changing transformation on a multi-consumer field type
  or format:
  - **Global consumer enumeration via grep, not directory walk.**
    When a WP changes a `MatchSetupConfig` field's value semantics
    or format (or any analogous cross-file contract — `G.foo`
    accessor pattern, hook-payload shape, snapshot-record field),
    the pre-flight session prompt's §Locked Values must include a
    grep manifest of every consumer site, derived from a single
    pass of `grep -rn "matchConfig\.{fieldName}\|config\.{fieldName}"
    packages/ apps/`, not from a curated list of directory names.
    The grep result becomes the §6 Files to Produce enumeration's
    starting point, not a sanity-check post-hoc.
  - **Type-guard real-method verification at promotion time.**
    When a WP promotes a type guard from local-scope to exported
    (so that other modules can use it for diagnostic detection or
    discriminated-union narrowing), the pre-flight must verify
    that every `typeof candidate.METHOD === 'function'` line
    inside the guard references a real method on the actual
    interface implementation it claims to recognize. The simplest
    discipline: read the canonical interface declaration (e.g.,
    `packages/registry/src/types/index.ts` for `CardRegistry`)
    side-by-side with the guard body, and confirm every method
    name in the guard appears in the interface. If a method name
    in the guard is aspirational or pre-dates an interface
    refactor, the guard is silently broken; promoting it to an
    export and using it at a diagnostic-emission site will
    transform the silent breakage into a noisy false positive
    that damages user trust in the diagnostic surface.
  Both rules apply to every Contract-Only or contract-changing WP
  beyond pure greenfield packets. Greenfield WPs (introducing a
  new field or interface with zero existing consumers) are
  trivially exempt — there are no consumers to enumerate, and no
  pre-existing guard to verify. Relationship to P6-50 / P6-43:
  P6-50 governs proactive grep-discipline rule loading at session-
  prompt-authoring time for purity-style greps; P6-54 governs
  proactive enumeration discipline at pre-flight time for
  cross-file contract changes. Relationship to P6-53: P6-53
  ensures every authority surface touched by a WP appears in
  §Context (Read First); P6-54 ensures every consumer of a
  contract being changed appears in §Files to Produce. P6-53 is a
  prose-completeness rule; P6-54 is a code-completeness rule.

**P6-55: Setup-builder error semantics — soft-skip when validator
is authoritative; throw only at the validator boundary
(WP-113 mid-execution build-vs-throw realignment)**
- **What happened:** WP-113's initial PS-7 transform of the four
  setup builders (`buildVillainDeck`, `buildMastermindState`,
  `buildSchemeSetupInstructions`, `buildHeroAbilityHooks`) followed
  the EC's "Throw if not found in named set (no cross-set
  fallback)" guardrail literally — the internal helpers
  (`findHenchmanGroupSlug`, `findSchemeSlug`, `findMastermindStrikes`,
  `findMastermindCards`) threw `Error` when their named-set lookup
  failed. The qualified-ID parsers similarly threw on malformed
  input. This was correct for the WP-113 stated case (real
  registry, validated config). But PS-3's widening of
  `EMPTY_REGISTRY` in `game.ts` from `{ listCards }` to
  `{ listCards, listSets, getSet }` flipped the four
  orchestration-side guards from `false` → `true` for the
  pre-existing test-bypass path (where `gameRegistry` is unset and
  the engine fall-back uses `EMPTY_REGISTRY`). Tests that
  previously hit the `isXRegistryReader → return empty` short-
  circuit now traversed the main builder path; the qualified-ID
  parsers parsed successfully (the test fixtures had been
  migrated to qualified form), but the named-set lookups all
  failed against the empty `EMPTY_REGISTRY`. Result: every test
  that called `LegendaryGame.setup()` or `buildInitialGameState`
  with `EMPTY_REGISTRY` (six engine test files; ~26 tests)
  threw mid-construction with `Villain group "test-villain-group-001"
  not found in set "test" flat cards.` The fix was to convert
  every "named-set not loaded / slug not in named set" throw to
  a soft-skip: helpers return `null`, builders skip the iteration
  step, the resulting `G` field comes out empty. Tests that don't
  assert on the empty fields pass; tests that do assert on
  populated fields use a real-shape mock registry instead of
  `EMPTY_REGISTRY`. The qualified-ID parser still rejects
  malformed input by returning `null`; the builder iterates over
  the `null` parse result by skipping (continues the loop).
  Documented in DECISIONS.md D-10014 § "Soft-skip semantic
  (validator-is-authoritative)".
- **Root cause:** Builder error semantics conflated two distinct
  failure cases that demand different responses:
  - **Case A — validator was bypassed.** Test path with
    `gameRegistry` unset (or any production path that skipped
    `validateMatchSetup` for any reason). The builder receives
    a config that was never format- or existence-checked. The
    correct response is degraded but inspectable: produce empty
    state so callers can introspect `G` and observe the missing
    data rather than crash mid-`Game.setup()` with a partial G.
  - **Case B — validator passed but data was misconfigured.**
    Production path with `gameRegistry` set, validator returned
    `{ ok: true }`, but the named-set lookup at builder time
    fails anyway. This is a real bug — either the validator and
    builder disagree about what counts as "loaded," or the
    registry mutated between validation and build (architecturally
    forbidden but worth defending against). The correct response
    is to throw and abort match creation.
  The initial WP-113 throws were correct for Case B but wrong for
  Case A. The validator IS the authoritative format/existence
  reporter — when it has run and returned `ok: true`, the builder
  can rely on its judgment; when it has been bypassed, the
  builder must defense-in-depth without throwing because there
  is no upstream guarantee.
- **Fix:** Builders now soft-skip on missing data (return null /
  empty) instead of throwing. The validator-is-authoritative
  principle is captured explicitly in D-10014 § "Soft-skip
  semantic". Tests either rely on the degraded-empty mode
  (acceptable when not asserting on the degraded field) or
  provide a real-shape mock registry (when asserting on positive
  data presence — e.g., the loadout integration test uses
  `buildLoadoutFixtureRegistry()` to satisfy all four guards
  with non-empty data).
- **Prevention:** Established Patterns now states: **setup
  builders that consume `MatchSetupConfig` (or any analogous
  validated-at-server-boundary contract) MUST follow the
  "validator-is-authoritative + builder-is-defense-in-depth"
  semantic. Specifically, when an internal lookup fails (set not
  loaded, slug not present, qualified-ID parse fails), the
  builder MUST return null / empty for that iteration and
  continue, NOT throw. Throws inside builder bodies are
  acceptable ONLY for invariant violations that cannot occur
  unless the validator itself is broken (e.g., mastermind cards
  array is non-array — a registry-schema-shape violation, not a
  config-content violation).** This rule applies to every WP
  that ships a setup-tier function consuming a validated
  contract — every `build*State`, `build*Hooks`, `build*Stats`,
  `build*Instructions` family member. Greenfield builders (no
  pre-existing test-bypass path) are still bound by the rule —
  the test-bypass path is implicit in the pre-WP-113 narrow-mock
  pattern (`{ listCards: () => [] }`) which every engine test
  inherits transitively. WPs that introduce a new setup builder
  should pre-load the EC's `// why:` comments on internal helpers
  with the soft-skip semantic explicitly so future readers (and
  future Claude sessions) don't re-derive the throw vs return-
  null trade-off. Relationship to P6-54: P6-54 governs the
  scope-of-enumeration discipline at pre-flight time (every
  consumer must be enumerated); P6-55 governs the error-semantic
  discipline at builder-implementation time (every internal
  failure must distinguish Case A from Case B). The two
  precedents are upstream-downstream — P6-54 ensures the right
  builders are touched; P6-55 ensures the touched builders
  degrade correctly when their inputs come from an unvalidated
  source. Relationship to game-engine.md "Throwing Convention"
  invariant ("Game.setup() may throw; moves never throw"):
  P6-55 narrows the `Game.setup()` throw permission to validator-
  surfaced errors and registry-schema-shape invariant violations
  only, NOT to internal builder lookup misses on a bypassed-
  validator path. The throwing convention's spirit is preserved:
  match creation aborts on invalid configs (when the validator
  catches them), but does not abort on configs that bypass the
  validator (because the test path is the only realistic
  bypass and degraded-empty G is more useful than mid-construction
  crash for those tests).

### Phase 6 continued — Identity & Profile Server Layer (WP-101, WP-102)

**P6-56: Wiring-site infrastructure singletons must be verified at
pre-flight, not at runtime (WP-102 execution + post-mortem §3.1)**
- **What happened:** WP-102 introduced `registerProfileRoutes(router,
  database)` taking a long-lived `pg.Pool` (`DatabaseClient`) as a
  caller-injected parameter; the WP wired it into
  `apps/server/src/server.mjs` via a one-line addition immediately
  after `registerHealthRoute(server.router)`. Pre-flight 2026-04-28
  declared READY TO EXECUTE based on the WP-052 `DatabaseClient`
  alias being available, the WP-101 `findAccountByHandle` being
  exported, and the Koa router being in scope at the proposed call
  site. None of these checks asked the necessary downstream question:
  *does a long-lived `pg.Pool` actually exist in `server.mjs` at the
  moment the new wiring lands?* It does not — the only `pg.Pool` in
  `server.mjs` is the rules-loader pool, which is created and
  `.end()`-ed inside a single `try…finally` at startup (a one-shot
  query lifetime, not a request-handler lifetime). The session prompt
  self-flagged this as §Goal #5 ("Pool lifecycle gate") but framed
  it as a runtime check ("if no `pg.Pool` is currently created, the
  executor must STOP"). The runtime check fired correctly mid-
  execution; the executor escalated, three options were authored
  (defer / add minimal pool / re-pre-flight), and Option A was
  authorized — defer route registration to a future request-handler
  WP, ship `registerProfileRoutes` exported but un-wired (`server.mjs`
  dropped from the Commit-A allowlist; 8 → 7 files; D-10202 records
  the deferral). The mid-execution amendment was clean (matches
  WP-053 `submitCompetitiveScore` shipped-but-unwired precedent), but
  the question should have been answered at pre-flight time.
- **Root cause:** Pre-flight §Dependency Contract Verification
  exhaustively enumerates type-level checks (names match, signatures
  compatible, fields exist in `G`, optional fields handled, locked
  values match producer code, file paths verified, function exports
  verified, forbidden imports verified) but had no entry for
  *runtime-singleton lifecycle verification at the wiring site*. The
  WP-052 / WP-103 precedents shipped contracts without consumer
  surfaces (no production caller — the function under test is the
  function under test). WP-053 explicitly used the `() => null`
  fail-closed default with no production caller and locked the
  lifecycle prohibition. WP-102 was the first WP in this layer to
  attempt the wiring step, and the pre-flight template inherited the
  prior WPs' "no consumer" assumption without noticing that WP-102
  was different. The runtime-only check in §Goal #5 was an
  appropriate safety net but not a substitute for the missing
  pre-flight gate — pre-flight is supposed to convert mid-execution
  STOP-and-escalate paths into pre-execution PS-N notes that keep the
  executor on a clean READY-to-Commit-A trajectory.
- **Fix:** Pre-flight §Dependency Contract Verification gains a new
  bullet *"Wiring-site infrastructure singletons exist at the
  proposed call site"* (this document, §Dependency Contract
  Verification, the bullet that follows the Filter/consumer input
  type bullet). The bullet enumerates three checks: constructor is
  present in the wiring-site file; the constructed singleton is
  long-lived (not `try…finally` short-lived); the constructed
  singleton is in lexical scope at the proposed call site. Failure
  on any of the three surfaces a PS-N note before READY, with two
  acceptable resolution paths: (a) defer to a future request-handler
  WP, ship un-wired (matches WP-053 / WP-102 precedent); (b) request
  scope expansion to include singleton creation in the wiring-site
  file (mid-execution amendment + new D-NNNN documenting the
  infrastructure-config decisions the amendment forces). Path (a) is
  the smaller-blast-radius default; path (b) is appropriate when no
  future request-handler WP is plausible within the project's
  planning horizon.
- **Prevention:** Established Patterns now states: **any WP that
  introduces a function taking a long-lived runtime singleton
  (`pg.Pool`, `CardRegistry`, `ParGate`, registry reader, similar)
  as a caller-injected parameter AND wires that function into a
  server-entry file (`apps/server/src/server.mjs`,
  `apps/server/src/index.mjs`, or equivalent) MUST be pre-flight-
  verified for wiring-site singleton availability before the verdict
  is declared READY. Pre-flight greps the named entry-point file for
  the singleton's constructor (`new Pool(`, `new pg.Pool(`,
  `createRegistryFromLocalFiles(`, `createParGate(`, etc.) and
  inspects the constructor's call-site context to confirm long-lived
  use and lexical scope.** This rule applies to every server-
  request-handler WP in the project from WP-102 onward. WP-102's
  own deferral (D-10202) is the canonical Option A precedent — when
  a wiring gap is detected and no future request-handler WP exists,
  ship the contract surface un-wired and let the future WP that
  owns pool lifecycle complete the wiring. The WP-053 fail-closed-
  default pattern (`() => null`) is the symmetric Option A precedent
  for non-pool runtime singletons.

**P6-57: EC-layer count-bounded grep gates collide with `// why:`
comment prose — paraphrase-the-policed-literal discipline (WP-101
post-mortem §3.2 + WP-102 post-mortem §3.2 confirmation)**
- **What happened:** WP-101's verification steps included six count-
  bounded grep gates (e.g., "exactly 1 match for `UPDATE
  legendary.players`", "exactly 3 matches for `ADD COLUMN IF NOT
  EXISTS`", "exactly N matches for `skip: 'requires test database'`",
  etc.) that targeted literal tokens in code files. The executing
  session authored `// why:` comments and module-header docstrings
  that named the policed literals verbatim — `"UPDATE
  legendary.players"` in a module-header explaining the single-
  writer invariant, `"ADD COLUMN IF NOT EXISTS x3"` in a migration
  header summarizing the idempotent DDL, `"{ skip: 'requires test
  database' }"` in a test-file docstring describing the locked
  WP-052 §3.1 pattern. Each comment was correct human-readable
  documentation but each tripped the count gate (1 → 2, 3 → 4, etc.)
  and was caught at the verification gate, then reworded as a
  paraphrase. The paraphrase pass cost ~10 minutes of mid-execution
  work and was captured in WP-101 post-mortem §3.2 as a recurring
  pattern. WP-102 then independently hit the **same failure mode in
  three new sites:** `requireAuthenticatedSession` in a route-handler
  `// why:`, `visibility IN ('public', 'link')` in a per-row
  narrowing `// why:`, `encodeURIComponent` in a client-API
  `// why:` — each triggered a Hard Stop or count gate; each was
  paraphrased and verified clean. Total: six independent grep-gate-
  prose collisions across two consecutive WPs in the same code
  category. Established the recurrence threshold for governance
  intervention.
- **Root cause:** Count-bounded grep gates in EC-layer verification
  steps share the same intrinsic asymmetry as the WP-layer literal-
  identifier guards governed by P6-43 / P6-50 / P6-52: the grep does
  not read code-vs-prose context, only raw text. P6-43 / P6-50 / P6-52
  collectively address (a) `// why:` comments inside engine files
  that mention forbidden engine identifiers, (b) the proactive load
  rule that prevents engine-file collisions, and (c) prose inside
  produced WP / EC documents that cites guarded identifiers in
  arguments-against. None of the three governs the EC-layer
  count-bounded gate pattern: the EC's verification step says
  "exactly 1 match for `UPDATE legendary.players` in `handle.logic.ts`",
  the executor writes a perfectly reasonable `// why:` comment that
  also mentions `UPDATE legendary.players` to explain the single-
  writer invariant, and the gate fires at count 2. The EC text is
  not a WP; the comment is in code, not in produced governance
  documents. Authorial impulse to quote the policed literal in
  documentation is correct for human readers but wrong for the
  count gate.
- **Fix:** EC-TEMPLATE §Rules gains a new bullet *"Grep-gate prose
  discipline"* (`docs/ai/execution-checklists/EC-TEMPLATE.md`,
  §Rules, last bullet) instructing EC authors that when a count-
  bounded grep gate targets a literal token, the policed literal
  MUST NOT appear in `// why:` comments, module-header docstrings,
  or any other prose in the same file. The bullet enumerates six
  worked examples drawn directly from WP-101 + WP-102 — three SQL/
  migration-keyword cases from WP-101 (`UPDATE legendary.players`,
  `ADD COLUMN IF NOT EXISTS`, `skip: 'requires test database'`) and
  three function/symbol cases from WP-102 (`requireAuthenticatedSession`,
  `visibility IN ('public', 'link')`, `encodeURIComponent`). Each
  example pairs the policed literal with the paraphrase that
  preserves intent and dropps the count-gate match. Future EC
  authors face the same authorial impulse and need the same
  paraphrase-or-cross-reference resolution; the EC-TEMPLATE bullet
  preempts the impulse.
- **Prevention:** Established Patterns now states: **EC verification
  steps that use count-bounded grep gates targeting literal tokens
  inherit the WP-layer P6-43 / P6-50 / P6-52 paraphrase discipline,
  and EC authors MUST NOT echo policed literals in prose inside
  files those gates target. Concrete authoring sub-rule: when
  `// why:` documentation needs to reference a policed literal, use
  cross-references ("the SQL filter above", "the function signature",
  "the locked skip pattern") rather than direct quotation. The
  EC-TEMPLATE §Rules bullet enumerates worked transformations.**
  Applies to every EC from this point forward whose verification
  steps include count-bounded literal greps. Does NOT apply to
  zero-match gates (those are governed by P6-52's authoring
  discipline directly) or to type-driven greps that match syntactic
  patterns rather than literal tokens. Relationship to P6-43 /
  P6-50 / P6-52: P6-43 governs forbidden-token comments in engine
  files; P6-50 makes P6-43 a rare fallback by proactively loading
  all known forbidden tokens at pre-flight time; P6-52 governs
  literal-identifier prose in produced documents at the WP /
  governance layer. P6-57 is the missing fourth quadrant — count-
  bounded gates at the EC layer where the gate text is written into
  the EC's verification steps and the matched file is the code
  file the EC governs. The four precedents (P6-43 / P6-50 / P6-52 /
  P6-57) collectively cover all three surface classes (engine-file
  comments / produced-document prose / EC-targeted code-file
  comments) for both gate sub-types (zero-match / count-bounded).
  The unifying principle: literal-token greps cannot distinguish
  mention from advocacy or prose-from-code; the authoring discipline
  must — and it lives in whichever artifact (code file, WP, EC)
  the gate's grep targets.
