  - Set "3dtc" hero "howard-the-duck" has 2 cards with cardCounts === 5 (Traveling Companion, Rebel Without a Cause). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/3dtc.patch.json (Phase 1b worklist).
  - Set "3dtc" hero "man-thing" has 2 cards with cardCounts === 5 (Form from Ooze, Burn the Fearful). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/3dtc.patch.json (Phase 1b worklist).
  - Set "anni" hero "brainstorm" has 2 cards with cardCounts === 5 (Time Loop Experiments, Borrow from the Future). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/anni.patch.json (Phase 1b worklist).
  - Set "anni" hero "fantastic-four-united" has 2 cards with cardCounts === 5 (Human Torch, Thing). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/anni.patch.json (Phase 1b worklist).
  - Set "anni" hero "heralds-of-galactus" has 2 cards with cardCounts === 5 (Firelord, Silver Surfer). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/anni.patch.json (Phase 1b worklist).
  - Set "anni" hero "psi-lord" has 2 cards with cardCounts === 5 (Avert Future Tragedy, Interdimensional Rescue). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/anni.patch.json (Phase 1b worklist).
  - Set "anni" hero "super-skrull" has 2 cards with cardCounts === 5 (Stretching Credibility, Rock Solid). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/anni.patch.json (Phase 1b worklist).
  - Set "antm" hero "ant-man" has 2 cards with cardCounts === 5 (Ride the Ants, Risky Science). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/antm.patch.json (Phase 1b worklist).
  - Set "antm" hero "black-knight" has 2 cards with cardCounts === 5 (Amulet of Avalon, Defend the Weak). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/antm.patch.json (Phase 1b worklist).
  - Set "antm" hero "jocasta" has 2 cards with cardCounts === 5 (Creation of Ultron, Reprocess). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/antm.patch.json (Phase 1b worklist).
  - Set "antm" hero "wonder-man" has 3 cards with cardCounts === 5 (One-Hit Wonder, Ionic Energy, Absorb Ambient Power). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/antm.patch.json (Phase 1b worklist).
  - Set "bkpt" hero "king-black-panther" has 2 cards with cardCounts === 5 (Unseen Protector, Vibranium Claws). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/bkpt.patch.json (Phase 1b worklist).
  - Set "bkpt" hero "queen-storm-of-wakanda" has 2 cards with cardCounts === 5 (Hurricane Winds, Torrential Downpour). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/bkpt.patch.json (Phase 1b worklist).
  - Set "bkpt" hero "princess-shuri" has 2 cards with cardCounts === 5 (Vibranium Experiments, Kimoyo Beads). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/bkpt.patch.json (Phase 1b worklist).
  - Set "bkpt" hero "general-okoye" has 2 cards with cardCounts === 5 (To My Last Breath, Lead the Dora Milaje). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/bkpt.patch.json (Phase 1b worklist).
  - Set "bkpt" hero "white-wolf" has 2 cards with cardCounts === 5 (Secret Assignment, Cloaking Tech Ambush). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/bkpt.patch.json (Phase 1b worklist).
  - Set "bkwd" hero "black-widow" has 2 cards with cardCounts === 5 (Evasive Acrobatics, Widow's Bite). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/bkwd.patch.json (Phase 1b worklist).
  - Set "bkwd" hero "yelena-belova" has 2 cards with cardCounts === 5 (Strike and Fade, Unveil Identity). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/bkwd.patch.json (Phase 1b worklist).
  - Set "bkwd" hero "red-guardian" has 2 cards with cardCounts === 5 (Sleeper Agent, Magnetic Shield). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/bkwd.patch.json (Phase 1b worklist).
  - Set "bkwd" hero "white-tiger" has 2 cards with cardCounts === 5 (Amulets of the Tiger God, Dark Influence of the Hand). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/bkwd.patch.json (Phase 1b worklist).
  - Set "ca75" hero "agent-x-13" has 2 cards with cardCounts === 5 (Sniper Squad, Paramilitary Ops). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/ca75.patch.json (Phase 1b worklist).
  - Set "ca75" hero "captain-america-falcon" has 2 cards with cardCounts === 5 (Aerial Catch, Winged Salvation). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/ca75.patch.json (Phase 1b worklist).
  - Set "ca75" hero "captain-america-1941" has 2 cards with cardCounts === 5 (Devoted Patriot, Storm the Beachhead). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/ca75.patch.json (Phase 1b worklist).
  - Set "ca75" hero "steve-rogers-director-of-shield" has 2 cards with cardCounts === 5 (International Strike Force, Reassign to Civilian Duty). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/ca75.patch.json (Phase 1b worklist).
  - Set "ca75" hero "winter-soldier" has 2 cards with cardCounts === 5 (Bionic Arm, Sniper Nest). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/ca75.patch.json (Phase 1b worklist).
  - Set "chmp" hero "gwenpool" has 2 cards with cardCounts === 5 (Come On, Nobody Reads Card Names, I'll Rescue You If I Feel Like It). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/chmp.patch.json (Phase 1b worklist).
  - Set "chmp" hero "ms-marvel" has 2 cards with cardCounts === 5 (Long Arm of the Law, Big Impact). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/chmp.patch.json (Phase 1b worklist).
  - Set "chmp" hero "nova" has 2 cards with cardCounts === 5 (Space Cop, Interstellar Hero). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/chmp.patch.json (Phase 1b worklist).
  - Set "chmp" hero "totally-awesome-hulk" has 2 cards with cardCounts === 5 (Beloved Behemoth, Incredible Mind, Awesome Body). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/chmp.patch.json (Phase 1b worklist).
  - Set "chmp" hero "viv-vision" has 2 cards with cardCounts === 5 (Walking Wi-Fi, Expanding Neural Network). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/chmp.patch.json (Phase 1b worklist).
  - Set "cvwr" hero "captain-america-secret-avenger" has 3 cards with cardCounts === 5 (Bold Leadership, Inspire a Nation, Inspire a Man). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/cvwr.patch.json (Phase 1b worklist).
  - Set "cvwr" hero "cloak-dagger" has 4 cards with cardCounts === 5 (Above, Below, Flee, Fight). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/cvwr.patch.json (Phase 1b worklist).
  - Set "cvwr" hero "cloak-dagger" has 2 cards with cardCounts === 3 (Darkness, Light). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/cvwr.patch.json (Phase 1b worklist).
  - Set "cvwr" hero "daredevil" has 2 cards with cardCounts === 5 (Dual Existence, Roundhouse Side Kick). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/cvwr.patch.json (Phase 1b worklist).
  - Set "cvwr" hero "daredevil" has 2 cards with cardCounts === 6 (Hidden Identity, Revealed Identity). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/cvwr.patch.json (Phase 1b worklist).
  - Set "cvwr" hero "falcon" has 3 cards with cardCounts === 5 (Rapid Reinforcements, Talk with Birds, Squawk Back). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/cvwr.patch.json (Phase 1b worklist).
  - Set "cvwr" hero "goliath" has 3 cards with cardCounts === 5 (Brilliant Biochemist, Massive Warrior, Growth Industry). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/cvwr.patch.json (Phase 1b worklist).
  - Set "cvwr" hero "hercules" has 3 cards with cardCounts === 5 (Manly Dullard, Boy Genius, Crowd Favorite). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/cvwr.patch.json (Phase 1b worklist).
  - Set "cvwr" hero "hulkling" has 3 cards with cardCounts === 5 (Half-Kree, Half-Skrull, Cellular Regeneration). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/cvwr.patch.json (Phase 1b worklist).
  - Set "cvwr" hero "luke-cage" has 3 cards with cardCounts === 5 (Cautious, Reckless, Take a Bullet for the Team). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/cvwr.patch.json (Phase 1b worklist).
  - Set "cvwr" hero "patriot" has 2 cards with cardCounts === 5 (New Generation of Heroes, Intuitive Tactician). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/cvwr.patch.json (Phase 1b worklist).
  - Set "cvwr" hero "patriot" has 2 cards with cardCounts === 3 (Incredible Effort, Effortless). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/cvwr.patch.json (Phase 1b worklist).
  - Set "cvwr" hero "peter-parker" has 2 cards with cardCounts === 5 (Conflicted Loyalties, Spider-Man Unmasked). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/cvwr.patch.json (Phase 1b worklist).
  - Set "cvwr" hero "peter-parker" has 2 cards with cardCounts === 3 (Protect My Family, Hot Bowl of Soup). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/cvwr.patch.json (Phase 1b worklist).
  - Set "cvwr" hero "speedball" has 2 cards with cardCounts === 5 (Reckless Rescue Attempt, Bounce Around). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/cvwr.patch.json (Phase 1b worklist).
  - Set "cvwr" hero "speedball" has 2 cards with cardCounts === 3 (Double Down, Bubble Up). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/cvwr.patch.json (Phase 1b worklist).
  - Set "cvwr" hero "stature" has 3 cards with cardCounts === 5 (Shrink to Nothing, Crush Ants, Crush File Sizes). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/cvwr.patch.json (Phase 1b worklist).
  - Set "cvwr" hero "storm-black-panther" has 4 cards with cardCounts === 5 (Gathering Rain Clouds, Gathering Clues, Lightning Strike, Pouncing Strike). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/cvwr.patch.json (Phase 1b worklist).
  - Set "cvwr" hero "storm-black-panther" has 2 cards with cardCounts === 3 (Tsunami of Water, Tsunami of Justice). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/cvwr.patch.json (Phase 1b worklist).
  - Set "cvwr" hero "tigra" has 3 cards with cardCounts === 5 (Friendship, Ferocity, Supernatural Senses). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/cvwr.patch.json (Phase 1b worklist).
  - Set "cvwr" hero "vision" has 2 cards with cardCounts === 5 (Solar Energy, Through Solid Objects). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/cvwr.patch.json (Phase 1b worklist).
  - Set "cvwr" hero "vision" has 2 cards with cardCounts === 3 (Lighter than Air, Harder than Diamond). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/cvwr.patch.json (Phase 1b worklist).
  - Set "cvwr" hero "wiccan" has 2 cards with cardCounts === 5 (Sorcerous Illusions, Astral Projection). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/cvwr.patch.json (Phase 1b worklist).
  - Set "cvwr" hero "wiccan" has 2 cards with cardCounts === 3 (Supersonic Spells, Supersonic Speed). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/cvwr.patch.json (Phase 1b worklist).
  - Set "core" hero "captain-america" has 2 cards with cardCounts === 5 (Avengers Assemble!, Perfect Teamwork). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/core.patch.json (Phase 1b worklist).
  - Set "core" hero "cyclops" has 2 cards with cardCounts === 5 (Determination, Optic Blast). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/core.patch.json (Phase 1b worklist).
  - Set "core" hero "emma-frost" has 2 cards with cardCounts === 5 (Mental Discipline, Shadowed Thoughts). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/core.patch.json (Phase 1b worklist).
  - Set "core" hero "gambit" has 2 cards with cardCounts === 5 (Card Shark, Stack the Deck). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/core.patch.json (Phase 1b worklist).
  - Set "core" hero "nick-fury" has 2 cards with cardCounts === 5 (Battlefield Promotion, High-Tech Weaponry). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/core.patch.json (Phase 1b worklist).
  - Set "core" hero "rogue" has 2 cards with cardCounts === 5 (Borrowed Brawn, Energy Drain). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/core.patch.json (Phase 1b worklist).
  - Set "core" hero "spider-man" has 2 cards with cardCounts === 5 (Astonishing Strength, Great Responsibility). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/core.patch.json (Phase 1b worklist).
  - Set "core" hero "storm" has 2 cards with cardCounts === 5 (Gathering Stormclouds, Lightning Bolt). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/core.patch.json (Phase 1b worklist).
  - Set "dkcy" hero "angel" has 2 cards with cardCounts === 5 (Diving Catch, High-Speed Chase). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/dkcy.patch.json (Phase 1b worklist).
  - Set "dkcy" hero "bishop" has 2 cards with cardCounts === 5 (Absorb Energies, Whatever the Cost). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/dkcy.patch.json (Phase 1b worklist).
  - Set "dkcy" hero "daredevil" has 2 cards with cardCounts === 5 (Backflip, Radar Sense). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/dkcy.patch.json (Phase 1b worklist).
  - Set "dkcy" hero "elektra" has 2 cards with cardCounts === 5 (First Strike, Ninjitsu). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/dkcy.patch.json (Phase 1b worklist).
  - Set "dkcy" hero "ghost-rider" has 2 cards with cardCounts === 5 (Hell on Wheels, Infernal Chains). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/dkcy.patch.json (Phase 1b worklist).
  - Set "dkcy" hero "iceman" has 2 cards with cardCounts === 5 (Deep Freeze, Ice Slide). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/dkcy.patch.json (Phase 1b worklist).
  - Set "dkcy" hero "iron-fist" has 2 cards with cardCounts === 5 (Focus Chi, Wield the Iron Fist). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/dkcy.patch.json (Phase 1b worklist).
  - Set "dkcy" hero "jean-grey" has 2 cards with cardCounts === 5 (Psychic Search, Read Your Thoughts). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/dkcy.patch.json (Phase 1b worklist).
  - Set "dkcy" hero "nightcrawler" has 2 cards with cardCounts === 5 (Bamf!, Blend Into Shadows). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/dkcy.patch.json (Phase 1b worklist).
  - Set "dkcy" hero "professor-x" has 2 cards with cardCounts === 5 (Psionic Astral Form, Class Dismissed). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/dkcy.patch.json (Phase 1b worklist).
  - Set "dkcy" hero "punisher" has 2 cards with cardCounts === 5 (Boom Goes the Dynamite, Hail of Bullets). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/dkcy.patch.json (Phase 1b worklist).
  - Set "dims" hero "jessica-jones" has 2 cards with cardCounts === 5 (Alter Ego, Alias Investigations). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/dims.patch.json (Phase 1b worklist).
  - Set "dims" hero "ms-america" has 2 cards with cardCounts === 5 (Star Power, Search Parallel Dimensions). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/dims.patch.json (Phase 1b worklist).
  - Set "dims" hero "squirrel-girl" has 2 cards with cardCounts === 5 (Find Tiny Friends, Nut Punch). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/dims.patch.json (Phase 1b worklist).
  - Set "dstr" hero "doctor-strange" has 2 cards with cardCounts === 5 (Wand of Watoomb, Keeper of the Sanctum). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/dstr.patch.json (Phase 1b worklist).
  - Set "dstr" hero "doctor-voodoo" has 2 cards with cardCounts === 5 (Commune with the Spirit World, Medallion of Many Loas). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/dstr.patch.json (Phase 1b worklist).
  - Set "dstr" hero "clea" has 2 cards with cardCounts === 5 (Prepare Dark Magic, Demonic Descendant). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/dstr.patch.json (Phase 1b worklist).
  - Set "dstr" hero "ancient-one-the" has 2 cards with cardCounts === 5 (Astral Confrontation, Teachings of Kamar-Taj). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/dstr.patch.json (Phase 1b worklist).
  - Set "dstr" hero "vishanti-the" has 2 cards with cardCounts === 5 (Oshtur, Hoggoth). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/dstr.patch.json (Phase 1b worklist).
  - Set "fear" hero "greithoth-breaker-of-wills" has 2 cards with cardCounts === 5 (Absorb Metal, Mace of Chains). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/fear.patch.json (Phase 1b worklist).
  - Set "fear" hero "kuurth-breaker-of-stone" has 2 cards with cardCounts === 5 (Reach for Power, Unstoppable Sledge). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/fear.patch.json (Phase 1b worklist).
  - Set "fear" hero "nerkkod-breaker-of-oceans" has 2 cards with cardCounts === 5 (Pull of the Tides, Feed My Undersea Legions). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/fear.patch.json (Phase 1b worklist).
  - Set "fear" hero "nul-breaker-of-worlds" has 2 cards with cardCounts === 5 (Demolition Derby, Otherworldly Maul). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/fear.patch.json (Phase 1b worklist).
  - Set "fear" hero "skadi" has 2 cards with cardCounts === 5 (Dark Prophecy, Ancient Oath of HYDRA). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/fear.patch.json (Phase 1b worklist).
  - Set "fear" hero "skirn-breaker-of-men" has 2 cards with cardCounts === 5 (Towering Leader, Underhanded Dealings). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/fear.patch.json (Phase 1b worklist).
  - Set "ff04" hero "human-torch" has 2 cards with cardCounts === 5 (Call for Backup, Hothead). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/ff04.patch.json (Phase 1b worklist).
  - Set "ff04" hero "invisible-woman" has 2 cards with cardCounts === 5 (Disappearing Act, Four of a Kind). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/ff04.patch.json (Phase 1b worklist).
  - Set "ff04" hero "mr-fantastic" has 2 cards with cardCounts === 5 (Twisting Equations, Unstable Molecules). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/ff04.patch.json (Phase 1b worklist).
  - Set "ff04" hero "silver-surfer" has 2 cards with cardCounts === 5 (Warp Speed, Epic Destiny). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/ff04.patch.json (Phase 1b worklist).
  - Set "ff04" hero "thing" has 2 cards with cardCounts === 5 (It Started on Yancy Street, Knuckle Sandwich). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/ff04.patch.json (Phase 1b worklist).
  - Set "gotg" hero "drax-the-destroyer" has 2 cards with cardCounts === 5 (Knives of the Hunter, Interstellar Tracker). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/gotg.patch.json (Phase 1b worklist).
  - Set "gotg" hero "gamora" has 2 cards with cardCounts === 5 (Bounty Hunter, Deadliest Woman in the Universe). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/gotg.patch.json (Phase 1b worklist).
  - Set "gotg" hero "groot" has 2 cards with cardCounts === 5 (Prune the Growths, Surviving Sprig). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/gotg.patch.json (Phase 1b worklist).
  - Set "gotg" hero "rocket-raccoon" has 2 cards with cardCounts === 5 (Gritty Scavenger, Trigger Happy). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/gotg.patch.json (Phase 1b worklist).
  - Set "gotg" hero "star-lord" has 2 cards with cardCounts === 5 (Element Guns, Legendary Outlaw). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/gotg.patch.json (Phase 1b worklist).
  - Set "asrd" hero "beta-ray-bill" has 2 cards with cardCounts === 5 (Hope of the Korbinites, Bio-Engineered Cyborg). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/asrd.patch.json (Phase 1b worklist).
  - Set "asrd" hero "lady-sif" has 2 cards with cardCounts === 5 (Dimensional Blade, Weapons Master). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/asrd.patch.json (Phase 1b worklist).
  - Set "asrd" hero "thor" has 2 cards with cardCounts === 5 (Test of Virtue, Divine Lightning). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/asrd.patch.json (Phase 1b worklist).
  - Set "asrd" hero "valkyrie" has 2 cards with cardCounts === 5 (Dragonfang, Flying Stallion). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/asrd.patch.json (Phase 1b worklist).
  - Set "asrd" hero "warriors-three-the" has 2 cards with cardCounts === 5 (Fandral the Dashing, Hogun the Grim). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/asrd.patch.json (Phase 1b worklist).
  - Set "cosm" hero "adam-warlock" has 2 cards with cardCounts === 5 (Transmute Matter, Regenerative Cocoon). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/cosm.patch.json (Phase 1b worklist).
  - Set "cosm" hero "captain-mar-vell" has 2 cards with cardCounts === 5 (Cosmic Awareness, Kree Genetics). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/cosm.patch.json (Phase 1b worklist).
  - Set "cosm" hero "moondragon" has 2 cards with cardCounts === 5 (Peaceful Meditation, Psionic Warning). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/cosm.patch.json (Phase 1b worklist).
  - Set "cosm" hero "nebula" has 2 cards with cardCounts === 5 (Ruthless Cyborg, Galactic Rogue). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/cosm.patch.json (Phase 1b worklist).
  - Set "cosm" hero "nova" has 2 cards with cardCounts === 5 (Draw From the Worldmind, Electromagnetic Wave). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/cosm.patch.json (Phase 1b worklist).
  - Set "cosm" hero "phyla-vell" has 2 cards with cardCounts === 5 (Channel Cosmic Power, Quantum Sword). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/cosm.patch.json (Phase 1b worklist).
  - Set "cosm" hero "quasar" has 2 cards with cardCounts === 5 (Manipulate Gravitons, Cosmic Champion). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/cosm.patch.json (Phase 1b worklist).
  - Set "cosm" hero "ronan-the-accuser" has 2 cards with cardCounts === 5 (Universal Weapon, Rally Kree Warriors). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/cosm.patch.json (Phase 1b worklist).
  - Set "cosm" hero "yondu" has 2 cards with cardCounts === 5 (Whistling Arrow, Interstellar Hunter). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/cosm.patch.json (Phase 1b worklist).
  - Set "msp1" hero "black-widow" has 2 cards with cardCounts === 5 (Dangerous Rescue, Mission Accomplished). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/msp1.patch.json (Phase 1b worklist).
  - Set "msp1" hero "captain-america" has 2 cards with cardCounts === 5 (Avengers Assemble!, Perfect Teamwork). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/msp1.patch.json (Phase 1b worklist).
  - Set "msp1" hero "hawkeye" has 2 cards with cardCounts === 5 (Quick Draw, Team Player). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/msp1.patch.json (Phase 1b worklist).
  - Set "msp1" hero "hulk" has 2 cards with cardCounts === 5 (Growing Anger, Unstoppable Hulk). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/msp1.patch.json (Phase 1b worklist).
  - Set "msp1" hero "iron-man" has 2 cards with cardCounts === 5 (Endless Invention, Repulsor Rays). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/msp1.patch.json (Phase 1b worklist).
  - Set "msp1" hero "nick-fury" has 2 cards with cardCounts === 5 (Battlefield Promotion, High-Tech Weaponry). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/msp1.patch.json (Phase 1b worklist).
  - Set "msp1" hero "thor" has 2 cards with cardCounts === 5 (Odinson, Surge of Power). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/msp1.patch.json (Phase 1b worklist).
  - Set "msmc" hero "stepford-cuckoos" has 3 cards with cardCounts === 4 (Find Mutants with Cerebro, Shared Thoughts, Telepathic Warning). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/msmc.patch.json (Phase 1b worklist).
  - Set "mdns" hero "blade-daywalker" has 2 cards with cardCounts === 5 (Where Monsters Lurk, Ride by Moonlight). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/mdns.patch.json (Phase 1b worklist).
  - Set "mdns" hero "elsa-bloodstone" has 2 cards with cardCounts === 5 (Axe of the Slayer, Silver Bullets). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/mdns.patch.json (Phase 1b worklist).
  - Set "mdns" hero "morbius" has 2 cards with cardCounts === 5 (Mesmerize, Insatiable Craving). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/mdns.patch.json (Phase 1b worklist).
  - Set "mdns" hero "werewolf-by-night" has 2 cards with cardCounts === 5 (Starlit Path, Snarling Fangs). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/mdns.patch.json (Phase 1b worklist).
  - Set "mdns" hero "wong-master-of-the-mystic-arts" has 2 cards with cardCounts === 5 (Bridge Between Dimensions, Searing Shards of Sunlight). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/mdns.patch.json (Phase 1b worklist).
  - Set "mgtg" hero "star-lord" has 3 cards with cardCounts === 3 (Starship Sensors, Borrowed Nova Blaster, Expandable Helmet). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/mgtg.patch.json (Phase 1b worklist).
  - Set "mgtg" hero "star-lord" has 3 cards with cardCounts === 2 (Give, Take, Don't Need that Stuff). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/mgtg.patch.json (Phase 1b worklist).
  - Set "mgtg" hero "gamora" has 3 cards with cardCounts === 3 (Sharpen Blades, Resourceful Fugitive, Retractable Sword). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/mgtg.patch.json (Phase 1b worklist).
  - Set "mgtg" hero "gamora" has 3 cards with cardCounts === 2 (Forgive, Resent, Stolen Necroblaster). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/mgtg.patch.json (Phase 1b worklist).
  - Set "mgtg" hero "rocket-groot" has 5 cards with cardCounts === 3 (Baby Groot, Passion, Compassion, Don't Press this Button, Press the Button). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/mgtg.patch.json (Phase 1b worklist).
  - Set "mgtg" hero "rocket-groot" has 3 cards with cardCounts === 2 (Gravity Mines, Tricky, Simple). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/mgtg.patch.json (Phase 1b worklist).
  - Set "mgtg" hero "drax" has 4 cards with cardCounts === 3 (Nothing Goes over my Head, Prison Riot, I am Invisible, Xandar is Invincible). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/mgtg.patch.json (Phase 1b worklist).
  - Set "mgtg" hero "drax" has 3 cards with cardCounts === 2 (Remove his Spine, Also Illegal, Dual Knives). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/mgtg.patch.json (Phase 1b worklist).
  - Set "mgtg" hero "mantis" has 4 cards with cardCounts === 3 (Empathic Bond, Selfless, Selfish, Inspire Courage). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/mgtg.patch.json (Phase 1b worklist).
  - Set "mgtg" hero "mantis" has 2 cards with cardCounts === 2 (Sleep, Emotional Wave). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/mgtg.patch.json (Phase 1b worklist).
  - Set "msis" hero "doctor-strange" has 3 cards with cardCounts === 3 (Open Portals, Defend this Dimension, Sift Futures). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/msis.patch.json (Phase 1b worklist).
  - Set "msis" hero "doctor-strange" has 2 cards with cardCounts === 2 (Invoke the Time Stone, Bind Evil). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/msis.patch.json (Phase 1b worklist).
  - Set "msis" hero "black-panther" has 3 cards with cardCounts === 3 (Avengers Reassembled, Great Many Lives Lost, Wakanda Forever). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/msis.patch.json (Phase 1b worklist).
  - Set "msis" hero "black-panther" has 2 cards with cardCounts === 2 (Vibranium Nanites, Council of War). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/msis.patch.json (Phase 1b worklist).
  - Set "msis" hero "bruce-banner" has 3 cards with cardCounts === 3 (Burst of Rage, Hulkbuster Armor, Brains and Brawn). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/msis.patch.json (Phase 1b worklist).
  - Set "msis" hero "bruce-banner" has 2 cards with cardCounts === 2 (Hulk Gets Smashed, Crush Puny Weaklings). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/msis.patch.json (Phase 1b worklist).
  - Set "msis" hero "captain-marvel" has 3 cards with cardCounts === 3 (Return from the Stars, Turning Point, Infused by the Tesseract). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/msis.patch.json (Phase 1b worklist).
  - Set "msis" hero "captain-marvel" has 2 cards with cardCounts === 2 (Moment of Destiny, Dawning Hope). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/msis.patch.json (Phase 1b worklist).
  - Set "msis" hero "wanda-vision" has 4 cards with cardCounts === 3 (We Have to Destroy It, Witchcraft, Hold On, Let Go). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/msis.patch.json (Phase 1b worklist).
  - Set "msis" hero "wanda-vision" has 4 cards with cardCounts === 2 (Magic, Science, Rage, Grief). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/msis.patch.json (Phase 1b worklist).
  - Set "noir" hero "angel-noir" has 2 cards with cardCounts === 5 (Impetuous Dive, Multitalented). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/noir.patch.json (Phase 1b worklist).
  - Set "noir" hero "daredevil-noir" has 2 cards with cardCounts === 5 (Balancing Act, Listen for Heartbeats). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/noir.patch.json (Phase 1b worklist).
  - Set "noir" hero "iron-man-noir" has 2 cards with cardCounts === 5 (Steam-Powered Arsenal, Mechanized Plate-Mail). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/noir.patch.json (Phase 1b worklist).
  - Set "noir" hero "luke-cage-noir" has 2 cards with cardCounts === 5 (Follow Big Leads, Private Investigations). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/noir.patch.json (Phase 1b worklist).
  - Set "noir" hero "spider-man-noir" has 2 cards with cardCounts === 5 (Gumshoe's Revolver, Webs of Darkness). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/noir.patch.json (Phase 1b worklist).
  - Set "pttr" hero "black-cat" has 2 cards with cardCounts === 5 (Casual Bank Robbery, Pickpocket). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/pttr.patch.json (Phase 1b worklist).
  - Set "pttr" hero "moon-knight" has 2 cards with cardCounts === 5 (Climbing Claws, Lunar Communion). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/pttr.patch.json (Phase 1b worklist).
  - Set "pttr" hero "scarlet-spider" has 2 cards with cardCounts === 5 (Flip Out, Perfect Hunter). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/pttr.patch.json (Phase 1b worklist).
  - Set "pttr" hero "spider-woman" has 2 cards with cardCounts === 5 (Bioelectric Shock, Radioactive Spider). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/pttr.patch.json (Phase 1b worklist).
  - Set "pttr" hero "symbiote-spider-man" has 2 cards with cardCounts === 5 (Dark Strength, Spider-Sense Tingling). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/pttr.patch.json (Phase 1b worklist).
  - Set "rlmk" hero "medusa" has 2 cards with cardCounts === 5 (Queen of the Inhumans, Splitting Hairs). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/rlmk.patch.json (Phase 1b worklist).
  - Set "rlmk" hero "crystal" has 2 cards with cardCounts === 5 (Earth, Air, Fire, and Water, Master the Four Elements). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/rlmk.patch.json (Phase 1b worklist).
  - Set "rlmk" hero "karnak" has 2 cards with cardCounts === 5 (Brilliant Strategist, Find Fatal Flaw). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/rlmk.patch.json (Phase 1b worklist).
  - Set "rlmk" hero "gorgon" has 2 cards with cardCounts === 5 (Lockjaw, Inhuman's Best Friend, Stomping Shockwave). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/rlmk.patch.json (Phase 1b worklist).
  - Set "rvlt" hero "captain-marvel-agent-of-shield" has 2 cards with cardCounts === 5 (The Sword of S.H.I.E.L.D., Radiant Blast). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/rvlt.patch.json (Phase 1b worklist).
  - Set "rvlt" hero "darkhawk" has 2 cards with cardCounts === 5 (Balance the Darkforce, Hawk Dive). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/rvlt.patch.json (Phase 1b worklist).
  - Set "rvlt" hero "hellcat" has 2 cards with cardCounts === 5 (Catlike Agility, Part-Time PI). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/rvlt.patch.json (Phase 1b worklist).
  - Set "rvlt" hero "photon" has 2 cards with cardCounts === 5 (Infrared Conversation, Ultraviolet Radiation). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/rvlt.patch.json (Phase 1b worklist).
  - Set "rvlt" hero "quicksilver" has 2 cards with cardCounts === 5 (Too Fast to See, Perpetual Motion). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/rvlt.patch.json (Phase 1b worklist).
  - Set "rvlt" hero "ronin" has 2 cards with cardCounts === 5 (Mysterious Identity, Storm of Arrows). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/rvlt.patch.json (Phase 1b worklist).
  - Set "rvlt" hero "scarlet-witch" has 2 cards with cardCounts === 5 (Hex Bolt, Alter Reality). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/rvlt.patch.json (Phase 1b worklist).
  - Set "rvlt" hero "speed" has 2 cards with cardCounts === 5 (Accelerate, Speedy Delivery). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/rvlt.patch.json (Phase 1b worklist).
  - Set "rvlt" hero "war-machine" has 2 cards with cardCounts === 5 (Simulated Target Practice, Military-Industrial Complex). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/rvlt.patch.json (Phase 1b worklist).
  - Set "shld" hero "agent-phil-coulson" has 2 cards with cardCounts === 5 (Impeccable Planning, Build the Strike Team). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/shld.patch.json (Phase 1b worklist).
  - Set "shld" hero "deathlok" has 2 cards with cardCounts === 5 (Authorize Lethal Force, Reanimate Into Service). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/shld.patch.json (Phase 1b worklist).
  - Set "shld" hero "mockingbird" has 2 cards with cardCounts === 5 (Take Cover, Battle Staves). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/shld.patch.json (Phase 1b worklist).
  - Set "shld" hero "quake" has 2 cards with cardCounts === 5 (Going Underground, Aftershock). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/shld.patch.json (Phase 1b worklist).
  - Set "smhc" hero "happy-hogan" has 2 cards with cardCounts === 5 (Head of Security, Watchful Eye). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/smhc.patch.json (Phase 1b worklist).
  - Set "smhc" hero "high-tech-spider-man" has 2 cards with cardCounts === 5 (Advanced Targeting System, Recon Drone Connection). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/smhc.patch.json (Phase 1b worklist).
  - Set "smhc" hero "peter-parker-homecoming" has 2 cards with cardCounts === 5 (Avenger in Training, Heightened Senses). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/smhc.patch.json (Phase 1b worklist).
  - Set "smhc" hero "peters-allies" has 2 cards with cardCounts === 5 (Ned, Michelle). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/smhc.patch.json (Phase 1b worklist).
  - Set "smhc" hero "tony-stark" has 2 cards with cardCounts === 5 (Genius, Billionaire..., Stay Out of Trouble). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/smhc.patch.json (Phase 1b worklist).
  - Set "ssw1" hero "black-bolt" has 2 cards with cardCounts === 5 (Destructive Whisper, Speak No Words). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/ssw1.patch.json (Phase 1b worklist).
  - Set "ssw1" hero "black-panther" has 2 cards with cardCounts === 5 (Catlike Reflexes, Multifaceted Genius). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/ssw1.patch.json (Phase 1b worklist).
  - Set "ssw1" hero "captain-marvel" has 2 cards with cardCounts === 5 (Absorb Energies, Supersonic Flight). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/ssw1.patch.json (Phase 1b worklist).
  - Set "ssw1" hero "dr-strange" has 2 cards with cardCounts === 5 (Cloak of Levitation, Trust Me, I'm a Doctor). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/ssw1.patch.json (Phase 1b worklist).
  - Set "ssw1" hero "lady-thor" has 2 cards with cardCounts === 5 (Mysterious Origin, Chosen by Asgard). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/ssw1.patch.json (Phase 1b worklist).
  - Set "ssw1" hero "magik" has 2 cards with cardCounts === 5 (Rally the New Mutants, Travel through Limbo). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/ssw1.patch.json (Phase 1b worklist).
  - Set "ssw1" hero "maximus" has 2 cards with cardCounts === 5 (Mental Domination, Enslave the Will). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/ssw1.patch.json (Phase 1b worklist).
  - Set "ssw1" hero "namor-the-sub-mariner" has 2 cards with cardCounts === 5 (Lead the Armies of Atlantis, Ruler of the Seas). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/ssw1.patch.json (Phase 1b worklist).
  - Set "ssw1" hero "old-man-logan" has 2 cards with cardCounts === 5 (Last Survivor, Loner). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/ssw1.patch.json (Phase 1b worklist).
  - Set "ssw1" hero "proxima-midnight" has 2 cards with cardCounts === 5 (Inspiration Through Power, Master Combatant). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/ssw1.patch.json (Phase 1b worklist).
  - Set "ssw1" hero "superior-iron-man" has 2 cards with cardCounts === 5 (Armor Upgrades, Optimized Technology). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/ssw1.patch.json (Phase 1b worklist).
  - Set "ssw1" hero "thanos" has 2 cards with cardCounts === 5 (Transdimensional Overlord, Revel in Destruction). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/ssw1.patch.json (Phase 1b worklist).
  - Set "ssw1" hero "ultimate-spider-man" has 2 cards with cardCounts === 5 (Leaping Spider, Marvel Team-Up). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/ssw1.patch.json (Phase 1b worklist).
  - Set "ssw2" hero "agent-venom" has 2 cards with cardCounts === 5 (Multi-Gun, Government Payroll). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/ssw2.patch.json (Phase 1b worklist).
  - Set "ssw2" hero "arkon-the-magnificent" has 2 cards with cardCounts === 5 (All-Terrain Barbarian, Quiver of Thunderbolts). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/ssw2.patch.json (Phase 1b worklist).
  - Set "ssw2" hero "beast" has 2 cards with cardCounts === 5 (Balanced Attack, Upside-down Thinking). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/ssw2.patch.json (Phase 1b worklist).
  - Set "ssw2" hero "black-swan" has 2 cards with cardCounts === 5 (Apocalyptic Vision, Witness the End). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/ssw2.patch.json (Phase 1b worklist).
  - Set "ssw2" hero "captain-and-the-devil-the" has 2 cards with cardCounts === 5 (Jurassic America, Patriotic Chomp). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/ssw2.patch.json (Phase 1b worklist).
  - Set "ssw2" hero "captain-britain" has 2 cards with cardCounts === 5 (Transatlantic Savior, Combined Strength). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/ssw2.patch.json (Phase 1b worklist).
  - Set "ssw2" hero "corvus-glaive" has 2 cards with cardCounts === 5 (Let None Escape You, Culling Blade). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/ssw2.patch.json (Phase 1b worklist).
  - Set "ssw2" hero "dr-punisher-soldier-supreme" has 2 cards with cardCounts === 5 (Sweep the Streets of Trash, You're a Slow Learner). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/ssw2.patch.json (Phase 1b worklist).
  - Set "ssw2" hero "elsa-bloodstone" has 2 cards with cardCounts === 5 (Monster Hunter, Bloodstone Pendant). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/ssw2.patch.json (Phase 1b worklist).
  - Set "ssw2" hero "phoenix-force-cyclops" has 2 cards with cardCounts === 5 (Reincarnate, Burn Out). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/ssw2.patch.json (Phase 1b worklist).
  - Set "ssw2" hero "ruby-summers" has 2 cards with cardCounts === 5 (Guerrilla Warfare, Heir to Legends). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/ssw2.patch.json (Phase 1b worklist).
  - Set "ssw2" hero "shang-chi" has 2 cards with cardCounts === 5 (Shuffling Footwork, Acrobatic Kung-Fu). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/ssw2.patch.json (Phase 1b worklist).
  - Set "ssw2" hero "silk" has 2 cards with cardCounts === 5 (Long-Range Spider-Sense, Cascading Maneuver). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/ssw2.patch.json (Phase 1b worklist).
  - Set "ssw2" hero "soulsword-colossus" has 2 cards with cardCounts === 5 (Invade the Inferno, Steel Interception). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/ssw2.patch.json (Phase 1b worklist).
  - Set "ssw2" hero "spider-gwen" has 2 cards with cardCounts === 5 (Fateful Bridge, Save the Day). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/ssw2.patch.json (Phase 1b worklist).
  - Set "ssw2" hero "time-traveling-jean-grey" has 2 cards with cardCounts === 5 (Throw Over the Railing, Bridge to a Better Future). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/ssw2.patch.json (Phase 1b worklist).
  - Set "vnom" hero "carnage" has 2 cards with cardCounts === 5 (Rending Claws, Carnivore). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/vnom.patch.json (Phase 1b worklist).
  - Set "vnom" hero "venom" has 2 cards with cardCounts === 5 (Devouring Drool, Razor Teeth). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/vnom.patch.json (Phase 1b worklist).
  - Set "vnom" hero "venom-rocket" has 2 cards with cardCounts === 5 (Hungry for Action, Spring the Trap). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/vnom.patch.json (Phase 1b worklist).
  - Set "vnom" hero "venomized-dr-strange" has 2 cards with cardCounts === 5 (Cauldron of the Cosmos, See Future Timelines). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/vnom.patch.json (Phase 1b worklist).
  - Set "vnom" hero "venompool" has 2 cards with cardCounts === 5 (Digest That Chimichanga, Shenanigans). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/vnom.patch.json (Phase 1b worklist).
  - Set "vill" hero "bullseye" has 2 cards with cardCounts === 5 (Fulfill the Contract, Everything's a Weapon). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/vill.patch.json (Phase 1b worklist).
  - Set "vill" hero "dr-octopus" has 2 cards with cardCounts === 5 (Brilliant Research, Crazed Experiments). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/vill.patch.json (Phase 1b worklist).
  - Set "vill" hero "electro" has 2 cards with cardCounts === 5 (Electroshock Therapy, Shocking Robbery). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/vill.patch.json (Phase 1b worklist).
  - Set "vill" hero "enchantress" has 2 cards with cardCounts === 5 (Enchant the Senses, Soul Sacrifice). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/vill.patch.json (Phase 1b worklist).
  - Set "vill" hero "green-goblin" has 2 cards with cardCounts === 5 (Pumpkin Bombs, Goblin Glider). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/vill.patch.json (Phase 1b worklist).
  - Set "vill" hero "juggernaut" has 2 cards with cardCounts === 5 (Crimson Gem of Cyttorak, Size Matters). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/vill.patch.json (Phase 1b worklist).
  - Set "vill" hero "kingpin" has 2 cards with cardCounts === 5 (Pull the Strings, Recruitment Day). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/vill.patch.json (Phase 1b worklist).
  - Set "vill" hero "kraven" has 2 cards with cardCounts === 5 (Ceaseless Tracker, Corner the Prey). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/vill.patch.json (Phase 1b worklist).
  - Set "vill" hero "loki" has 2 cards with cardCounts === 5 (All Humans Are Expendable, Illusionary Bindings). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/vill.patch.json (Phase 1b worklist).
  - Set "vill" hero "magneto" has 2 cards with cardCounts === 5 (Magnetic Levitation, Mutants Will Rule). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/vill.patch.json (Phase 1b worklist).
  - Set "vill" hero "mysterio" has 2 cards with cardCounts === 5 (Psychedelic Mist, Shifting Decoy). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/vill.patch.json (Phase 1b worklist).
  - Set "vill" hero "mystique" has 2 cards with cardCounts === 5 (Show Your True Colors, Hidden Weapons). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/vill.patch.json (Phase 1b worklist).
  - Set "vill" hero "sabretooth" has 2 cards with cardCounts === 5 (Leap of the Tiger, Take One for the Team). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/vill.patch.json (Phase 1b worklist).
  - Set "vill" hero "ultron" has 2 cards with cardCounts === 5 (Encephalo-Ray, Army of Ultrons). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/vill.patch.json (Phase 1b worklist).
  - Set "vill" hero "venom" has 2 cards with cardCounts === 5 (Symbiote Takeover, Devour). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/vill.patch.json (Phase 1b worklist).
  - Set "wwhk" hero "amadeus-cho" has 3 cards with cardCounts === 5 (Extrapolate, Gamma-Draining Nanites, Like Totally Smart Hulk). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/wwhk.patch.json (Phase 1b worklist).
  - Set "wwhk" hero "bruce-banner" has 3 cards with cardCounts === 5 (Solve the Impossible, Gamma Bomb Disaster, Savage Hulk Unleashed). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/wwhk.patch.json (Phase 1b worklist).
  - Set "wwhk" hero "caiera" has 2 cards with cardCounts === 5 (Shadow Queen, Shadowforged Blade). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/wwhk.patch.json (Phase 1b worklist).
  - Set "wwhk" hero "gladiator-hulk" has 3 cards with cardCounts === 5 (Don't Make Me Angry, Seize The Throne, Hulk Is King). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/wwhk.patch.json (Phase 1b worklist).
  - Set "wwhk" hero "hiroim" has 3 cards with cardCounts === 5 (Seek Redemption, Save from the Rubble, Hiroim Redeemed). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/wwhk.patch.json (Phase 1b worklist).
  - Set "wwhk" hero "hulkbuster-iron-man" has 2 cards with cardCounts === 5 (Pound for Pound, Attune Techtonic Transducer). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/wwhk.patch.json (Phase 1b worklist).
  - Set "wwhk" hero "hulkbuster-iron-man" has 2 cards with cardCounts === 3 (Build the Suit, Ultra-Massive Armor). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/wwhk.patch.json (Phase 1b worklist).
  - Set "wwhk" hero "joe-fixit-grey-hulk" has 2 cards with cardCounts === 5 (Carefully Considered Smashing, Threaten And Bribe). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/wwhk.patch.json (Phase 1b worklist).
  - Set "wwhk" hero "joe-fixit-grey-hulk" has 2 cards with cardCounts === 3 (Ambitious Enforcer, Underworld Boss). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/wwhk.patch.json (Phase 1b worklist).
  - Set "wwhk" hero "korg" has 2 cards with cardCounts === 5 (Nothing Beats Rock, Move Mountains). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/wwhk.patch.json (Phase 1b worklist).
  - Set "wwhk" hero "korg" has 2 cards with cardCounts === 3 (Forged by Fire, Lord of Granite). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/wwhk.patch.json (Phase 1b worklist).
  - Set "wwhk" hero "miek-the-unhived" has 2 cards with cardCounts === 5 (This Bug Smashes You, Devouring Frenzy). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/wwhk.patch.json (Phase 1b worklist).
  - Set "wwhk" hero "namora" has 2 cards with cardCounts === 5 (Crushing Tsunami, Heart of the Ocean). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/wwhk.patch.json (Phase 1b worklist).
  - Set "wwhk" hero "namora" has 2 cards with cardCounts === 3 (Herculean Effort, Master of Depths). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/wwhk.patch.json (Phase 1b worklist).
  - Set "wwhk" hero "no-name-brood-queen" has 3 cards with cardCounts === 5 (Surprise Attack, Appetite for Destruction, Torrent of Broodlings). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/wwhk.patch.json (Phase 1b worklist).
  - Set "wwhk" hero "rick-jones" has 3 cards with cardCounts === 5 (Hacktivist, Seek the Nega-Bands, Captain Marvel). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/wwhk.patch.json (Phase 1b worklist).
  - Set "wwhk" hero "rick-jones" has 2 cards with cardCounts === 3 (Irradiated Blood, A-Bomb). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/wwhk.patch.json (Phase 1b worklist).
  - Set "wwhk" hero "sentry" has 3 cards with cardCounts === 5 (Agoraphobia, Golden Guardian of Good, Rival Personalities). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/wwhk.patch.json (Phase 1b worklist).
  - Set "wwhk" hero "sentry" has 2 cards with cardCounts === 3 (Mournful Sentinel, The Void Unchained). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/wwhk.patch.json (Phase 1b worklist).
  - Set "wwhk" hero "she-hulk" has 3 cards with cardCounts === 5 (Hurl Legal Objections, Hurl Trucks, Window of Opportunity). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/wwhk.patch.json (Phase 1b worklist).
  - Set "wwhk" hero "skaar-son-of-hulk" has 2 cards with cardCounts === 5 (Anger Management, Scarred Past). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/wwhk.patch.json (Phase 1b worklist).
  - Set "wwhk" hero "skaar-son-of-hulk" has 2 cards with cardCounts === 3 (Mood Swings, Raging Savage). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/wwhk.patch.json (Phase 1b worklist).
  - Set "xmen" hero "aurora-northstar" has 3 cards with cardCounts === 5 (Northern Lights, Blazing Flare, Blazing Fists). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/xmen.patch.json (Phase 1b worklist).
  - Set "xmen" hero "banshee" has 2 cards with cardCounts === 5 (Sonar Detection, Speed of Sound). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/xmen.patch.json (Phase 1b worklist).
  - Set "xmen" hero "beast" has 2 cards with cardCounts === 5 (Captivating Conundrum, Furry Fury). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/xmen.patch.json (Phase 1b worklist).
  - Set "xmen" hero "cannonball" has 2 cards with cardCounts === 5 (Kinetic Blast Field, Carry to the Air). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/xmen.patch.json (Phase 1b worklist).
  - Set "xmen" hero "colossus-wolverine" has 3 cards with cardCounts === 5 (Reliable, Unpredictable, Fastball Special). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/xmen.patch.json (Phase 1b worklist).
  - Set "xmen" hero "dazzler" has 2 cards with cardCounts === 5 (Convert Sound to Light, Dazzling Glamour). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/xmen.patch.json (Phase 1b worklist).
  - Set "xmen" hero "havok" has 2 cards with cardCounts === 5 (Blinding Burst, Unleash Havok). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/xmen.patch.json (Phase 1b worklist).
  - Set "xmen" hero "jubilee" has 2 cards with cardCounts === 5 (Light a Spark, Blasting Fireworks). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/xmen.patch.json (Phase 1b worklist).
  - Set "xmen" hero "kitty-pryde" has 2 cards with cardCounts === 5 (Intangible Qualities, Going through a Phase). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/xmen.patch.json (Phase 1b worklist).
  - Set "xmen" hero "legion" has 4 cards with cardCounts === 5 (Bend Steel, Bend Light, Split Personality, Split Eardrums). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/xmen.patch.json (Phase 1b worklist).
  - Set "xmen" hero "legion" has 2 cards with cardCounts === 3 (Channel Time, Channel Fire). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/xmen.patch.json (Phase 1b worklist).
  - Set "xmen" hero "longshot" has 2 cards with cardCounts === 5 (Fortune Favors the Bold, Flurry of Blades). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/xmen.patch.json (Phase 1b worklist).
  - Set "xmen" hero "phoenix" has 2 cards with cardCounts === 5 (Life & Death Incarnate, Obliterating Fire). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/xmen.patch.json (Phase 1b worklist).
  - Set "xmen" hero "polaris" has 2 cards with cardCounts === 5 (Ride the Magnetic Waves, Electromagnetic Pulse). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/xmen.patch.json (Phase 1b worklist).
  - Set "xmen" hero "psylocke" has 2 cards with cardCounts === 5 (Psychic Knife, Precognition). This is a candidate paired-equal pattern — if these are split-side dual-faced cards (e.g., bkwd/falcon-winter-soldier Attune/Atone), add an explicit physicalCards[] declaration to scripts/convert-cards/inputs/patches/xmen.patch.json (Phase 1b worklist).
